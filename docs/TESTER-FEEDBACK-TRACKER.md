# Tester Feedback & Support Workstream Tracker

**File:** /Users/liborballaty/LocalProjects/GitHubProjectsDocuments/fala_madeira/docs/TESTER-FEEDBACK-TRACKER.md
**Description:** Durable tracker for tester-reported bugs, the support-ticket workstream, and every deferred item in it. Standing rule (owner directive 2026-07-14): nothing is closed by declaring it "not our lane" — every deferral is logged here with owner + next action so it cannot get buried.
**Author:** Libor Ballaty
**Created:** 2026-07-14
**Last Updated:** 2026-07-17
**Last Updated By:** claude-en23 (EN-23 admin audio panel APPROVED + BUILT, EN-8-independent slice)

---

> **⇄ Lane B ↔ Lane A alignment (2026-07-15):** shared-write files to keep in sync — `SettingsView.tsx` (preserve `data-testid` `path-switcher`/`goal-track-chooser` — specs 36/37 depend on them), `src/paths/index.ts` (SEC-2 WP1 `paths:selection` namespacing — existing-value migration Lane A flagged is now HANDLED), `src/paths/goal-track.ts` + `src/App.tsx` logout (reserve before editing — 3 streams touch it), specs `user/36`+`user/37` (shared). Decisions in TB-11 · TB-12 · COORD-1 · COORD-2 · SEC-1 (Lane A addenda).

---

> **★ EN-8 SERVER-HOSTED AUDIO — status 2026-07-16 (owner-approved staging trial COMPLETE + mechanism PROVEN).** Branch `feat/en8-server-hosted-audio` (pushed; behind develop 12 / ahead 14). Active plans in the SUPPORT worktree (`fala_madeira-support/plans/`): `plan-2026-07-15-en8-hosted-audio.yaml` (+ state `.plan-state-en8-hosted-audio.yaml`) = Phases 0–7 done; **NEW `plan-2026-07-16-en8-completion.yaml`** = the go-forward plan (Phase 1 close staging trial, Phase 2 land on develop, Phase 3 GATED prod activation).
> - **Live on the SHARED Supabase project (owner-approved):** migration `00012` applied+verified (public `tts-audio` bucket + `tts_audio_public_read` RLS + `tts-audio-orphan-backstop` pg_cron); `log-sink` edge fn deployed (`audio-sync-confirm`, `--no-verify-jwt`; 401/200 verified); `AUDIO_SYNC_TOKEN` set (staging). **`gemini` left at v10 (untouched)** — runtime write-back not needed for the trial (flag `TTS_BUFFER_WRITEBACK` OFF; pregen uploads directly).
> - **Staging Verpex cron LIVE:** `pull.php` at `testfalamadeira.searchingfool.com/audio-sync/` (php-lint clean on server, web-guard 403, `*/15` cron). **Mechanism PROVEN end-to-end:** pregen→buffer→cron copy→Verpex serves **200 application/octet-stream** (real PCM) → client skips provider. Buffer copy-confirm-emptied to 0.
> - **Open (in the completion plan):** finish the level-0 warm (**83/527** hosted — throttled by a live **provider sustained rate-limit finding**, the exact 503-class failure EN-8 fixes) → formal `VERIFY_WIN`; then land on develop + `update-docs`; then GATED prod activation (needs a fix for the **single-buffer multi-env contention**: first cron to copy deletes from the buffer, so env #2 misses). Throwaway pregen user `en8-pregen@falamadeira.internal` (uid 1fe2dc9f) pending cleanup.
> - **Follow-ups (NEEDS REQUIREMENTS / backlog):** in-app admin audio-management panel — **now EN-23** (APPROVED + BUILT 2026-07-17 on branch `feat/en23`, EN-8-independent slice; EN-8 server-tier + `pregen --from-queue` wire-up still owed when EN-8 lands on develop; ties EN-12); PCM compression pass (raw ~48KB/s). `public/.htaccess` `/audio` 404 rule ships with the next web deploy (client content-type guard covers it meanwhile).

---

## Status legend
`OPEN` active · `IN PROGRESS` being worked · `DEFERRED` parked-but-tracked (never "dropped") · `DONE` complete/verified · `NEEDS DECISION` blocked on a product/owner call · `NEEDS REQUIREMENTS` captured from feedback but **not buildable** until a written spec is drafted AND owner-approved (AGENTS §3 requirements gate — no coding before then)

---

## Security

### SEC-2 — Cross-user data bleed on a shared device (reporter: owner, staging 2026-07-15) — `APPROVED + BUILT (on develop; full regression green; renamed from SEC-1 → SEC-2 to resolve the id collision with the edge-rate-limiting SEC-1)`
- **Report (owner):** on the same device, logged in as liborballaty@gmail.com, logged out, logged in as testuser@testuser.com — the profile/settings appeared to be the same. "Check carefully… users must be properly isolated both on device and in Supabase."
- **Investigation (2026-07-15, two read-only audits + live DB verification):**
  - **Supabase (server) — ISOLATED, verified LIVE.** All 17 public tables have RLS ON (0 RLS-OFF); every per-user table (`profiles`, `user_track_selection`, `user_situation_progress`, `mastery_items`, `missions_log`, `lessons`, `tickets`, `logs`, `pronunciation_attempts`, `writing_submissions`) is scoped to `auth.uid()` with `WITH CHECK` on writes. `global_settings` SELECT is `USING true` **by design** (readable config, no secrets). **User B cannot read or write user A's DB rows.** Verified against live `pg_policies`/`pg_tables.rowsecurity` (two-pass: migrations + live agree). Minor non-leak symmetry gaps: `video_suggestions` UPDATE is admin-only (no owner-UPDATE); `lesson_corrections` has no owner-DELETE. Low priority, DB-agent territory.
  - **↩ UPDATE (Lane A, 2026-07-15, EN-15 migration `00013`):** the `profiles` SELECT/UPDATE policies are now `auth.uid()=id **OR public.is_admin()**` (were owner-only when this audit ran — that was itself drift from 00001, which had documented an admin bypass). This does **not** weaken user↔user isolation (a non-admin B still cannot read/write A; the OR only grants *admins* full-profile access, which is intended and needed for the EN-15 admin grant control). Flagging so this entry's "profiles scoped to auth.uid()" line is read as "auth.uid() OR admin" going forward. See EN-15.
  - **Client device storage — REAL LEAK (root cause).** Several durable stores use **fixed device-global keys** (not per-user) and **nothing clears them on logout** (`handleLogout` clears React state + the Supabase session only): `paths:selection` (path type + active track + cursor — the "same profile" symptom), `sync:queue` (A's offline writes; RLS rejects cross-user drain server-side but the queue stalls and A's writes sit readable on-device), `missions:log:local`, settings (`is_sound_enabled`/`playback_speed`/`global_voice_limit`/`offline_save_audio`/`offline_cache_limit_bytes`), `active_lessons_month_*`, `fm_theme`, and the TTS audio-blob cache. Correctly scoped already: `onboarding:record:${userId}`, `home:streak-freeze:${userId}`. Also: for a brand-new user B, `fetchProfile`'s `getPrefsForNewProfile()` seeds B's new DB profile with A's device prefs from localStorage.
- **Fix plan (owner-approved 2026-07-15):** `docs/SEC-2-USER-ISOLATION-FIX-PLAN.md` — namespace user-scoped durable keys by userId (WP1), clear device-global stores + fix new-profile seeding on logout (WP2), namespace + guard the sync queue by user (WP3), a login-A→logout→login-B isolation regression test (WP5); **audio-cache-on-logout DEFERRED to coordinate with the other agent's active EN-8 work** (`audioCache.ts`/`storage.web.ts`, WP4). No schema changes.
- **Conflict check:** EN-8 (other agent) is actively restructuring `audioCache.ts` + `storage.web.ts` → this plan does NOT touch those. `sync-queue.ts` is the other agent's file but no active WIP (reserve + coordinate). All other targets are conflict-free. No active queue locks by others.
- **★ EN-8 OWNER ACCEPTS WP4 (audio-cache-on-logout) — 2026-07-15:** confirmed. EN-8 owns `audioCache.ts`/`storage.web.ts` and now also adds a NEW device-global store (the pinned offline store), so the audio-store logout policy is EN-8's. **Owner decision (2026-07-15):** on logout, **clear the LRU audio cache** (can hold user-private tutor free-chat audio) and **KEEP the pinned offline-download store** (curated PUBLIC content only — no PII; preserves offline downloads for the next user). Folded into `plans/plan-2026-07-15-en8-hosted-audio.yaml` as step `logout-clears-lru-cache` (Phase 3, fatal), wiring `audioCache.clear()` (LRU-only) into `App.tsx` `onLogoutCleanup`. **Coordination:** WP4 stays with EN-8; Lane B keeps WP1/WP2/WP3/WP5. Both edit `App.tsx` logout — sequence via reserve on `src/App.tsx` (EN-8 adds the LRU-clear call; Lane B clears the KV/global stores) to avoid a collision.
- **Note (tracker hygiene) — RESOLVED (Lane B, 2026-07-15):** renamed this device-bleed item from `SEC-1` → `SEC-2` (the edge-rate-limiting `SEC-1` from 2026-07-14 keeps the original id). All client code/tests/plan authored under this item now reference `SEC-2`; plan doc renamed `docs/SEC-2-USER-ISOLATION-FIX-PLAN.md`.
- **Owner:** Lane B (client fix WP1-3,5); EN-8 agent (WP4 audio-cache-on-logout — accepted); DB-agent (the 2 RLS symmetry gaps). **Status:** BUILT on develop, full regression green; NOT shipped (TB-11b release .4 + this held per owner). WP4 rides the EN-8 branch.
- **✅ Lane B implementation (2026-07-15, owner-approved):**
  - **WP1** — `paths:selection` namespaced per user (`paths:selection:${userId}`) in `src/paths/index.ts`. **Lane A's migration gap: HANDLED** — on first load under the new key, the legacy device-global value is adopted into the signed-in user's namespace ONCE then the legacy key is deleted (preserves path type + structured cursor; activeTrackId is separately restored from `user_track_selection`). Residual: on a shared device the first user to open post-update inherits the device's last path *type* once — strictly less than the pre-fix continuous bleed, non-sensitive UI state only.
  - **WP2** — logout resets device prefs (`useSettings.resetForLogout` → DB profile re-applies the next user's prefs on login) + `clearDeviceUserState()` (`src/lib/session-cleanup.ts`) clears `active_lessons_month_*`, `missions:log:local`, and the legacy `paths:selection`. **App.tsx `onLogoutCleanup`: my KV/prefs clearing landed; EN-8 layers its `audioCache.clear()` (LRU) after — no collision (verified EN-8 not yet in that block).**
  - **WP3** — sync-queue drain guard (`isForeignQueueEntry`, exported + unit-tested): a queued write whose `payload.user_id` ≠ the signed-in user is skipped and retained (RLS is the server backstop). Storage-key namespacing of the singleton queue was assessed as higher-risk-than-value on the deadlock-hardened flush path; the guard provides the isolation guarantee.
  - **WP5** — unit: `sync-queue.guard.test.ts`, `session-cleanup.test.ts`; e2e `user/56` (3 tests: selection under per-user key never the shared key; **WP1 legacy→per-user migration**; **WP2 logout clears device state** — added `writeKv`/`deleteKv` IndexedDB seed helpers since the app reads IndexedDB first). Updated specs 14/24/31/36/37 to the per-user key (`readKvByPrefix` helper). Full gate green.
  - **Coverage follow-up (tracked, not a blocker):** `useSettings.resetForLogout`'s **new-user seeding** path (a brand-new user B post-logout is seeded with defaults, not user A's device prefs) is not e2e-covered — needs a two-user signup flow (login A → logout → sign up brand-new B → assert B seeded with defaults). Single-user e2e can't isolate it (`applyProfilePrefs` re-applies DB prefs on login). Low risk (trivial state reset + DB-authoritative prefs). Owner: Lane B.
  - **Shared-write confirmed:** `SettingsView.tsx` `data-testid` `path-switcher`+`goal-track-chooser` intact; TB-11 chooser + TB-11b deep-link coexist.
- **↩ Lane A review (2026-07-15):** plan is sound — server isolation verified live, and namespace+clear+guard is the right design.
  - **WP4 (audio-on-logout) — endorsed:** the "clear LRU (holds private free-chat audio) / keep pinned (curated public)" decision is exactly the curated-vs-dynamic split; it's the same signal COORD-2's `hostable` flag draws — good reuse.
  - **WP1 — one open gap: existing-value migration.** Namespacing `paths:selection` → `paths:selection:${userId}` orphans the CURRENT device-global value, so on first load after the fix the existing user's path type + active track reset to defaults. (Interacts with TB-11's goal chooser, which writes `activeTrackId` into that key — leak is pre-existing, not introduced by TB-11.) Either migrate the current value into the active user's namespaced key on first load, or explicitly accept + note the one-time reset in WP1.
  - **COORD-2 residual (FYI):** the new `audio-sync-confirm` delete endpoint (shared-token) — token must be strong+rotated and hard-scoped to the `tts-audio` buffer bucket only; throttle+payload-validation already in the plan. Bounded/self-healing; not blocking.

## Tester-reported bugs

### TB-1 — Placement level not reflected + not changeable (reporter: dancingtoothbrush) — `DECIDED (Option B) + REQUIREMENTS DRAFTED (docs/TB-1-PROFICIENCY-LEVEL-REQUIREMENTS.md, 2026-07-15) → awaiting owner approval + DB coordination before build`
- **Report:** "It says I'm Absolute Beginner even though I said I could have a simple conversation, but I can't seem to change it."
- **Root cause (confirmed, code-read 2026-07-14):**
  - Onboarding `complete()` persists placement (`placementLevel` 0/1/2 from "Complete beginner / A few words / Basic conversation") **only to `platform.storage`** (`useOnboarding.ts` OnboardingRecord).
  - Onboarding's `profiles.update` (`useOnboarding.ts:142-148`) writes **only** `has_accepted_terms` + `has_accepted_ai_usage` — it **never writes a level**.
  - Home shows `getLevelName(profile?.unlocked_level || 1)` (`HomeView.tsx:151`) → with `unlocked_level` unset it defaults to **1 = "Absolute Beginner"**, regardless of placement.
  - No post-onboarding UI to change level; `unlocked_level` is access-key/paywall gated (`HomeView.tsx:358` "Enter your access key to unlock Month N+1").
- **Decision needed (owner):** the display conflates two concepts — **paywall `unlocked_level`** vs **proficiency/placement level**. Options:
  - (A) onboarding placement sets `profile.unlocked_level` — simplest, BUT `unlocked_level` also gates paid content, so a free placement choice could unlock paid months. *(verify gating first)*
  - (B) add a separate proficiency/practical-level field driven by placement; Home label reads that; keep `unlocked_level` for the paywall. Cleanest, small migration.
  - (C) keep as-is but add a Settings "change my level" control + write placement→display level.
- **★ OWNER DECISION (2026-07-15) — Option B:** proficiency level and paywall access are **two totally separate, unrelated concepts.** The user's **proficiency/practical level** (driven by placement) must be a distinct field from **`unlocked_level`** (which stays PURELY the paywall/access-key gate). One never influences the other. This resolves the conflation; (A) is explicitly rejected (a free placement answer must never unlock paid content).
- **Implementation shape (Option B — for requirements + approval before coding):**
  1. **DB:** add a new `profiles` column for proficiency (e.g. `proficiency_level` smallint, nullable; 0/1/2 from placement, extensible to a CEFR band later) — **separate migration, coordinate numbering with the DB/EN-8 agent** (EN-8 already claims `00012`). No change to `unlocked_level`.
  2. **Onboarding** (`useOnboarding.ts` `complete()`): write placement → `proficiency_level` (today it writes only the two consent flags + a local mirror).
  3. **Home** (`HomeView.tsx:151`/`useHome.ts`): the level label reads `proficiency_level`, NOT `unlocked_level`.
  4. **Settings:** a self-service "Your level" control to change `proficiency_level` (no paywall) — the "can't change it" half of the report.
  5. **`unlocked_level`** stays exactly as-is (access-key/paywall only).
- **Coordination flags:** DB migration (DB agent). Touches `SettingsView.tsx` (Lane A's goal chooser + Lane B's TB-11b already there) and possibly `App.tsx` (Lane B live for SEC-1) — reserve + sequence before editing. `useOnboarding.ts`/`HomeView.tsx`/`useHome.ts` are currently conflict-free.
- **Owed:** requirements doc → owner approval → build → **e2e test** (owner asked explicitly). Branch: `develop`.
- **Status:** DECIDED (Option B); **NEEDS REQUIREMENTS + owner approval before coding** (AGENTS §3) + DB coordination.

### TB-2 — First-words screen speaker/audio button does nothing (reporter: owner) — `DONE (fixed + verified; edge deployed)`
- **Report:** "In the first words screen the speaker button doesn't seem to work when I click it."
- **Root cause (empirical, via the new observability + probes 2026-07-14):** a self-inflicted `obs-trace` regression. The client now sends a W3C `traceparent` request header on every `functions.invoke` (OBSERVABILITY-CONTRACT §8), but `_shared/http.ts` CORS `Access-Control-Allow-Headers` did **not** list `traceparent`. A custom request header triggers a browser CORS **preflight**; the missing allow-entry made every preflight fail → the browser blocked **EVERY edge call** with `FunctionsFetchError` (tts/chat/translate/delete-account/log-sink), not just audio. node/curl were unaffected (no CORS enforcement), which masked it in tests.
- **Evidence:** `public.logs` showed repeated `speech_playback_failed` + `edge_fn_failed {action:tts, FunctionsFetchError}`; a fresh-token node probe returned HTTP 200 + audio (function healthy, ~6.2s); an OPTIONS preflight sending `traceparent` was rejected (allow-headers omitted it).
- **Fix (commit `4fdebc4`, `develop`):** added `traceparent` to `Access-Control-Allow-Headers`; redeployed gemini/delete-account/log-sink to prod. Preflight now echoes `traceparent` (verified live).
- **Follow-up (minor):** the first-words handler swallows a TTS failure as a toast-less WARN — consider a user-visible retry/hint when audio genuinely fails. Tracked as a small nice-to-have.
- **Owed to reach testers:** merge `develop`→`main` + web deploy (edge already live in prod).
- **Status:** DONE (root cause fixed + preflight verified; edge deployed).

### TB-3 — Onboarding resets to step 1 ("Bem-vindo") on browser tab switch (reporter: Nadia Laabs, Chrome) — `DONE (fixed + regression test, on develop)`
- **Report:** "Just by switching tabs on my browser. Then when I go back to the page, it's reset to the beginning." Browser: Chrome.
- **Root cause (confirmed, code-read 2026-07-14):** `useOnboarding`'s hydration effect depended on the **`user` object** (`useOnboarding.ts` `}, [user]`) and calls `setIsLoaded(false)` on every re-run. supabase-js/gotrue refreshes the session on tab-focus (`TOKEN_REFRESHED`) and hands back a **new `user` object** → the effect re-ran → the gate `isLoaded && !isComplete` toggled → the lazy `OnboardingFlow` **remounted to step 1**. Storage read/write (`coerceRecord`, IndexedDB) were correct — it was object-dependency churn (EF-33 family). Not Chrome-specific (standard tab-focus refresh).
- **Fix (commit `b5fd30d`, `develop`):** key the effect on the stable `user?.id` string, not the object — re-hydrate only on real sign-in / user switch.
- **Test (regression, so it can't recur):** `src/features/onboarding/__tests__/useOnboarding.test.ts` — a same-id new `user` object must NOT trigger a second storage read (would fail on the old `[user]` dep); a real id change still does. 2/2 green.
- **Owed to reach testers:** merge `develop`→`main` + web deploy.
- **Status:** DONE (fixed + regression-tested on develop; awaiting promotion).

### TB-4 — Mobile: stuck on first screen after password login, can't scroll (reporter: mobile tester) — `DONE (fixed on develop; regression test owed)`
- **Report:** "Normal process with PW works but then I am stuck on first screen and can't scroll further." **Env: Android 14 (Brave + Chrome, both Chromium) AND a laptop after installing (likely Windows/installed PWA). Fine in a tall macOS browser window.** The cross-platform + installed reproduction ruled out a pure mobile-URL-bar cause.
- **Root cause (confirmed, code-read 2026-07-14) — two compounding issues:**
  1. **PRIMARY (cross-platform flexbox trap):** the onboarding `StepShell` scroll region (`OnboardingFlow.tsx:137`) was `flex-1 overflow-y-auto` **without `min-h-0`**. A flex child defaults to `min-height:auto`, so it grew to its content height instead of scrolling — pushing the `shrink-0` footer (Continue / "Let's go") **below the fold** on ANY short/installed window, every OS. `overflow-y-auto` never engaged. This is why it hit Android AND the installed laptop app but not a tall macOS browser window.
  2. **SECONDARY (mobile only):** the app shell used `h-screen` = `100vh`, which on Chromium/Android includes the URL-bar area, making the shell taller than the visible viewport and worsening (1).
- **Fix (`develop`):** (1) `OnboardingFlow.tsx:137` `flex-1 overflow-y-auto` → `flex-1 min-h-0 overflow-y-auto` (commit `822feb6`) — the real cross-platform fix; (2) app shell `h-screen` → `h-dvh` (commit `9c73629`) — mobile viewport correctness. Both verified: `min-h-0` + `.h-dvh`/`100dvh` generated in the built CSS; build green.
- **Regression test (DELIVERED, commit `03b98d8`):** `tests/e2e/user/52-onboarding-footer-reachable-short-viewport.spec.ts` (`@mobile`, 390×560) asserts the onboarding footer CTA `toBeInViewport()` and advances the flow. `toBeVisible()` would not catch the bug (button stays in DOM, below the fold); `toBeInViewport()` fails pre-fix / passes post-fix. GREEN in e2e Run 22.
- **Owed:** promote (deploy) `develop`→`main` to reach the tester.
- **Status:** DONE (fixed + regression-tested on develop; awaiting promotion).

### TB-5 — Tutor practice session auto-reads every message aloud (reporter: owner) — `OPEN (auto-read default handled; NEW bilingual-read / bad-accent dimension NEEDS REQUIREMENTS 2026-07-16)`
- **Report:** "When I go to the tutor practice session it reads all out loud regardless if I want to or not."
- **Root cause (code-read 2026-07-14):** `isSoundEnabled` defaults to **true** (`useSettings.ts:64` — no saved pref → true). The practice session auto-plays the seeded first message (`useTutorSession.ts:362`) + every AI reply (`:232`, `:393`) via `playMessageInChunks` whenever sound is on. Opt-OUT: a fresh user is read to until they hit Mute (persists in `localStorage.is_sound_enabled`).
- **Fix options (owner decision):** (A) default `isSoundEnabled` false (opt-in); (B) keep mute but STOP auto-reading the transcript — rely on the existing per-message play buttons ("help on demand"); (C) first-run audio choice. Recommend A or B.
- **Owner:** Agent S (`fix/*`). **Status:** OPEN (needs owner default decision).
- **Status:** DONE (fixed on develop; @mobile regression test + deploy owed).
- **↑ ADDENDUM — bilingual read + bad accent (owner report 2026-07-16), NEEDS REQUIREMENTS:** separate from the *whether-to-auto-read* fix above — this is about *what* gets read. When auto-read is on, the tutor reads the ENTIRE reply — Portuguese AND the English translations / phonetic guides — through the **pt-PT** voice, so English is spoken with a Portuguese accent and "sounds terrible." Owner: "makes no sense to read both unless the user can control each part independently."
  - **Root cause (code-read 2026-07-16):** the tutor system instruction (`supabase/functions/_shared/gemini.ts` L128–142) makes the model emit bilingual markdown (PT + phonetics + a `**English:**` blockquote). `useTutorSession.playMessageInChunks` (`src/features/tutor/useTutorSession.ts`) splits the reply by sentence and speaks EVERY chunk via pt-PT `geminiService.playSpeech` — no PT/EN filtering, no per-part control. (The simulator's `speak()` voices only the NPC's PT `npc_text`, not the English `translation`, so it's closer to correct — but should follow the same rule for consistency.)
  - **Options (owner to decide — NO coding yet):** **A (recommended)** stop auto-reading; render per-part 🔊 on the **Portuguese only**, driven by a MACHINE-READABLE tutor output (structured `pt`/`en`/`phonetic` fields) instead of parsing markdown — best UX, no English-in-a-PT-voice, and fewer/curated PT clips (lowest TTS cost → EN-8 synergy). **A-lite** same UX but heuristically parse the existing markdown (less reliable split). **B** keep auto-read but strip English/phonetics before speaking (fragile if the model's formatting drifts). **C** per-language voice (an English robot voice in a PT app is odd + more cost) — not recommended.
  - **Relationship:** refines TB-5 (content of what's read vs the original whether-to-read); relates to **EN-13** (select-to-translate) and **EN-8** (curated PT-only clips are cheaper + hostable). A structured tutor output would also help **EN-17a/EN-18**.
  - **Owner:** Agent E (`feat/*`). **Status:** NEEDS REQUIREMENTS — owner chose "investigate/spec, decide later" (2026-07-16); no coding until requirements written + owner-approved.

### TB-6 — Onboarding "Say it back" (first-words screen) doesn't listen to what I say (reporter: owner) — `DONE (fixed + regression test on develop, commit a5c9d00; owner picked "make it work")`
- **FIX (2026-07-14, commit `a5c9d00`):** `handleSayItBack` now uses the recognized transcript instead of discarding it. Success echoes what it heard ("Nice — I heard you!" + the transcript) so the learner sees it genuinely listened; `no-speech`/`timeout` offers a **Try again** (not a fake success); `unavailable`/`permission-denied`/`not-implemented` (e.g. Brave/permission) offers an honest **"I said it"** self-confirm. Regression: `src/features/onboarding/__tests__/FirstWinStep.test.tsx` mocks `platform.speech.recognize` and asserts all three outcomes (transcript echoed / retry / honest self-confirm; and NOT the old unconditional "Nice!"). vitest GREEN.
- **Owed:** promote (deploy) to reach testers. (A live-mic manual check on Brave/iOS still worthwhile to confirm the unavailable-path copy.)
- **Report (owner 2026-07-14):** "the 'say it back' button on the your first words screen doesn't really listen to what I say."
- **Root cause (confirmed, code-read — `src/features/onboarding/OnboardingFlow.tsx` `FirstWinStep`/`handleSayItBack` L514):** the say-it-back is **by design a no-op on content** — it calls `platform.speech.recognize({ language:'pt-PT', timeoutMs:6000 })` and **discards the transcript**. On success it does `setSayState('done')`; on **any** error (`no-speech`, `timeout`, `unavailable`, mic-permission) the catch **also** does `setSayState('done')` (comment: "the point is the try, not the score"). Either way it shows *"Nice! You just said your first Madeiran words."* It never compares your speech to "Bom dia!", never echoes what it heard, and never signals failure → so whatever you say (or if nothing is captured) you get the same success message. That is exactly why it "doesn't really listen."
- **Likely compounding (needs live-mic confirm):** on **Brave** (an earlier tester's browser) the Web Speech API is **disabled by default** (Brave blocks Google's speech endpoint), and iOS Safari/WebView `webkitSpeechRecognition` is intermittent (noted in `speech.web.ts` header). In those cases recognition never starts/returns — but the unconditional "Nice!" masks it. `pt-PT` also depends on the browser's server-side model + network + mic permission.
- **Decision needed (product):**
  - (A) **Make it actually listen:** compare the final transcript to the phrase (fuzzy/loose match), echo *"I heard: …"* + a match indicator. Real feedback; heavier (STT reliability varies by browser).
  - (B) **Keep it low-stakes but honest:** if a transcript comes back, show *"I heard: …"*; if recognition is unavailable/empty, say so gently (e.g. *"Couldn't hear that — tap I said it"*) instead of a blanket "Nice!". Copy stops over-promising.
  - (C) Leave behavior, but only show "Say it back" when `platform.speech.isAvailable()` AND a transcript is likely (hide on Brave/unsupported) so it never appears to listen when it can't.
  - Recommend **B** (honest, low effort) as a first step; **A** if we want genuine pronunciation feedback (ties the broader speaking-practice STT).
- **Owed:** decision → fix → regression test (onboarding say-it-back: transcript echoed on success; graceful "couldn't hear" on `no-speech`/`unavailable`, mocking `platform.speech`). Owner: Agent S (`fix/*`) + product decision.
- **Status:** OPEN (root cause confirmed by code-read; a live-mic repro would confirm the Brave/STT-availability angle).

### TB-7 — App restarts onboarding (and re-asks Terms) on every login instead of continuing (reporter: owner, staging 2026-07-14) — `FIXED on develop (092605b); staged in 2026.07.14.3 — awaiting owner staging-verify + prod approve`
- **Report (owner, verifying staging `2026.07.14.2`):** "why does it start all over every time you log in even if you have already been signed up… where you were last time should persist… it asks again for terms of service which you already answered." Broadened by owner to a general app requirement: returning users must continue, not restart; config/choices/progress/results must persist and be recalled; expect interruptions mid-flow.
- **Root cause (confirmed by code-read):** the onboarding gate in `src/App.tsx:244` renders `OnboardingFlow` whenever `onboarding.isLoaded && !onboarding.isComplete`. `isComplete` comes from `useOnboarding` (`src/features/onboarding/useOnboarding.ts`), which reads the completion flag + placement level **only** from client `platform.storage` (`storageKeyFor(userId)` → `onboarding:record:<userId>`, `useOnboarding.ts:57,113`). **This flag is never written to the DB** (the hook's own header, L4-9, documents that `profiles` has no onboarding-complete/placement column). So on any context where that client record is absent — **new device/browser, cleared site data, private/incognito, or IndexedDB+localStorage unavailable → in-memory fallback lost on reload** — `isComplete=false` and the FULL first-run flow re-runs. Consent is re-asked because the flow is gated by this client-only flag and never consults `profiles.has_accepted_terms` (already `true` in the DB) to skip the consent step. `signOut`/`onLogoutCleanup` (`src/App.tsx:207`) does NOT wipe the record, so same-device logout→login *should* persist — **needs empirical confirmation** whether staging hits the IndexedDB→memory fallback (which would explain "every login").
- **Broader interruption gap (same requirement, from the flow map):** Daily Session (`useDailySession`), every Practice engine, Quiz (`PracticeQuiz`), Tutor free-chat + AI-Practice (`useTutorSession`), and Learning modal drafts hold progress in **component memory only** → lost on tab-switch/reload. Last-active tab is not persisted (always boots to Home, `App.tsx:79`).
- **Fix direction (see DF11 + docs/USER-WORKFLOWS-AND-STORIES.md):** persist onboarding-complete + placement to a `profiles` column and gate on the DB signal (client mirror as fallback); short-circuit already-answered steps from existing DB state (consent, active track); restore last route on login; persist resumable state for interruptible flows.
- **Owner:** Agent S / E (`fix/*` for the gate; broader persistence is an E feature). **Severity:** HIGH — re-consent-every-login is also a consent-integrity smell.
- **FIX (2026-07-14, commit `092605b` on `develop` via merge `b6fcde3`; staged in release `2026.07.14.3`, commit `6d1ece9`):** `useOnboarding.isComplete` now = `record.complete || (profile.has_accepted_terms && profile.has_accepted_ai_usage)`. Consent is the terminal onboarding step, so the DB consent flags prove prior completion — a returning user skips the ENTIRE first-run flow on any device and is never re-asked Terms. A heal effect writes the local mirror once so it never flashes again on that device. Regression tests added in `useOnboarding.test.ts` (skip-with-consent + heal; still-gate-genuine-new-user); 194 unit tests green; lint + build clean.
- **Scope of this fix vs DF11:** this closes the "restart + re-consent every login" symptom (the reported bug). The BROADER continuity work stays open under **DF11**: persist onboarding-complete/placement to a `profiles` column (so placement survives too — ties TB-1), restore last-active tab/route, and make interruptible flows (Daily Session, Practice engines, Quiz, Tutor) resumable. Also still worth an empirical check whether same-device re-login was hitting the IndexedDB→memory fallback.
- **Status:** FIXED on develop, staged in 2026.07.14.3. **OWNER-CONFIRMED on staging 2026-07-14** ("TB-7 seems to be fixed"). Awaiting prod promotion (bundled with the rest of `2026.07.14.3`).

### TB-8 — Daily voice/audio limit enforced at 5, not the configured global 20 (reporter: owner, staging 2026-07-14) — `SERVER FIXED (2026-07-15); CLIENT CLOBBER FIXED + limit=50 live (2026-07-16, staged 2026.07.16.3/.4); EN-11 tiers open`
- **Report (owner):** "can't find where to configure the max per-user audio limit — it seems to be set to 5 but I recall setting it to 20 — this must also persist."
- **Verified in the live DB (2026-07-14, pg direct):** `global_settings.voice_limit = "20"` — **the setting DID persist server-side.** `profiles.voice_limit` is NULL for all users (no per-user override).
- **Enforcement path (code):** `useTutorSession.ts:247` `const limit = profile?.voice_limit ?? globalVoiceLimit` — with per-user null, the effective limit is `globalVoiceLimit`, which `useSettings.ts:304-321` loads from `global_settings.voice_limit` on mount. RLS is not the blocker (`global_settings` SELECT policy = "readable by all", `qual: true`, verified). So a correctly-loaded client should show/enforce **20**.
- **The "5" is the client-side default** (`config.voice.defaultDailyVoiceLimit: 5`), used as the initial state before the async fetch resolves (`useSettings.ts:70-73`, seeded from `localStorage` or 5). Suspected causes (need a repro on the owner's session): (a) the mount fetch not populating/overwriting before the value is read/displayed (timing), or (b) a stale `localStorage 'global_voice_limit'` = 5 shown first. Persistence server-side is fine; the **client load/reflection** is the bug.
- **Discoverability:** the only UI to change it is the "Global Voice Limit" +/- control in Settings, gated behind **admin mode** (`SettingsView` `isAdminMode && profile?.role === 'admin'`). That is why it's "hard to find." Also note it's a **global** (all-users) limit despite reading like a per-user one — the label/UX should clarify.
- **UPDATE (2026-07-14): client data path VERIFIED WORKING.** Querying `global_settings?key=eq.voice_limit` with the **anon key exactly as the app does** returns `HTTP 200` + `value:"20"`. So the fetch/RLS/data are fine — the "shows 5" the owner saw was most likely a **pre-fetch flash** (initial state seeds from localStorage-or-default 5 before the async fetch applies 20) or a **stale/older build** (owner first hit this on `2026.07.14.2`). **Action:** owner to re-check the limit on the current staged `2026.07.14.3` — it should read 20.
- **Fix direction (hardening, small):** (1) make the fetch observability-compliant — it currently swallows errors (`const { data } = …`, no logging, doctrine violation); switch to `.maybeSingle()` + log failures with correlation, and seed initial state from the fetched value rather than a localStorage-first default so a stale local 5 can't mask the server value; (2) surface the effective limit read-only to ALL users (discoverability — see EN-11). Ties DF11.
- **★ ROOT CAUSE CONFIRMED (2026-07-14, via public.logs + code) — SERVER-SIDE:** `public.logs` shows repeated `speech_playback_failed` → `EdgeFunctionError: "Daily voice limit of 5 reached"`. The **edge function** `supabase/functions/gemini/index.ts:164` does `const limit = profile?.voice_limit ?? 5;` — a **hardcoded 5** that NEVER consults `global_settings.voice_limit` (=20). Since `profiles.voice_limit` is NULL for everyone, the **server caps all users at 5**, and the server is authoritative for TTS. So the client showing 20 is cosmetic — the real enforcement is the server's 5. This is also a **forbidden hardcoded-fallback** (masks the source-of-truth) per the observability/standards doctrine.
- **FIX (server; belongs with the EN-11 audio-limits batch to stay coherent):** in `gemini/index.ts` compute `limit = profile.voice_limit (per-user) ?? global_settings.voice_limit (global) ?? 5 (hard floor)` — i.e., read the global default from `global_settings` instead of hardcoding 5. Deploy the edge fn. Keep the client precedence (`useTutorSession.ts:247`) aligned. **Coordination:** the voice-limit domain is being edited by `feat/nav-audio-limits-batch`; this server fix is routed there (or to whoever owns EN-11) so client+server change as ONE coherent design — Lane A did NOT deploy a competing server patch (per "don't step on each other").
- **Remaining client hardening (small, separate):** the client fetch swallows errors (`const { data } = …`) — switch to `.maybeSingle()` + log; surface the effective limit read-only to all users (EN-11).
- **★ SERVER FIX DEPLOYED (2026-07-15, commit `4d62982`, on develop; `gemini` edge fn deployed live to project `gxlrmdfqcqimwwplrdgd`):** `gemini/index.ts` now computes `limit = profile.voice_limit (per-user) ?? global_settings.voice_limit (global, =20) ?? 5 (floor)` via a `global_settings` lookup — no more hardcoded 5. Backward-compatible (only raises the cap); shared Supabase backend so it's live for staging AND prod edge immediately (prod frontend unchanged). Owner has headroom to re-test voice now (usage 5 < 20; no reset needed).
- **Still open (smaller):** (1) client hardening — the `useSettings` global-limit fetch swallows errors (`const { data } = …`); switch to `.maybeSingle()` + log; (2) surface effective limit read-only to all users; (3) EN-11 per-user override UI. These live in `feat/nav-audio-limits-batch`'s domain.
- **Owner:** server DONE (Lane A); client bits → the EN-11 audio-limits owner. **Status:** SERVER FIXED + DEPLOYED; client hardening + EN-11 tiers remain.
- **★ CLIENT CLOBBER FIXED + LIMIT RAISED TO 50 (2026-07-16, commit `6f4a25e`, staged in 2026.07.16.3/.4):** the remaining "client hardening" was actually a live **clobber bug**. `useSettings` marked the global-limit "loaded" even on a failed/empty fetch (swallowed `.single()` error) AND re-wrote `global_settings.voice_limit` on every effect run (any dep change, incl. profile-identity churn) — so when an **admin** loaded the app with a stale device value, it overwrote the server setting. Observed live: a value I set to **50** was reset to **20** by an admin app-load during e2e. Fix: fetch uses `.maybeSingle()` + logs errors + records the authoritative value only on a real read; the admin write-back now fires only via the pure `shouldPersistGlobalVoiceLimit` (admin AND value ≠ server value) — never on load/profile-churn. Unit-tested (`src/features/settings/__tests__/voiceLimitPersist.test.ts`). **`global_settings.voice_limit` set to `50` live + verified.** A diagnostic proved a fresh user is `profile.voice_limit=NULL` (edge uses the global), and the **`gemini`/`ai-gateway` edge redeploy was a NO-OP** — the edge always read `global_settings` correctly; the clobber was 100% client-side. **Durability caveat:** 50 only stays put once a build containing `6f4a25e` reaches PROD (currently deployed prod frontend still has the old clobbering code) — staged in 2026.07.16.3/.4; prod pending owner approve. Owner: Lane B (client). Ties EN-11.

### TB-9 — Offline audio doesn't appear to be saved (reporter: owner, staging 2026-07-14) — `OPEN (needs repro)`
- **Report (owner):** "it doesn't appear that audio is saved."
- **Code facts (verified):** playback goes through `synthesizeCached` (`geminiService.ts:309-335`), which on a cache miss **always** writes the clip to the IndexedDB bounded-LRU (`audioCache.set`) — there is **no** gate on the "Save audio on device" toggle (matches the owner "always cache" decision). So under normal conditions audio *is* persisted on play. `saveAudioOnDevice` defaults ON (`useSettings.ts:96-99`); turning it OFF clears the cache (`useSettings.ts:138`).
- **Suspected causes (need a repro on the owner's browser):** (a) the Settings "Used: X MB" display refreshes only after cache-mutating *actions*, not after ordinary plays (`useSettings.ts` `refreshCacheUsage`), so it can read 0/stale → *looks* unsaved though it isn't; (b) IndexedDB unavailable on the test browser (private mode / storage blocked) → `platform.storage` falls back to **in-memory** (lost on reload) — the same fallback flagged for TB-7; (c) the "Download all for offline" action failing. 
- **Diagnostic plan:** repro on the owner's browser; check `public.logs` for `tts_cache_*` / storage errors; confirm whether the browser has IndexedDB; verify the usage display updates after a play and after a download. Ties **QA-1/QA-1b** (download + survive-SW-upgrade tests) and the caching disclosure (COMP-1). Owner: Agent E/T. **Status:** OPEN (needs repro to localize display-vs-persistence).

### TB-10 — Tutor microphone throws an error after granting mic permission (macOS) (reporter: owner, staging 2026-07-14) — `OPEN (one cause confirmed = TB-8 limit; mic-input error needs client repro)`
- **Report (owner):** in the tutor, the microphone doesn't work — it throws an error when I grant it permission on macOS.
- **Confirmed contributing cause (= TB-8):** `public.logs` shows the tutor hitting `EdgeFunctionError: "Daily voice limit of 5 reached"` repeatedly. `toggleRecording` (`useTutorSession.ts:248`) gates the mic on that limit and shows a "Daily voice limit reached" error; TTS playback also fails at the 5-cap. So the low limit (TB-8 server `?? 5`) alone produces a "voice throws an error" experience. **Fixing TB-8 (server limit → global 20) likely resolves most of this.**
- **Possible separate mic-INPUT bug (needs client repro):** if the failure is a genuine Web Speech recognition error AFTER permission is granted (not the limit), the tutor surfaces it via `onError` as `"Microphone error: <code>"` (`useTutorSession.ts:197-208`). macOS Chrome/Safari Web Speech is server-backed and commonly errors with `network` / `audio-capture` / `language-not-supported` (pt-PT) / `aborted`. **Observability gap:** `speech_recognition_error` is logged at **WARN**, which is NOT persisted to `public.logs` (only the playback ERRORs show) — so we're blind to the actual mic-input code. 
- **Diagnostic plan:** (1) fix TB-8 first, re-test the tutor mic; (2) if it still errors, repro on the owner's macOS browser with devtools to capture the `onError` code/detail; (3) consider persisting `speech_recognition_error` (bump to ERROR or add to the persisted set) so mic failures are visible in `public.logs` going forward. 
- **UPDATE (2026-07-15):** the confirmed contributing cause (TB-8 server limit=5) is now **FIXED + DEPLOYED** (`gemini` edge reads global_settings=20). **Owner: re-test the tutor mic on staging** — if voice now works, TB-10 was the limit and can close; if it still errors after granting permission, it's the separate Web Speech mic-input bug → repro with devtools for the `onError` code + (fix) persist `speech_recognition_error` so it's visible in `public.logs`.
- **Owner:** Agent S (mic-input, if it persists). **Status:** OPEN pending owner re-test — likely resolved by the TB-8 fix; genuine mic-input error still possible (repro needed).

### TB-11 — Learning-path picker (Profile) appears to select "Structured" when "Goal Track" is chosen (reporter: owner, staging 2026-07-15) — `FIXED (on develop, commit fb1b723, e2e-verified)`
- **Report (owner):** the goal-track picker in the Profile screen seems to pick the structured course instead.
- **Root cause (CONFIRMED, 2026-07-15):** NOT a wrong write — the picker sets `type='goal-track'` correctly and the "active" pill moves correctly. The defect is that **Settings offered no way to choose WHICH goal track** (unlike onboarding, which routes to a track-picker step via `needsTrack`). With `activeTrackId` left `null`, `goal-track.ts:24-30 resolveActiveTrack()` **silently falls back to `context.tracks[0]`**, so `order()`/`next()` present the first track's situations as an app-led ordered sequence — visually indistinguishable from the Structured Course. Confirmed the consumer side too: a `next()` action of `kind:'free'` only routes to the Practice hub (`App.tsx:192-193`), so there is no Home path to a track picker — the missing surface had to be Settings.
- **Fix (commit `fb1b723`):** SettingsView renders a goal (track) chooser under Learning Path when goal-track is active; picking one calls `setActiveTrack` (persists locally + `user_track_selection`). Added `data-testid` `path-switcher` / `goal-track-chooser` to disambiguate the two button lists (a **seed track is literally named "Structured Course"**, which collided with e2e `user/36`'s substring locator — that spec was scoped to `path-switcher`).
- **Coverage:** new `tests/e2e/user/37-goal-track-picker.spec.ts` (chooser appears only for goal-track; picking persists a concrete `activeTrackId`, ending the silent tracks[0] fallback) — passes with `user/36` green.
- **Owner:** Agent E (`src/paths` + Settings). **Status:** FIXED on develop; owner to verify on the next staging cut.

### TB-11b — goal-track masquerades as Structured on Home before a goal is picked (spun off from TB-11, 2026-07-15) — `FIXED (on develop, commit de4215f) — reported again by owner on staging 2026.07.15.3`
- **What:** even with the Settings chooser (TB-11), a user who switches to goal-track but never opens the chooser still hit `resolveActiveTrack()`'s silent `tracks[0]` fallback on Home — `next()` showed "Continue your track" against an arbitrary first track (seed track literally named "Structured Course"). The owner re-hit this on the 2026.07.15.3 staging cut ("I pick goal track but Home still shows structured course… I didn't notice the goal chooser").
- **Decision (owner-approved, 2026-07-15):** (a) **deep-link to the Settings Learning Path chooser.** Home shows an honest "no goal chosen" state that routes the learner to the picker.
- **Fix (commit `de4215f`):** `resolveActiveTrack()` no longer falls back to `tracks[0]` (returns null when no goal chosen). New `NextAction` kind `'choose-goal'`: `next()` returns "Choose your goal" / "Goal track · no goal chosen yet — pick a goal in Profile" when `activeTrackId` is null; `order()` passes through all situations. `App.handleStartPathNext` routes `'choose-goal'` → Settings tab + a `focusGoalChooser` deep-link signal; `SettingsView` scrolls the chooser into view, highlights it (ring), and shows a "pick a goal to start" hint. When a goal IS chosen, Home's CTA carries the track name (reflects the pick).
- **Coverage:** unit `paths.test.ts` — `choose-goal` with/without tracks loaded, `order()` pass-through, and `next().detail` reflects the picked track name. e2e `user/37` — after picking a goal, Home "Today's Focus" reads "Continue your track" and shows the chosen track's name (Home reflects the pick). e2e `user/36` — picks a goal first so the CTA assertion is deterministic. Full regression green (eslint/tsc/vitest 206 + e2e 117). *No-goal Home state is unit-covered, not e2e: `user_track_selection` persists per-user in the DB, so the null state is not reliably reproducible for the shared/throwaway e2e user.*
- **Owner:** Agent E (code by Lane B, coordinated — Agent E was on EN-7/EN-8). **Status:** FIXED on develop; rides release 2026.07.15.4.

### TB-12 — Situation Simulator repeats one situation and feels shallow; should span the same thematic content by level (reporter: owner, staging 2026-07-15) — `OPEN (bug + content-depth)`
- **Report (owner):** the simulator "only seems to do one situation over and over"; "it should be building up according to the levels and vocabulary used"; "only 5 levels and a short conversation in each — super inadequate." Design question: shouldn't the simulator cover the **same thematic areas as the other parts/paths, just in conversation-simulation style?**
- **Answer (architecture — YES):** the app is "one content model, many engines over the SAME shared situations" (the Profile path note even says *"All paths use the same situations and share your progress"*). The simulator (`src/features/practice/simulator/`) is designed as the conversation-style engine over those situations, with difficulty L1→L5 (`scenario.ts`: weaves the situation's `phrase_patterns` + `vocabulary` + `roleplay`, guided at L1 → messy real-life at L5). So conceptually it SHOULD traverse the same thematic situation set by level — the owner's instinct is correct.
- **Two distinct problems:**
  1. **BUG — not traversing:** it stays on one situation instead of advancing across the situation set/themes. Likely the entry (`openMode('simulator', situationId)`) pins one situation and there's no "next situation" progression within the simulator. Fix: after completing a situation, advance to the next by level/track/theme (drive from the path's `recommendNext`/situation ordering, same as Home's coach focus), or expose a situation list.
  2. **CONTENT DEPTH — thin:** authored `roleplay`/`dialogues` per situation are short and levels feel limited. This is a CONTENT task (Agent C) — richer, longer roleplay graphs + more situations per theme/level; ties EN (content generation) and the L1–L5 difficulty ladder.
- **Owner:** Agent E (traversal/progression wiring in `src/paths`+simulator) + Agent C (content depth). **Status:** OPEN — traversal is a code bug; depth is a content build-out.
- **AGREED DESIGN (owner, 2026-07-15) — all reuse, no duplicate infrastructure:**
  - **Reuse the tutor stack, don't fork it.** The simulator's `free` (AI) mode ALREADY runs over `geminiService.startChat` (the tutor edge fn) with a scenario prompt (`scenario.ts`), and all speech goes through the shared `playSpeech` → `synthesizeCached` → audio-cache pipeline. The only simulator-specific code is the `scripted` roleplay-graph walker. Confirmed the AI half is not new code (owner: "the fundamentals already exist in the tutor … reuse the same infrastructure rather than building duplicate paths").
  - **Audio mode = hybrid by learner CHOICE, not level-gated.** Scripted (authored, cacheable, offline/503-safe) available at ALL levels; a session toggle lets the learner switch to AI-driven roleplay for variety (online-only, device-speech fallback). Scripted is the pre-hostable/offline path; free AI is the enhancement.
  - **Traversal = user-driven continuation.** Finish a situation → offer "Continue" to the NEXT situation in the existing path `order()` (goal-track/adaptive), and keep going until the learner leaves the screen. No fixed batch / no new sequencer — reuse path ordering; completions feed the same shared mastery. Per-situation unit == EN-7's `situationId` download unit.
  - **Audio coverage depends on the shared enumerator being extended** — see EN-8 coordination flag below (owner: "Yes, coordinate now").
- **Status refinement:** design agreed; **NEEDS REQUIREMENTS + owner approval before build** (per the requirements/approval gate). Two build tracks once approved: (1) Agent E — traversal wiring + scripted/AI session toggle (reuse only); (2) Agent C — authored roleplay graphs for the first track's situations, sequenced WITH the EN-8 first-lessons pre-gen scope.

### TB-13 — Pronunciation/accent is wrong — audio doesn't sound like mainland (European) or Madeiran Portuguese (reporter: owner, 2026-07-15) — `OPEN (investigated — root cause found; fix NEEDS DECISION)`
- **Report (owner):** some pronunciation is not correct; it doesn't sound like mainland Portuguese or Madeiran Portuguese (sounds off/Brazilian).
- **Root cause (code-read 2026-07-15):** TTS runs through a provider chain (`supabase/functions/_shared/tts/router.ts`). Providers split:
  - **Locale-pinned → correct European Portuguese:** Azure (`pt-PT-RaquelNeural`/`DuarteNeural`/`FernandaNeural`), Google (`voice.languageCode: "pt-PT"` + `pt-PT-Wavenet-*`), Polly (`LanguageCode: "pt-PT"`, Inês/Cristiano).
  - **Accent-INFERRED → drifts Brazilian:** **Gemini** (`gemini.ts:58` sends only `prebuiltVoiceConfig: { voiceName }` — Kore/Zephyr/Charon/Puck — with **NO language param and NO European-PT instruction**; Gemini 2.5 multilingual voices infer accent from text and default Portuguese to **Brazilian**), OpenAI (`gpt-4o-mini-tts` — "voices are language-agnostic; accent comes from the text"), ElevenLabs (`language_code: "pt"`, not `pt-PT`).
  - **Default chain = `["azure","gemini"]`.** So when **Azure is not keyed** (or a user's BYO provider is Gemini/OpenAI), audio serves from **Gemini → Brazilian accent**. The client device-speech fallback IS correct (`geminiService.ts:274` `lang: 'pt-PT'`), but that only triggers when server TTS is fully down.
- **Important caveat (set expectations):** **no TTS provider offers a *Madeiran* dialect voice.** The realistic target is **mainland European Portuguese (pt-PT / Lisbon)**; true Madeiran accent is not achievable from any current TTS. "Correct" here = pt-PT, not Brazilian.
- **Approved fix (owner 2026-07-15): "steer all + prefer locale-pinned."** Applied in code on develop:
  - **Chain reordered** (`router.ts` `DEFAULT_CHAIN` → `["azure","google","polly","gemini"]`): locale-pinned pt-PT providers (Azure/Google/Polly — hard guarantee, engine can't override) run FIRST; Gemini is the always-available fallback; OpenAI/ElevenLabs only via explicit `provider` request (accent inferred).
  - **Gemini steered** (`gemini.ts`): prepend `"Say the following in European Portuguese as spoken in Portugal (Lisbon accent), not Brazilian: …"` — best-effort (Gemini TTS style is prompt-based; **needs a listen-test** to confirm it shifts accent).
  - **OpenAI steered** (`openai.ts`): added `instructions: "Speak in European Portuguese … not Brazilian."`.
  - **Residual — ElevenLabs** (only via explicit BYO): `language_code: "pt"` is the only API lever; the mapped voices (Rachel/Adam/…) are English natives → accent is voice-driven and NOT guaranteed without European-PT voice IDs. Left as-is + flagged; needs real EU-PT voice IDs (config) to guarantee.
- **The guarantee, honestly:** HARD when a locale-pinned provider serves (Azure/Google/Polly); BEST-EFFORT (steered) for the Gemini fallback + OpenAI. **A hard guarantee therefore requires ≥1 locale-pinned provider keyed in prod** — ops must confirm an Azure or Google TTS key is set (else audio serves from steered-Gemini).
- **Ships separately:** edge-function change → **manual `supabase functions deploy`** (NOT the Verpex web `ship.sh`); no edge-deploy script in-repo.
- **DEPLOYED (2026-07-15):** `gemini` edge function redeployed to prod (project `gxlrmdfqcqimwwplrdgd`, **version 9**, `verify_jwt` unchanged=true) — TB-13 steering + chain reorder now live.
- **⚠️ PROD-KEY FINDING (2026-07-15):** prod edge secrets contain **only `GEMINI_API_KEY`** — NO Azure/Google/Polly/OpenAI/ElevenLabs key. So in prod all TTS serves from **Gemini** and the locale-pinned chain never engages → **the hard guarantee is NOT active; prod is best-effort Gemini steering only.** **To make the guarantee real, ops must add an `AZURE_SPEECH_KEY` (+region) or Google TTS key to the prod edge secrets** (`supabase secrets set …`); then Azure/Google (hard `pt-PT`) serves first and Gemini is the steered fallback.
- **Owner:** Lane B (edge code — DONE + deployed) + **ops (ACTION: add an Azure/Google Speech key to prod edge secrets for the hard guarantee)**. **Status:** DEPLOYED (best-effort steering live); HARD GUARANTEE pending a locale-pinned key + a **manual accent listen-test** on staging/prod. **Coverage owed:** no Deno test harness in-repo (same gap as QA-2) → verify by ear + add a deno test when a harness exists.

### TB-14 — Chat: per-PHRASE click-to-play (not whole-message read-aloud); today it reads the English too (reporter: owner, 2026-07-16) — `OPEN (requirement clarified — root cause found)`
- **★ REQUIREMENT (owner, 2026-07-16, CLARIFIED):** do NOT read the whole message at once. The user must be able to **click next to an individual Portuguese phrase to hear that phrase** — per-phrase play controls, so the learner chooses which phrase to hear. (This supersedes the earlier "PT-only whole-message read" framing.)
- **Current behavior / why it's wrong:** playback is per-MESSAGE and speaks EVERYTHING. `playMessageInChunks` (`useTutorSession.ts:282`) splits by sentence for pacing then speaks every chunk — so the `**English:**` translation, the phonetic `**Pronunciation:**` guide, and the labels are all read aloud; `TutorChatView`'s per-message 🔊 (`TutorChatView.tsx:166`) plays `playSpeech(msg.text)` on the entire raw message. There is no phrase-level control and no PT isolation.
- **Fix (design):** render each tutor message as its **constituent Portuguese phrases**, each with an **inline play control beside it** (tap → speak that one phrase only). **Reuse, don't invent:** `src/components/AudioButton.tsx` (EN-1 — loading/playing state) for the per-phrase control and `src/components/TranslatableText.tsx` (tap-to-reveal EN) for the phrase — both already used together in `LessonDetailModal`. Parse the tutor message's `**Português:**` sections into phrases; EN/pronunciation are shown as reveal/subtext, never auto-spoken. Client-only (edge-independent).
- **Ties EN-21 (mode-aware):** in `conversation` mode the phrases are the PT turns; in `translate`/`lesson` the `Português` section. Fold the per-phrase control into the EN-21 chat rework so the message renderer is built once.
- **★ REAL OUTPUT (owner-captured 2026-07-16, lesson AI-Practice / entry #2):** a real tutor turn is a LONG, INTERLEAVED wall — narrative Portuguese → `English:` translation → more PT → `English:` → an English objectives list → a `Português:/Pronunciation:/English:` pattern block → a "Nota do João" note. Implications for the renderer: (1) the Portuguese to make playable is **scattered throughout**, NOT a single block — per-phrase parsing must find each PT line/sentence + the `Português` block, and treat `English:`/`Pronunciation:`/notes/objectives as non-spoken reveal/subtext; (2) whole-message read-aloud is untenable (confirms per-phrase); (3) the tutor is **very verbose** — a prompt-brevity polish (shorter turns / progressive reveal) belongs with EN-21. Build the per-phrase message renderer to handle THIS structure.
- **Owner:** Agent E (`feat/*`). **Status:** OPEN — requirement clarified: **per-phrase click-to-play** over the real interleaved structure above, reusing AudioButton + TranslatableText. Build with EN-21 (shared message renderer). Ties EN-1, EN-21, TB-5.

### COORD-1 — Extend `linesForSituation()` to enumerate authored roleplay-node text (TB-12 ⨯ EN-7 ⨯ EN-8) — `RECONCILED (already covered; content-authoring constraints below — EN-8 owner, 2026-07-15)`
- **What / why:** `linesForSituation()` (`src/lib/audio-download.ts`) — the shared enumerator that feeds BOTH EN-7 offline downloads and the EN-8 pre-gen script — currently reads only `phrase_patterns` + `vocabulary` (verified: `audio-download.test.ts` asserts "3 patterns + 1 vocab"). It does **NOT** enumerate the simulator's authored roleplay-graph node text. Consequence: even `scripted`-mode simulator audio is outside the download/pre-host machinery, so the simulator (the most 503-exposed surface — see EN-8 ★ motivation) gets none of EN-8's benefit.
- **Ask (owner said "coordinate now"):** the EN-8 owner (other agent, owns `audio-download.ts` + `synthesizeCached` + the pre-gen script) should extend `linesForSituation()` to also collect roleplay-graph node text, and include the first track's simulator situations in the EN-8 first-lessons pre-gen scope — so scripted simulator audio is pre-hosted + downloadable via the SAME keys. Only meaningful once Agent C authors those graphs (TB-12), so sequence together.
- **★ EN-8 OWNER REPLY (2026-07-15) — RECONCILED, no enumerator change needed:** verified against `develop` `9185f50` (and confirmed identical in the EN-8 worktree): `linesForSituation()` **already enumerates `situation.roleplay.nodes[].npc_text` with `npc_voice_type`** (plus `dialogues[].lines[]`, `phrase_patterns` base+variants, and `vocabulary`). The "3 patterns + 1 vocab" figure is just a **minimal test fixture**, not the enumerator's real coverage. So scripted-simulator audio authored in the standard schema is **already** in the download (EN-7) + pre-host (EN-8) machinery via the same `buildKey`. No code extension is being made.
- **✅ ACTIONABLE FOR TB-12 / Agent C (author content to THIS contract so it's auto-covered):**
  1. Author roleplay under the **standard `situation.roleplay.nodes[]` schema** — each node with `npc_text` + `npc_voice_type`. Do **NOT** introduce a new/parallel roleplay-graph structure for "richer" content: anything outside `situation.roleplay.nodes[]` is **not** enumerated and would silently escape the audio pipeline (the exact 503 gap COORD-1 flagged).
  2. `npc_voice_type` **must** be a `VOICE_TYPES` enum value (`teacher/local/older/younger/service_worker/phone/noisy`). EN-8 normalizes cache keys to the **resolved voiceType**, so a valid archetype is what keys + pre-gens the clip correctly; undefined → default voice (works, but no per-speaker distinction).
  3. Only NPC-spoken `npc_text` is pre-hostable. Learner `RoleplayOption` text is **not** spoken/enumerated — confirm that's intended; if any option prompt should be spoken, that IS an enumerator change → tell the EN-8 owner.
  4. **AI/free-mode** NPC text is generated per session → **not** pre-hostable by design (consistent with the scripted=cacheable / free=enhancement framing in TB-12).
- **Sequencing handshake (NOT frozen):** the EN-8 pre-gen set auto-expands — when first-track scripted roleplay graphs are authored, ping the EN-8 owner and pre-gen is **re-run** to include those situations. No code change, no freeze. The L0 trial already pre-gens whatever roleplay exists at level 0 today.
- **Guard added (EN-8 plan Phase 1):** the move-only test for the extracted `linesForSituation` (→ `src/content/lines.ts`) fixture now **must** include a `roleplay.nodes[]` entry so roleplay enumeration is locked against regression.
- **Owner:** EN-8 agent (enumerator + pre-gen — reconciled), with Agent E (TB-12 traversal) + Agent C (content, to the contract above). **Status:** RECONCILED — enumerator already covers it; TB-12/Agent C unblocked; pre-gen scope tracks authored content (no freeze). See `plans/plan-2026-07-15-en8-hosted-audio.yaml`.
- **↩ Lane A correction (2026-07-15):** confirmed the EN-8 owner's reply against the code — `linesForSituation()` (`audio-download.ts:124-129`) DOES enumerate `situation.roleplay.nodes[].npc_text` (+ `npc_voice_type`), plus `dialogues[].lines[]`, `phrase_patterns`, `vocabulary`. Lane A's original "does NOT enumerate roleplay" was a bad inference from a minimal test fixture (retracted). No enumerator gap for NPC lines.
- **DECISION — learner option-prompt audio (owner, 2026-07-15):** learner `RoleplayOption` prompts (currently NOT spoken/enumerated) get audio that is **configurable + on-demand ("tap-to-hear" at the point of engagement)**, reusing the existing `AudioButton` + `synthesizeCached`/`buildKey` path — NOT auto-played. **The one justified enumerator change:** to make that on-demand option audio also offline/503-safe, add `RoleplayOption` text to `linesForSituation()` (EN-8 owner's file) so EN-7 downloads + EN-8 pre-gen cover it via the same key. Small additive loop; flagged to the **EN-8 owner** to fold into the enumerator when the option-audio UI is built (Agent E). Until then, on-demand playback still works online via `synthesizeCached`.

### COORD-2 — Lane A review of `plan-2026-07-15-en8-hosted-audio.yaml` (for EN-8 owner) — `RESOLVED (all findings accepted + plan revised — EN-8 owner 2026-07-15)`
- **Context:** Lane A reviewed the EN-8 executable plan at the owner's request. Verdict: architecturally sound and disciplined (pure extractions before behavior change; key-normalization as an explicit prerequisite; per-phase tests + path-form commits; observability first-class; COORD-1 already reconciled correctly). The items below are the review findings — 2 should be resolved before Phase 5, 2 are quick sequencing fixes, plus minors.
- **🔴 BLOCKING-1 — write-back is unscoped (privacy + waste + unbounded public growth).** `edge-tts-writeback` fires `uploadTtsClip(key,pcm)` after EVERY `routeTts` return, and the gemini `tts` action (`index.ts:142`) is the single endpoint for ALL TTS — including tutor read-aloud and simulator **free-mode NPC lines** (LLM-generated, conversation-specific, non-curated). Consequence: permanently, world-readably hosts audio of dynamic/unique text that will never be replayed (zero cache-hit value), on a **public** bucket + a Verpex `/audio/` that **never prunes** ("No deletes"); public anon LIST also exposes the whole inventory. Potential COMP-1 / GDPR / EU-AI-Act exposure.
  - **Fix:** scope write-back to the KNOWN enumerable content set (only text in `linesForSituation`/pre-gen scope), OR have ONLY `pregen-audio.mjs` populate the bucket and drop the runtime edge write-back for dynamic text. Add a Verpex `/audio` retention story.
- **🔴 BLOCKING-2 — "staging trial" mutates the SHARED prod backend.** Edge functions + the Supabase project (`gxlrmdfqcqimwwplrdgd`) are shared across staging AND prod. Deploying `gemini` (write-back) + applying `00012` (bucket + pg_cron) takes effect for PROD traffic immediately — every prod TTS call attempts a Supabase upload and pg_cron drains project-wide, while prod's Verpex box has no pull cron (side effects, no benefit). Non-blocking, so prod playback still works, but it's not the isolated trial it reads as.
  - **Fix:** gate the edge write-back behind an env flag (e.g. `TTS_BUFFER_WRITEBACK=on`) enabled only once prod's Verpex cron exists; note explicitly that `00012` lands in the shared prod DB.
- **🟠 SEQ-1 — `tracker-spec` step is stale; will clobber a reconciled COORD-1.** COORD-1 is already reconciled (`c3ffddb`) and extended with the option-audio decision (`7f9bf20`). Re-running `tracker-spec` would rewrite that section and likely drop the option-audio decision; its validation (`grep plan-2026-07-15…`) already passes trivially. **Mark `tracker-spec` done/skip.**
- **🟠 SEQ-2 — branch cuts off `origin/develop`, which is behind local by 2 unpushed commits** (`c3ffddb` + `7f9bf20`), so the new branch won't contain the reconciled COORD-1 (feeds SEQ-1). **Push `develop` first, then branch.**
- **ROBUSTNESS-1 — TTL drain is time-based, not copy-confirmed.** pg_cron deletes Supabase objects >2h regardless of pull success; if the 15-min Verpex pull is down >2h, uncopied clips are deleted → self-heals via provider fallback but silently **reintroduces 503 exposure** during the outage. Prefer copy-confirmed deletion or a longer TTL + an `audio_sync` staleness alert.
- **ROBUSTNESS-2 — the core proof step is non-fatal.** `verify-win` + `run-pregen-level0` are `fatal_on_failure: false`, so `/execute-plan` can report success without ever proving the 503 is gone (the trial's whole hypothesis). Make `verify-win` gating (or surface it loudly).
- **Minor (flag, don't block):** (a) key-scheme normalization invalidates existing on-device/EN-7 caches (one-time re-synth; fine for greenfield); (b) log-sink shared-token ingress from the Verpex cron adds a static-token public write path to `public.logs` — needs rate-limiting/secret hygiene; (c) sample rate isn't in the cache key (a non-default-rate clip could serve a default request; listening's uncached path mostly avoids this); (d) raw PCM size — already flagged as a compression follow-up (matters for EN-7 granular downloads).
- **★ EN-8 OWNER RESPONSE (2026-07-15) — ALL ACCEPTED; plan revised (`plans/plan-2026-07-15-en8-hosted-audio.yaml`, lands on the `feat/en8-server-hosted-audio` branch at execution):** thank you Lane A — sharp review, every finding taken.
  - **BLOCKING-1 (unscoped write-back) → FIXED:** write-back is now scoped by a caller-declared `hostable` flag. Curated callers (phrases/vocab/lesson/dialogue/scripted-roleplay + the pre-gen script) pass `hostable:true`; **tutor free-chat + simulator FREE-mode pass `hostable:false` and are NEVER uploaded.** Only default-rate (24kHz) clips upload (also closes minor-c). Retention: the coverage/audit script reports orphans for cleanup.
  - **BLOCKING-2 (shared backend) → FIXED + owner decision:** owner accepted a shared-backend trial (not a separate project). Edge write-back is gated by env `TTS_BUFFER_WRITEBACK` (default **off**), flipped on only AFTER **both** staging+prod Verpex crons exist; the plan states explicitly that `00012` lands in the shared prod DB.
  - **SEQ-1 (stale tracker-spec) → FIXED:** step removed/skipped; nothing re-writes COORD-1.
  - **SEQ-2 (branch base) → RESOLVED:** `c3ffddb`+`7f9bf20` pushed; the branch now cuts off `origin/develop` `fad6e18` (includes reconciled COORD-1 + TB-11b).
  - **ROBUSTNESS-1 (time-based drain) → FIXED:** switched to **copy-confirmed deletion** — the Verpex cron POSTs the keys it confirmed-copied to an edge `audio-sync-confirm` action that deletes exactly those from Supabase (matches the owner's original "confirm then clean up"). `pg_cron` is now only a 7-day orphan backstop; missing heartbeat → staleness alert.
  - **ROBUSTNESS-2 (non-fatal proof) → FIXED:** `verify-win` is now `fatal_on_failure: true` with a scriptable check (curl L0 keys → 200 + `public.logs` absence of a provider `tts_*` event).
  - **Minors:** (b) cron→log-sink reuses the per-IP throttle + a strong shared token; (c) default-rate-only write-back; (a)/(d) acknowledged (key-invalidation self-heals; PCM compression is a tracked follow-up).
  - Also folded COORD-1/`7f9bf20`: `linesForSituation` extended to enumerate `RoleplayOption` text (offline + on-demand tap-to-hear).
- **Owner:** EN-8 agent (plan changes — done), with Lane A (review). **Status:** RESOLVED — all findings incorporated into the revised plan; ready to execute after the Verpex feasibility spike GO.

### SW-1 — Admin "all tickets" triage console — `DONE (on develop, unverified in full suite)`
- Built on `develop`, commit `b439439`: admin sees ALL tickets (all statuses), status filter + text search, submitter/date, Reopen for closed. `resolveTicket` widened to accept `open`. tsc + lint clean. e2e `tests/e2e/admin/10-admin-all-tickets.spec.ts` added.
- **Owed before it reaches testers:** full regression (SW-5) → merge `develop`→`main` → deploy (SW-6).

### SW-2 — Data lands in DB (submit + Send Logs) — `DONE (verified by design + e2e)`
- Ticket → `tickets` on submit; diagnostics → `logs` (`event='user_report'`) only on separate "Send Logs" click; the two rows are **not linked**. (Linking is SW-3 scope.)

### SW-3 — Submission enrichment — `DEFERRED (designed, DB-coordinate)`
- Reuse modal + **category** field; **contextual "Report a problem"** entry (pre-fill route + recent errors + correlation_id); structured **environment** capture (browser + device_type — mobile/tablet/desktop — via a `platform.diagnostics.getContext()` adapter). Owner decisions recorded: reuse+category+contextual; global affordance + error-state; single `resolution_note` (not threaded).
- **DB (one batched migration, coordinate with DB/PF-13 agent):** `category`, `updated_at`, `device_type`, `browser_name`, `environment` (jsonb), `resolution_note`.

### SW-4 — Agent skill to view/update tickets — `DEFERRED (design point open)`
- A slash-command/skill so an agent can list + update tickets. **Design point:** needs a DB-access path — no service-role key in local env today (only anon + DB password; cloud psql blocked). Options: mint an admin session (as e2e does), or add a service-role secret path. Decide before building.

### SW-5 — Full regression suite before promotion — `DEFERRED (owed now)`
- Owner asked to run the full suite "once ready." SW-1 is ready; run `npm run test:e2e` on `develop`, triage, before merging to `main`.

### SW-6 — Promote develop→main + deploy — `DEFERRED (after SW-5)`
- Deploy ONLY from the release worktree/main (see INFRA-1). Testers get it after regression is green.

### SW-7 — Support design doc — `DEFERRED (owed)`
- `docs/SUPPORT-TICKETS-DESIGN.md` + `REQUIREMENTS-TRACKER.md` entry capturing SW-1..SW-4, per methodology. Deferred while shipping the critical console first.

### SW-8 — Lesson-correction review queue: low-value rows, approve/reject is a no-op, no format guidance, no bulk actions — `OPEN (owner-reported 2026-07-14)`
- **Report (owner):** correction review items give very little info; unclear what checking approved vs not-approved does; the submission modal needs format guidance ("has … xyz but should have zdef …"); need **bulk approve + bulk reject**. **Approve semantics (owner): approving a correction should end up as a support ticket to be followed up.**
- **Findings (code-read 2026-07-14):**
  - **Data model** (`src/types.ts:134`): `lesson_corrections` = a single free-text `correction_text` + `lesson_id` / `user_id` / `status` / `created_at`. No structured "current vs should-be" → that's why rows carry so little.
  - **Approve/reject** (`useAdminQueues.ts` `resolveCorrection`): ONLY flips `status` → `approved`/`rejected` via an RLS `UPDATE`. **No** content change, **no** ticket, **no** notification. That is the entire effect — hence "unclear what happens": functionally almost nothing.
  - **Submission** (`src/features/learning/CorrectionModal.tsx`): placeholder is just "Describe the correction needed…" — no format guidance.
- **Scope to build:**
  1. **Approve → create a support ticket (owner-defined follow-up):** on approve, insert a `tickets` row (category=content/correction; body = the correction text + lesson reference + submitter; status=open) so it lands in the all-tickets triage console (SW-1) and gets followed up. Set correction `status=approved`. Reject → `status=rejected` (+ optional reason; consider notifying the submitter). **Confirmation + explanatory copy** so the admin knows what each button does.
  2. **Richer queue rows:** resolve `lesson_id`→lesson **title**, show submitter, timestamp, the full `correction_text`, and a deep-link to the lesson.
  3. **Submission format guidance:** structured prompt — "What does it say now?" + "What should it say?" with an example ("e.g. shows 'bom dia' but should be 'Bom dia!'"). Cheap: two client-side fields + example placeholder. Richer: add `original_text` / `suggested_text` columns (DB migration).
  4. **Bulk approve / bulk reject:** multi-select checkboxes + bulk actions on the corrections queue (extend the pattern to the other queues).
- **Constraints:** the approve→ticket insert and any schema change are **DB writes** → coordinate with the DB-owning agent (Lane B does not write DB). The admin-UI rows/bulk-actions and the submission-modal guidance are client-side.
- **Owner/lane:** Agent S/E (admin + submission UI) + DB agent (ticket-on-approve + optional structured columns) + content (what "followed up" resolves to). Priority: owner to sequence. **Status:** OPEN.

### CS-1 — Content Studio is unexplained: jargon, no purpose, raw-JSON editing — `NEEDS REQUIREMENTS (then owner approval before any coding)`
- **Report (owner 2026-07-14):** not clear what Content Studio does / what it's for / what to expect; **"nobody knows what a content pack is"** (same for *situation*, *track*, *enrichable field* — unexplained jargon); "and so on". Raw-JSON editing is opaque.
- **Findings (code-read):** Content Studio (`src/features/admin/ContentStudio.tsx` + `useContentStudio.ts`) is the admin authoring loop over the modular content model (**packs → situations → tracks**): load packs incl. drafts → edit scalar fields + nested enrichable fields (`phrase_patterns`/`vocabulary`/`cultural_notes`) as **JSON textareas** → validate (schema) → **publish** (upserts the versioned pack + re-projects `situations`/`tracks`, migration 00006, stamps version/checksum/status). Ref `docs/CONTENT-ARCHITECTURE.md §8`. There is **no in-tool explanation** and domain terms are undefined in the UI.
- **Direction (NOT a build order yet):** in-tool purpose/explainer + a plain-language **glossary** of domain terms; friendlier **structured editing** instead of raw JSON; empty-state guidance. → Needs a **written spec + owner approval before coding** (AGENTS §3 requirements gate).
- **Owner/lane:** Agent D/E (spec) → owner approval → Agent E build. **Status:** NEEDS REQUIREMENTS.

### CS-2 — Wire Content Studio into the correction follow-up (SW-8) — `NEEDS REQUIREMENTS (then owner approval before any coding)`
- **Gap:** the correction approve→ticket flow (SW-8) has no link to *where the fix is actually applied* (Content Studio). Define how an approved correction/ticket routes an editor to the relevant situation in Content Studio and back to closing the ticket. Depends on SW-8. → Needs a written spec + approval before coding.
- **Owner/lane:** Agent E + content, after SW-8. **Status:** NEEDS REQUIREMENTS.

---

## Infra / process deferrals

### INFRA-1 — Release WORKTREE (not just branch) + agent instructions — `OPEN (owed this session)`
- Owner directive: use a **release worktree** (separate checkout on `main`, deploys run only there) vs. a shared-branch model, so feature WIP on `develop` can never leak into a deploy. **Owed:** create the worktree + add agent instructions to `AGENTS.md`.

### INFRA-2 — Coverage-inventory the new admin-ticket controls — `DEFERRED (coverage owner)`
- The all-tickets search / status-filter / reopen controls are not in `tests/e2e/control-inventory.json`. Not blocking the current gate (it only fails on inventoried-but-uncovered controls), but they should be inventoried so the crawl-drift check stays honest.

### INFRA-3 — dotfiles template fix push — `DEFERRED (owner/dotfiles)`
- `commit-and-sync` co-author-trailer fix committed locally in `~/.ai-dev-dotfiles` (`238bb82`), unpushed (6 local commits, only 1 mine).

### INFRA-4 — Staging / pre-release deploy — `DONE (2026-07-14) — first live run needs the develop→main release cut`
- **Staging URL confirmed: `testfalamadeira.searchingfool.com`** (owner, 2026-07-14). Pre-release verify step before prod (`falamadeira.searchingfool.com`).
- Slotted into the release flow (`MULTI-AGENT-WORKFLOW.md` §3/§7): `develop`→`main` → deploy to `testfalamadeira` (staging) → verify → deploy to prod. Runs from the release worktree (on `main`).
- **DONE:** deploy code (staged two-target + approve gate, `57062fb`); workflow docs §3/§7/§8 (`a876082`); deploy mechanism (`deploy-verpex.sh --target staging|production`, reads `VERPEX_STAGING_REMOTE_PATH`, validates the dir contains `testfalamadeira`).
- **DONE — Supabase Auth redirect URLs (via Management API, 2026-07-14, verified):** `uri_allow_list` now = `https://testfalamadeira.searchingfool.com/**,https://falamadeira.searchingfool.com/**`.
  - **Discovery (logged):** before this change `uri_allow_list` was **empty** — so **production was ALSO not allow-listed**. Added prod alongside staging (its own origin — clearly correct, non-destructive). Current email/password auth uses no redirect, which is why the empty list never surfaced; magic-link/OAuth (AUTH-1) on any env would have failed.
  - **DONE — `site_url` set to `https://falamadeira.searchingfool.com`** (owner decision 2026-07-14; was `http://localhost:3000`). To preserve local-dev auth after moving `site_url` off localhost, `http://localhost:3000/**` was added to `uri_allow_list`. Final `uri_allow_list` = `https://testfalamadeira.searchingfool.com/**,https://falamadeira.searchingfool.com/**,http://localhost:3000/**`. All verified via read-back.
- **DONE — `VERPEX_STAGING_REMOTE_PATH`** appended to base `.env.deploy` (operator, via `!`) and copied into the release worktree's `.env.deploy` (both verified present).
- **DONE — pipeline verified end-to-end via dry-run (2026-07-14):** `deploy-verpex.sh --target staging --dry-run` resolves to `…/testfalamadeira.searchingfool.com`; `--target production --dry-run` resolves to `…/falamadeira.searchingfool.com` (target/dir guards correct); dry-run is credential-free + no network. Production-gate ordering confirmed in code — the "not staged+approved" `die` (lines ~200-210) fires **before** the rsync transport (~238), so a real prod deploy refuses before any upload.
- **⚠️ First LIVE staged deploy requires the first `develop`→`main` release cut.** `main` (release worktree) is stale (`1d16e6f`) and still has the OLD `deploy-verpex.sh` with no `--target` (verified: 0 refs on main vs 12 on develop; `57062fb` not yet an ancestor of `main`). The new script reaches `main` when the Release role merges `develop`→`main` in the release worktree (MULTI-AGENT-WORKFLOW §7) — which is the step *before* it deploys, so the sequence is self-consistent. Until that cut, dry-run/verify from the base worktree (on `develop`) only.

---

## Enhancements (future releases — backlog)

### EN-1 — Audio buttons need immediate click feedback (loading/playing state) — `OPEN (backlog, future release)`
- **Report (owner):** clicking any audio icon gives no feedback, so if playback takes a moment a user clicks repeatedly instead of waiting. Add an immediate visual state (spinner / pulsing / disabled-while-loading) on tap.
- **Scope:** app-wide — every audio play control (phrases speaker, Vocabulary "Play the word", Simulator "Play line", Listening, speaking reference, drill audio, tutor/coach TTS). Today playback is fire-and-forget with no DOM "playing" indicator (confirmed in e2e specs 44/45 + `usePractice`/vocab `playText`).
- **Implementation hint:** centralize at the shared audio seam — `src/hooks/useSpeechPlayback.ts` / `geminiService.playSpeech` (resolves when playback STARTS, `onEnded` when it stops) — expose a `status: idle|loading|playing` and a shared `AudioButton` that reflects it + guards double-taps. One change covers all call sites.
- **Owner:** Agent E (`feat/*`). Priority: future release (not blocking). **Status:** OPEN (backlog).

### EN-2 — Local-data transparency + "clear my local data" — `OPEN (backlog, future release)`
- **Report (owner):** a user has no way to see what's stored locally on their device, or to clear/delete it.
- **What exists today:** Settings shows **audio-cache** usage ("Used: X of Y MB") + a **"Clear cache"** button (`handleClearAudioCache`) + a save-audio-on-device toggle. That's the ONLY local data surfaced/clearable.
- **Gap:** the rest of local storage is invisible + unclearable — IndexedDB `KV_STORE` (onboarding record, path selection, mastery/SRS + progress cache, offline sync queue, settings), `localStorage` (prefixed keys), and PWA service-worker caches. No "what's stored" view beyond audio; no "clear all local data / reset device data."
- **Feature:** a "Local data" settings section listing categories + sizes (use `navigator.storage.estimate()` for the total) and a **"Clear local data"** action. **Must warn** that the offline **sync queue** may hold *unsynced* writes (lost on clear); progress/mastery are server-backed (safe to clear when signed in). Keep the existing audio "Clear cache" as one row.
- **Implementation:** `platform.storage` needs `enumerate`/`clear` helpers over `KV_STORE` + `BLOB_STORE` + the `localStorage` prefix + SW `caches`. Fits the "calm, honest, real consent" principle (data transparency).
- **Owner:** Agent E (`feat/*`). Priority: future release. **Status:** OPEN (backlog).

### EN-3 — In-app PWA "update available" prompt — `OPEN (backlog; offered, not yet confirmed)`
- PWA is `registerType: 'autoUpdate'` with no in-app prompt → new versions land silently on next relaunch; testers must fully close/reopen to get a fix. Add a `needRefresh` toast ("New version available — reload") via `useRegisterSW` so updates are visible/one-tap. Owner: Agent E (`feat/*`). Priority: future release.

### EN-4 — In-app "About": release version + release notes — `DONE (built on develop, commit 5f75fc8; awaiting release cut)`
- **DONE (2026-07-14, commit `5f75fc8`):** built per owner ask ("fold it in now"). Settings → **About** shows app name + tagline, running version (`__APP_VERSION__` injected from root `VERSION` via vite `define`), **per-version release notes** parsed from canonical `CHANGELOG.md` (`?raw`), links to Terms/Privacy/AI-Disclosure/Support, credits. Files: `src/features/about/{AboutModal.tsx,changelog.ts,index.ts}`, `vite.config.ts`, `src/vite-env.d.ts`, `SettingsView.tsx`. Regression: vitest parser test + e2e `user/53` (both GREEN, Run 22). **Release-cut still MUST bump `VERSION` + add the `CHANGELOG.md` entry** (hard gate, §7) so About shows the shipped version's notes.
- **Report (owner):** users need to see the release version + release notes in the app (an "About" link or similar).
- **Report (owner, 2026-07-14):** the **macOS window menu-bar "About FalaMadeira" is empty** — owner wants version there + a link to release notes **for each version** (history, not just current).
- **CONSTRAINT / finding (2026-07-14):** on macOS this app is an **installed PWA** — packaging is Capacitor **iOS/Android + web only** (`@capacitor/ios`, no `@capacitor/electron`, no Tauri/Electron macOS target). The native macOS menu-bar "About" is **provided by the browser/PWA shell and CANNOT be populated by our web code.** So the owner ask is delivered as an **in-app About surface**, not the native menu item. (On iOS Capacitor, native app-info is settable separately, but that is iOS, not the macOS menu.) Do **not** promise native-menu content for the PWA.
- **Sources exist:** `VERSION` (CalVer `2026.07.14.1`) + `CHANGELOG.md` are the version + release-notes sources. Version is **not yet exposed to the client at runtime** (vite has a `define:` block but no version define); no About/version UI exists.
- **Feature:** inject the version at build (vite `define` `__APP_VERSION__` from `VERSION`), add an "About" entry in Settings showing version/build + release notes. Render **per-version** history from `CHANGELOG.md` entries (each version's notes), not only the current version, to satisfy the "release notes for each version" ask.
- **Deployment-workflow tie-in (owner ask):** the release cut MUST bump `VERSION` + add a `CHANGELOG.md` entry (release notes) — already in the MULTI-AGENT-WORKFLOW §7 release checklist; About surfaces exactly those. Make the CHANGELOG entry a **hard release gate** so About always shows current notes. (Cross-refs the versioning rollout, TODO #122.)
- **Regression (per §3 methodology):** vitest for the version-define/CHANGELOG parser + an e2e opening Settings → About asserting the current version string + at least one release-notes entry render.
- **Owner:** Agent E (`feat/*`) + release-workflow. Priority: future release (owner may pull into the imminent release — small: vite define + Settings About + CHANGELOG render). **Status:** OPEN (backlog).

### EN-5 — Quiz results: persist + admin visibility + regression — `OPEN (backlog; owner-identified gap)`
- **Gap (confirmed 2026-07-14):** quiz **scores/results are NOT stored** — only pass/fail survives as `profiles.completed_lessons` (score ≥ 3). No score value, per-question data, attempt history, or timestamp; the score doesn't feed the SRS/mastery engine.
- **What IS covered:** completion persistence is regression-tested against the DB — `user/25-learning-quiz-progression-write.spec.ts` asserts `profiles.completed_lessons` is written after a passing quiz; `user/21-quiz-full-flow` covers the UI/scoring flow.
- **No admin visibility:** there is NO admin view of quiz results/scores/completions (admin has only moderation queues + Content Studio).
- **Feature:** (1) store quiz results in a new table (score, lesson/situation id, timestamp, optional per-question) — small DB migration, **coordinate with DB agent**; (2) add an **admin learner-progress / quiz-results view**; (3) e2e asserting the result row is written AND shown in admin; (4) fix the hardcoded `score >= 3` threshold → relative to `questions.length` (a pass ratio); (5) consider feeding results into `applyGrade`/mastery.
- **Owner:** Agent E (`feat/*`) + DB agent (migration) + admin UI. Priority: future release. **Status:** OPEN (backlog).

---

## Cross-references (owned elsewhere, not buried)
- **Versioning rollout** → `aidevops/plans/plan-2026-07-14-versioning-rollout.yaml` (TODO #122).
- **PF-13 schema drift, observability** → other agent (DB).
- **EF-34/35 (Lane A specs), EF-36 (WS2 test-user isolation)** → `docs/E2E-LIVE-RUN-TRACKER.md`.
- **DF1–DF10** (translation slices, gamification celebrate-layer, place-graphics extension) → `REQUIREMENTS-TRACKER.md` §"Deferred/open follow-ups (2026-07-14)".

---

## Compliance / legal notices

### COMP-1 — Data-security, privacy (GDPR) & EU AI Act notices — `OPEN (owner/legal review; owed pre-beta)`
- **Owner directive (2026-07-14):** ensure notices exist for **data security, privacy, and EU AI Act conformance** even where the build doesn't yet support everything (forward-honest; no over-claiming).
- **Foundation:** `src/features/legal/{terms,privacy,ai-use}.ts` + `LegalPage`; onboarding captures `has_accepted_terms` + `has_accepted_ai_usage`. A quick grep found NO explicit coverage of local caching, GDPR data-rights, or EU AI Act transparency — needs a proper read + drafting.
- **Required (careful drafting + likely legal review):** (1) **local-caching disclosure** (owner decided always-cache + inform) → privacy/storage notice + release notes/About (ties EN-2/EN-4); (2) **data security** (TLS, RLS, server-side secrets, retention, contact); (3) **GDPR** (data collected incl. logs w/ correlation IDs, lawful basis, retention, access/erasure — delete-account exists; processors Supabase + Google/Gemini; international transfer); (4) **EU AI Act transparency** — inform users they interact with an AI tutor + that lessons/corrections/chat are AI-generated (Art. 50); confirm risk class (likely limited-risk, confirm w/ counsel); extend `ai-use.ts`.
- **Owner:** owner/Agent D + legal. **Status:** OPEN (owed before real beta testers).

## QA / test hardening

### QA-4 — Content coverage: lessons should cover ≥ the 1000 most-used Portuguese words (owner 2026-07-16) — `OPEN (quality follow-up — content audit)`
- **Ask (owner):** as a matter of course, the authored curriculum vocabulary should cover at least the **top ~1000 most-frequent European Portuguese words**, so learners get the highest-utility vocabulary by default.
- **Current state:** live content has **1,145 vocabulary words** across 187 situations (6×28 daily lessons + ~19 goal-track). No check that this set overlaps a frequency list — coverage unknown.
- **Direction (later):** obtain a top-1000 pt-PT frequency list → diff against the authored `vocabulary` set → report coverage % + missing high-frequency words → enrich content to close gaps. Feeds EN-18 (better default vocab pool) + content authoring.
- **Owner:** content (Agent C) + Lane B (audit script). **Status:** OPEN — quality follow-up, not blocking EN-18.

### QA-3 — EN-10 (inventory-first vocab lookup) shipped with stale/broken e2e — `FIXED by Lane B (release-gate), flagged to Lane A`
- **Gap (caught by the pre-staging full regression, 2026-07-15):** Lane A's `8a79591` "feat(EN-10): inventory-first vocab lookup" changed the Vocab Lookup modal UI but did NOT update its e2e — `user/03`, `user/04`, `user/43` failed. Two causes (feature works; specs were stale): (1) input placeholder renamed `"Enter a word or phrase..."` → `"Portuguese or English word..."`; (2) inventory-first resolves an in-inventory word synchronously so the transient loading spinner is no longer observable.
- **Fix (Lane B, commit `a2742ef`):** updated the placeholder selectors (03/04/43) and changed `user/43`'s assertion to the deterministic response (spinner OR the rendered result's "Explanation" section). Verified deterministic (`43:98` ×3 green).
- **Flag to Lane A:** per AGENTS §3, EN-10 should have shipped its own updated e2e. If you want a stricter assertion (e.g. exercise the AI-fallback loading path with a known-miss word, which needs your inventory knowledge), refine `user/43`. **Owner:** Lane A (feature); coverage restored by Lane B. **Status:** e2e green.

### QA-2 — Edge voice-limit fix shipped untested (TB-8 edge) — `OPEN (coverage gap; owner: edge/DB agent)`
- **Gap (found 2026-07-15 reviewing the 2026.07.15.2 release):** commit `4d62982` "fix(edge): voice limit reads global_settings.voice_limit, not hardcoded 5" changed `supabase/functions/gemini/index.ts` (server-side authoritative limit) and shipped with **ZERO tests**. Client pieces ARE covered — `user/55` (Settings reflects the server value) + `user/27` (client-side enforcement blocks recording) — but nothing verifies the **edge** actually reads `global_settings.voice_limit` for enforcement.
- **Owed (per AGENTS §3 "test every change"):** an integration/e2e (or edge unit) that sets a low `global_settings.voice_limit` and confirms the gemini edge rejects a voice call past it (structured `VOICE_LIMIT_REACHED`), i.e. the server enforces the *configured* value, not a hardcoded default. Ties EF-36 / WS2 test-user isolation (voice-limit e2e is flaky on the shared user).
- **Owner:** edge/DB agent (whoever owns `supabase/functions/gemini`). **Status:** OPEN (coverage gap, tracked so it isn't buried).

### QA-1 — Offline-audio download tests — `OPEN (Agent T)`
- **(a) Download works:** `src/lib/__tests__/audio-download.test.ts` — mock synthesize + storage; `downloadForOffline(scope)` enumerates the scope's speakable lines, stores a clip each, reports `{synthesized, fromCache}`, respects storage-limit/abort.
- **(b) Survives an app/SW upgrade:** `src/platform/web/__tests__/storage.web.test.ts` — put blobs in the `audio` store at DB_VERSION, reopen at a HIGHER version through the real guarded `onupgradeneeded`, assert blobs persist. Locks the non-destructive migration. (Survival verified SAFE today: IndexedDB is a separate tier from the SW precache; `onupgradeneeded` is create-if-missing; `cleanupOutdatedCaches` sweeps only precache.)
- **Owner:** Agent T. **Status:** OPEN.

## Infra additions (Lane A, 2026-07-14)

### INFRA-4 UPDATE — staging target VERIFIED + gating decided
- **Verified (read-only SSH):** `/home/gomadeir/testfalamadeira.searchingfool.com` EXISTS (created 14:56), empty of app content (only cgi-bin + .well-known), same server/account as prod `/home/gomadeir/falamadeira.searchingfool.com`.
- **Gating decided (owner): SEPARATE APPROVE STEP** — `deploy --target staging` → `deploy --approve` (records approver+time, tied to the staged build hash) → `deploy --target production` (REFUSES without a fresh matching approval). Enforcement MUST live in `deploy-verpex.sh`, not just docs (a checklist is skippable) — so the pre-release step happens no matter which agent runs it.
- **Design:** `--target staging|production` selects `VERPEX_STAGING_REMOTE_PATH` vs `VERPEX_REMOTE_PATH`; guards — staging requires `*testfalamadeira*`, prod requires `falamadeira` AND NOT `testfalamadeira`; bare `deploy` errors (no default). Prereqs: set `VERPEX_STAGING_REMOTE_PATH` in `.env.deploy`; add `https://testfalamadeira.searchingfool.com/**` to Supabase Auth redirect URLs.
- **DONE (code, commit `57062fb` on develop):** `deploy-verpex.sh --target staging|production` + separate `--approve`; production REFUSES unless THIS git commit was staged+approved (enforced in-script, verified — refuses before the transport). npm scripts `deploy:staging`/`deploy:approve`/`deploy:production`; `.env.deploy.example` + `.gitignore` (`.deploy-state.json`) updated.
- **Still owed:** (1) plug staging→approve→prod into AGENTS.md + MULTI-AGENT-WORKFLOW §7/§8 (were locked); (2) operator prereqs — set `VERPEX_STAGING_REMOTE_PATH=/home/gomadeir/testfalamadeira.searchingfool.com` in `.env.deploy`, add `https://testfalamadeira.searchingfool.com/**` to Supabase Auth redirect URLs. **Status:** code DONE; docs + prereqs OPEN.

### INFRA-5 — Per-worktree agent profiles + selective secret provisioning — `DONE (2026-07-14)`
- Model B worktrees need config to "just work". The `.{claude,codex,agy}-w/profiles/falamadeira-dev.json` profiles are PATH-BOUND (startup_repo + scopes hardcode the base `fala_madeira` path), so a worktree at a different path isn't covered; git worktrees also don't carry gitignored files (`.env.local`, `.env.deploy`, `.claude/settings.local.json`, `node_modules`).
- **DONE — `scripts/setup-worktree.sh <role>`** (roles: `feat|support|content|release`). One command per role: creates the worktree on its allowed branch if missing → `npm install` → **generates a PATH-CORRECT `claude-w` profile** for that worktree (solves the path-bound problem generically — profiles are *generated per path*, not hand-maintained) → prints the operator-only secret-copy commands. Idempotent. Launch a role agent with `claude-w --profile falamadeira-<role>-dev`.
- **Profiles generated + verified (2026-07-14):** `falamadeira-{feat,support,content,release}-dev.json` in `~/.ai-dev-dotfiles/.claude-w/profiles/`; all 4 load via `claude-w --profile … --dry-run` and resolve `repo_path` to the correct worktree. Per-role write scopes: feat/support → `src public supabase/functions`; content → `src/content public`; release → worktree root (bump/merge/deploy). `README.md` + `AGENTS.md` edits denied in every role (canonical — edit on `develop` in base).
- **Secret provisioning is OPERATOR-ONLY by design:** the agent harness *hard-denies* agent `cp` of `.env*`. `setup-worktree.sh` therefore PRINTS the exact `cp` lines (`.env.local` to feat/support/content+release; `.env.deploy` to release ONLY — least-privilege) for the operator to run via `!`; it never copies secrets itself.
- **Decision (logged):** the generated profiles are **NOT committed** to `~/.ai-dev-dotfiles` — they are generated artifacts (`setup-worktree.sh` is the committed source of truth), the base `falamadeira-dev.json` was already untracked, and the dotfiles working tree holds other agents' uncommitted changes (not our lane to sweep). If the ai-dev-dotfiles owner later wants them tracked, that's a dotfiles-side task.
- **Deferred (not blocking):** codex-w/agy-w equivalents of the per-role profiles (script currently generates the `claude` platform profile only). Add `--platform` to `setup-worktree.sh` when a non-Claude agent needs a role worktree.
- **Status:** DONE (Claude platform). Committed with `setup-worktree.sh`.

### INFRA-6 — Instantiate the worktree fleet — `DONE (2026-07-14)`
- **Reality verified 2026-07-14 (before):** the fleet was **designed + documented (MULTI-AGENT-WORKFLOW) + enforced (branch guard, staged-deploy gate) but NOT stood up** — `git worktree list` showed ONLY the base `fala_madeira` on `develop`.
- **DONE 2026-07-14 — `git worktree list` now shows all 5:**
  ```
  fala_madeira          develop          # base — Agent D (docs) / T (tests)
  fala_madeira-feat     feat/scratch     # Agent E (rename per task)
  fala_madeira-support  fix/scratch      # Agent S
  fala_madeira-content  content/scratch  # Agent C
  fala_madeira-release  main             # Release (deploy ONLY)
  ```
  (topic branches renamed per task; base stays on `develop`.)
- **Provisioned:** `node_modules` installed in all four new worktrees (`npm install`, exit 0). `.env.local` copied into **feat/support/content AND release** — *refinement of the original line:* release needs `.env.local` because `deploy-verpex.sh` runs `npm run build` (bakes `VITE_SUPABASE_*`); `.env.local` also carries `SUPABASE_DB_PASSWORD`/`SUPABASE_ACCESS_TOKEN`/`GEMINI_API_KEY`. `.env.deploy` (Verpex SSH secret) copied into **`-release` ONLY** (least-privilege — only the release worktree deploys). All `.env*` remain gitignored in every worktree (no commit risk). *Secret copies were run by the operator via `!` — the harness hard-denies agent `cp` of `.env*`.*
- **Verified:** `npm run check:branch` = OK in base/feat/support/content. Release is on `main` and the guard *rule* is satisfied, but **`main` is stale** (at `1d16e6f`, behind `develop`) so it lacks the `check:branch` script + staged-deploy tooling. Those land on the **first `develop`→`main` release cut**, which happens *in* the release worktree before it deploys (MULTI-AGENT-WORKFLOW §7) — so the sequence is self-consistent; not a defect.
- **Still open (rolled into INFRA-5):** `scripts/setup-worktree.sh <role>` automation + per-role launcher profiles so an agent boots knowing its role.
- **Owner:** Lane A. **Status:** DONE (instantiation + provisioning + verification complete).

### INFRA-7 — Rename the `gemini` edge function → `ai-gateway` — `CODE DONE + DEPLOYED (2026-07-16); delete old `gemini` after prod rollout`
- **Why (owner, 2026-07-16):** the edge function name `gemini` caused confusion — it's the app's general AI/voice edge (actions: chat, tts, translate, generate-lesson, error-analyst; TTS routes through azure/google/polly/gemini), not Gemini-specific.
- **Done (commit `2c93143`):** `git mv supabase/functions/gemini → ai-gateway`; updated every caller — `geminiService.ts` (5 `invokeEdgeFunction` targets), `listeningAudio.ts`, the `geminiService` unit assertion, and 11 e2e specs (route mocks + real-call labels + the CORS-preflight function list). Left unchanged (unrelated): the TTS *provider* named `gemini` (`_shared/tts/*`, `types.ts`) and the `geminiService` client class + `_shared/gemini.ts` provider module.
- **Migration (phased — live prod):** `ai-gateway` deployed to prod; the old `gemini` function is **kept live** so the currently-deployed prod/staging frontends keep working. Verified: real CORS preflight to `ai-gateway` passes (e2e `51`) + `user/59` confirms the client calls `ai-gateway`; full suite 127/130 on the combined tree. Shipped to staging in 2026.07.16.3/.4.
- **REMAINING (gated):** **delete the old `gemini` edge function ONLY after** a prod release on `ai-gateway` is approved + verified (else prod, still calling `gemini`, breaks). Also fold the rename into `docs`/`_shared/appHelp` comments on a later pass (generated file, low priority).
- **Owner:** Lane B (edge/client). **Status:** CODE DONE + `ai-gateway` DEPLOYED; delete-old-`gemini` pending prod rollout.

### EN-6 — Quiz checking: more flexible / AI-driven grading — `OPEN (backlog; owner wants to discuss)`
- **Report (owner 2026-07-14):** quiz checking is too strict. For a **listening** exercise, a missing exclamation mark (or other punctuation/case) should NOT be marked wrong — we're testing listening comprehension, not punctuation. Don't fixate on the wrong things. Wants more flexible matching, potentially **AI-driven**.
- **Direction (to discuss):** (1) **Normalize before compare** (cheap, deterministic, offline): lowercase, strip/relax trailing punctuation + diacritics-optional + collapse whitespace, per exercise type (stricter for spelling drills, looser for listening/meaning). (2) **Fuzzy match** (Levenshtein/token overlap) with a per-mode threshold. (3) **AI-driven semantic grade** (gemini) for open/meaning answers — highest quality, but adds cost/latency + needs the offline/degrade story (ties EN-8 audio-cache + EF-36 quota). Likely layer 1+2 first (covers the punctuation complaint immediately, offline), AI for genuinely open answers.
- **Scope note:** grading logic lives across the quiz/practice graders (`src/components/Quiz.tsx`, practice `speaking`/`vocabulary` accuracy). A shared, per-mode "answer match" policy would centralize this.
- **Owner:** Agent E (`feat/*`), product to define tolerance per mode. **Status:** OPEN (backlog; discuss).

### EN-7 — Offline/background downloads must be modular + resilient (granular chunks) — `OPEN (HIGH PRIORITY — owner escalated 2026-07-14)`
- **Report (owner 2026-07-14):** downloads should be more modular, at least in the background — otherwise they "just totally fail all the time" (a single large/all-or-nothing download is fragile).
- **Report (owner, escalation 2026-07-14):** the current download granularity is **per-level (the 7 levels)**, and "the downloads are too big this way and they timeout and fail." Wants **more granular parts** than whole-level. High priority.
- **Current state (verify in code):** the Settings offline download offers "Download all" / per-track(level) downloads (`SettingsView` offline section + `src/lib/audio-download.ts`). Each level bundles all its situations' audio → large payload → single long-running fetch that times out on slower connections with no resume.
- **Direction:** chunk downloads into small independently-retryable units — **finer than a level** (per situation / per lesson / per asset), with per-unit progress + resume, exponential-backoff retry, and partial-success (a failed unit doesn't fail the whole batch); run in the background (service worker / Capacitor background) so a dropped connection resumes rather than restarts. Surface per-unit state + let the user pick sub-level scopes. Ties EN-8 (pre-generated/server-hosted audio is the bulk of weight — hashed per-asset URLs make per-asset download natural) and QA-1/QA-1b (offline-audio + survive-upgrade tests). Directly reduces timeout/failure risk the owner is hitting.
- **PHASE-1 ENGINE DONE (2026-07-14, commit `074ba69`, branch `feat/en7-download-resilience`):** (1) **per-clip retry with exponential backoff** (`config.offline.downloadMaxAttempts=3`, `downloadRetryBaseMs=800`) in `audio-download.ts` — transient 429/503/network/timeout failures retry instead of counting as failed, so a large run stops failing wholesale; (2) **`situationId` download unit** added to `DownloadScope` + `SituationFilter` (`repository.ts`) — the finest granular unit (one situation at a time). 3 unit tests (retry recovers / retry exhausts+continues / situationId scoping); 197 unit tests green; lint+build clean. **Coordination:** the UI (per-situation picker in Settings) is DEFERRED — `SettingsView`/`useSettings` are being edited concurrently by `feat/nav-audio-limits-batch` (EN-9 done + audio-limits next); wiring the picker there now would collide. Wire phase-1b AFTER that batch merges.
- **REMAINING (phase-1b UI + phase-2):** per-situation picker UI in the Settings offline section (uses the new `situationId` scope); background/resume via SW/Capacitor; per-unit progress list. Ties EN-8 (server-hosted hashed per-asset URLs make per-asset download natural) + QA-1/QA-1b.
- **Owner:** Agent E (`feat/*`). **Status:** OPEN — engine landed on a branch; UI deferred for coordination; not yet merged to develop.

### EN-8 — Pre-generate + server-host audio (cache tiers) to cut Gemini cost + kill 503s — `OPEN (HIGH — owner greenlit a first-lessons trial 2026-07-15)`
- **★ LIVE MOTIVATION (2026-07-15, via public.logs + owner):** Gemini TTS is returning **503 (service unavailable/overloaded)** — owner hit `Server text-to-speech is unavailable. Falling back to device speech. (Ref: 4347eeb6)`; logs show `tts_unavailable {attempted:["gemini"]}` (azure not configured, so gemini-only). Device-speech fallback works (graceful) but voice quality/consistency suffers. Pre-hosted audio **removes the per-play Gemini dependency entirely** → no 503s for pre-generated content. This is now the primary driver, not just cost.
- **★ THE TEXT↔AUDIO MAPPING ALREADY EXISTS (verified 2026-07-15):** `audioCache.buildKey(provider, voice, text)` = `fm-audio:<provider>:<voice>:<hashText(text)>` (`src/lib/audioCache.ts:49`) is a stable, deterministic key per (provider, voice, text) — the device cache already uses it. Server-hosted files just reuse that key as the filename/URL path, so the client lookup naturally becomes **device cache → our-server `/<buildKey>` → Gemini**. The finite text set is already enumerable via `linesForSituation()` (`src/lib/audio-download.ts`) — the same code the offline download walks. So most plumbing exists; EN-8 adds (a) a pre-gen+upload script and (b) a server-fetch step in `synthesizeCached`.
- **★ FIRST-LESSONS TRIAL (owner greenlit 2026-07-15):** start small — pre-generate audio for the **first lessons/situations only**, host them, prove the device→server→Gemini lookup, then expand. Phase-1: (1) a script that enumerates first-lesson lines (`linesForSituation` filtered to the starter situations), synthesizes each via the edge TTS, writes files named by `buildKey`, uploads to the web server (Verpex static dir or Supabase Storage — DECISION NEEDED); (2) add a `fetch(serverBase + '/' + buildKey)` step in `synthesizeCached` before the Gemini call (200 → cache+play; 404/err → Gemini fallback). Backward-compatible; unhosted text just falls through to Gemini as today.
- **Open decision:** storage target — **Verpex static** (same box as the site; simplest, free, CDN-less) vs **Supabase Storage** (public bucket, signed/cached URLs) vs a CDN. Recommend Verpex static for the trial (zero new infra).
- **Owner reinforcement (2026-07-14):** owner re-requested this directly — "pre-loading audio from our server, pre-creating the audio, storing it on our webserver, and only going to the TTS provider if there is no audio already available … will require some design." Confirms the lookup order below (device cache → our server → TTS-generate-and-persist) and the pre-generation pipeline. No change to scope; priority reaffirmed. Pairs tightly with EN-7 (server-hosted per-asset files are exactly what granular downloads fetch) and SEC-1/EF-36 (removes per-play TTS dependency → fewer 429/503).
- **⚠ COORDINATION (see COORD-1, 2026-07-15):** `linesForSituation()` currently enumerates only `phrase_patterns` + `vocabulary` — it misses the **simulator's authored roleplay-node text** (the most 503-exposed surface). Owner asked to coordinate NOW: extend the enumerator + include the first track's simulator situations in the first-lessons pre-gen scope so scripted-simulator audio is pre-hosted via the same `buildKey`. Decide before freezing the pre-gen set.
- **Report (owner 2026-07-14):** rather than every user hitting Gemini TTS, pre-generate the audio for content phrases and **store it on our server** (Verpex), so the client only calls Gemini when the audio is missing from (a) device cache, then (b) our server. "Reduce cost — right?"
- **Assessment (yes, likely a large cost + reliability win):** content phrases are a FINITE, mostly-static set → generating each once and serving a static file is far cheaper than regenerating per user per play, and it removes the per-play Gemini dependency (directly mitigates the 429/503 quota pain behind EF-36 / user/47). Lookup order: **device cache → our server (CDN/static) → Gemini (generate, then persist to server + device)**. 
- **Design sketch:** a build/admin step renders TTS for every content phrase to audio files keyed by a stable hash (text+voice+lang), uploads to Verpex (or Supabase Storage) under a versioned path; client `playText` checks IndexedDB cache → fetches the hashed URL (200 → cache + play) → on 404 falls back to the gemini edge fn, then writes the result back. Ties EN-7 (these files are what gets downloaded for offline) + the audio-cache already in `src/lib/audioCache.ts`.
- **Trade-offs to investigate:** storage/CDN cost vs TTS savings (favorable for static content); cache invalidation on content/voice change (hash key handles it); which store (Verpex static vs Supabase Storage vs a CDN); pre-generation pipeline ownership.
- **★ DEVICE PERSISTENCE — BUILT (2026-07-17, `feat/en8-server-hosted-audio`, commits `89b47c5` + `996eb63`; owner design session 2026-07-17):** the client tiering (EN-7/EN-8 "save audio on device") is now correct. **Verified defect:** `synthesizeCached` never read `offline_save_audio` — played clips only warmed the ephemeral LRU (cleared on logout), never a durable store, and turning the toggle OFF cleared the *cache* (conflating cache vs saved). **Owner model (agreed):** a locally-stored curated clip serves BOTH intents (local⇒fast, persistent⇒offline), so collapse the duplication. **Delivered:** `audio_pinned` is now the DURABLE saved store — written on PLAY of curated (`hostable`) clips (not only downloads), bounded by the user storage budget, survives logout/restart; the `audio` LRU is now ephemeral private-only (cleared on logout, SEC-2). Downloads are PROTECTED (never evicted by playback); auto-saved plays are reclaimed oldest-first; a play that can't fit falls back to the cache, a download that can't fit surfaces "out of offline space" + a take-me-there (EN-18 `focusControl('storage-limit')`), never silently dropped. Toggle OFF deletes the durable store (not the cache); Clear-cache/logout clear only the ephemeral tier; "Used" = cache+saved. No cache/saved duplication (a downloaded clip serves from the durable store on replay, never re-cached). Full gate green (319 unit incl. routing/protect-vs-reclaim/refusal/fallback/no-retry). **OWED:** e2e play→reload→served-from-device (needs running app; folds with the completion plan's `test-server-tier-e2e`); native filesystem protect-flag port (web-only for now — native not shipping). Client tiering stays INERT for server hits until the Verpex/Supabase warm lands (server side of EN-8 below). Design + assumptions recorded in `fala_madeira-support/plans/.plan-state-en8-hosted-audio.yaml` (`device-persistence-fix` + `device-persistence-protect`).
- **Owner:** Agent E (`feat/*`) + ops (storage/pipeline). **Status:** OPEN — device-persistence (client) BUILT on the branch; server-hosting trial completion still open (see the ★ EN-8 callout at the top).

### EN-9 — Sign out must be in the nav sidebar (always available), not only on the Profile page — `BUILT on develop (desktop sidebar) — test coverage added 2026-07-17 (beaf322); mobile parity still owed (ties EN-21)`
- **✅ RECONCILED (2026-07-17):** the desktop sidebar Sign Out control was already built + wired to `handleLogout` (`Sidebar.tsx:117-125` renders a red `LogOut` button on `onSignOut`; `App.tsx:317` passes `onSignOut → handleLogout`) but had shipped **without a test** (coverage gap). Added `data-testid="nav-sign-out"` + component test `src/components/__tests__/Sidebar.signout.test.tsx` (renders + calls onSignOut + absent when unwired), 2/2 green, pushed `beaf322`. **Still owed:** MOBILE affordance — the sidebar is desktop-only (`hidden md:flex`); mobile still relies on the Profile-tab Sign Out. Decide the mobile control separately (ties EN-21 chat/nav rework).
- **Report (owner, staging 2026-07-14):** "Sign out should be available in the navigation sidebar near the bottom at all times, not just in the Profile at the bottom of the page." High priority.
- **Current state (verified in code):** the only Sign Out control lives in **`src/features/settings/SettingsView.tsx:616`** (a red "Sign Out" button at the bottom of the Profile tab). To sign out, a user must navigate to Profile and scroll to the bottom. The desktop sidebar (`src/components/Sidebar.tsx`, `hidden md:flex md:flex-col`, L52) renders nav items in a `flex-1` `<nav>` and does **not** currently receive or render a logout control.
- **Fix direction:** add a persistent Sign Out control pinned at the **bottom of the desktop sidebar** (below the `flex-1` nav, above `safe-area-bottom`), wired to the existing `handleLogout` — thread `handleLogout` into `SidebarProps` from `App.tsx` (already available there, `App.tsx:88`). Keep the Profile-page control too (or make the sidebar one the primary). **Mobile note:** the sidebar is desktop-only (`md+`); the mobile bottom tab bar has no room for a sign-out — decide the mobile affordance separately (e.g., a small header action, or keep Profile-page sign-out on mobile). Reuse the same red-text styling + confirmation behavior as the current button; no new logout logic.
- **Owner:** Agent E (`feat/*`) or S. Small, self-contained UI change + prop threading + a test asserting the sidebar sign-out calls `handleLogout`. **Status:** OPEN (HIGH PRIORITY).

### EN-10 — Vocab lookup: bidirectional (PT↔EN) + diacritic-insensitive/fuzzy + inventory-first search — `BUILT (inventory search on develop, commit 8a79591, 2026-07-15) — AI-fallback edge deferred`
- **★ BUILT (Lane A, owner go 2026-07-15, commit `8a79591`):** the inventory-first hybrid (steps 1–3 of the design) is on `develop`. New pure matcher `src/features/phrases/vocabSearch.ts` (`buildVocabIndex`/`searchVocabInventory`, diacritic-strip normalize reusing `search.ts`'s `normalizeForSearch`, bounded Levenshtein per length) over bundled content; `VocabLookupModal` accepts "Portuguese or English word" (bidirectional); `useLessonModals.handleVocabLookup` searches inventory first and falls back to the existing `geminiService.translateWord` ONLY on a miss. 14 unit tests (diacritics, both directions, fuzzy, miss→fallback); tsc+eslint clean. Built via isolated-worktree subagent (a stray raw-NUL dedup delimiter was fixed to a `\0` escape before landing).
- **DEFERRED (step 4 — coordinate with EN-8/Lane B):** the AI-fallback edge `translate` prompt currently assumes PT→EN input; making it auto-detect input language is a change to `supabase/functions/gemini/index.ts` + `geminiService.ts` (Lane B's active EN-8 files) — left untouched. Flag to the EN-8 owner to fold the language-auto-detect in when they next touch the edge. Inventory search already covers offline + the bulk of the request; the AI path only handles words NOT in the finite curriculum.
- **Report (owner):** the vocab link/modal should accept **either** a Portuguese **or** an English word and translate in **either direction** (not just a Portuguese-only field); search should **not fixate on diacritics** (find close matches, not only exact); and — the open question — should the search be AI-driven, or do we have the app's vocabulary inventoried and searchable somewhere?
- **Answer to the question (investigated 2026-07-14):**
  - **Current lookup is purely AI-driven, one-directional.** `handleVocabLookup` (`useLessonModals.ts:153`) → `geminiService.translateWord` → `gemini` edge `action:translate`, whose prompt is hardcoded **PT→EN** (`supabase/functions/gemini/index.ts:131`). No fuzzy/diacritic handling (free-text LLM call). Modal is a single field, copy says "Type any Portuguese word…" (`VocabLookupModal.tsx:80,139`).
  - **The vocabulary IS inventoried as structured data.** Situations (and lessons) carry `vocabulary: VocabularyItem[]` = `{ word: PT, translation: EN }` (`src/content/schema.ts:234`; e.g. `{word:'café',translation:'coffee'}`), stored BOTH bundled client-side (`src/content/packs/*`) AND in the DB content model (`situations.vocabulary` jsonb, migration `00006`; `lessons.vocabulary` jsonb, `00001`). It's a **finite curriculum set** — it does NOT cover arbitrary words or dynamic tutor-generated text.
- **Recommended design — HYBRID, inventory-first:**
  1. **Build a searchable vocab index** from the inventoried `{word, translation}` pairs (bundled content is enough for offline; optionally union with DB content for authored packs). Both directions indexed.
  2. **Bidirectional:** accept either language; match against PT `word` OR EN `translation`; return the other side + example. Relabel the modal field ("Portuguese or English word") and drop the PT-only copy.
  3. **Diacritic-insensitive / fuzzy:** normalize query + keys (lowercase, strip diacritics via `NFD`+combining-mark removal, collapse whitespace); match on normalized equality first, then a bounded Levenshtein/token-overlap threshold (per length) for "close" hits. Cheap because the set is finite + offline.
  4. **AI fallback (misses only):** for words not in the inventory, call Gemini — but update the edge `translate` prompt to **auto-detect input language** and translate the appropriate direction (today it assumes PT input). Online-only; inventory search works offline.
- **Open decisions:** index source (bundled-only vs union with DB content); whether to add a dedicated searchable `vocabulary` view/table for server-side search (vs client-side over bundled content); fuzzy threshold tuning. Ties DF2–DF5 (translation strategy), EN-8 (audio hosting), and the shipped `TranslatableText` primitive.
- **Owner:** Agent E (`feat/*`). Test: unit-test the normalize+fuzzy matcher (diacritics, either-direction, close-but-not-exact) + AI-fallback-on-miss. **Status:** OPEN.

### EN-11 — Voice-practice limit: per-user (primary) + global default, configurable & visible — `DELIVERED via EN-25 — SHIPPED to PRODUCTION in 2026.07.17.1 (2026-07-17)`
- **✅ DELIVERED via EN-25 (2026-07-17):** the per-user `voice_limit` control now lives in the admin **User Access** panel (`data-testid="user-access-voice-limit"`; blank clears to NULL = global default), and the global default lives in the admin **Config** tab. Enforcement already read `profile.voice_limit ?? globalVoiceLimit` (client + `ai-gateway` edge). Shipped in the 2026.07.17.1 release. (The 2026-07-16 DB stopgap that set `voice_limit=999999` for the two test users can now be managed from the UI.)
- **Report (owner):** "the limit should be a per-user limit not just global, but global can also be there."
- **Current state (code):** enforcement already supports the precedence — `useTutorSession.ts:247` `const limit = profile?.voice_limit ?? globalVoiceLimit`. So **per-user overrides global** already at the enforcement layer. What's missing: (a) `profiles.voice_limit` is NULL for everyone and there is **no UI to set a per-user limit**; (b) the only editable control is the **global** limit, hidden behind admin mode (`SettingsView`); (c) users can't see their own effective limit.
- **Direction:** (1) admin UI to set a **per-user** `voice_limit` override (e.g. from the admin console / a user's profile), falling back to the global default when null — the enforcement precedence is already correct; (2) surface the **effective** limit (per-user if set, else global) read-only to each user; (3) clarify labels ("your daily voice limit" vs the admin "global default"); (4) keep `subscription_tier==='unlimited'` and `role==='admin'` bypasses as-is. Ties TB-8 (client hardening) + AUTH/monetization (tiers). 
- **Owner:** Agent E (`feat/*`) + admin console. Test: enforcement precedence (per-user beats global; null falls back), effective-limit display. **Status:** OPEN.
- **↩ Update (2026-07-16):** confirmed still unbuilt — there is **no UI anywhere to set a per-user `voice_limit`** (the only voice control is the GLOBAL default, buried in `SettingsView` behind the admin-mode toggle, with no nav to it). Owner needed two test users unlimited and could not do it from the UI → **stopgap applied via service-role DB update: `profiles.voice_limit=999999` for `liborballaty@gmail.com` (ead858ac) + `testuser@testuser.com` (89c85dcf)** (reversible → set NULL). The proper per-user `voice_limit` control belongs in the consolidated admin surface — folds into **EN-25**. Priority raised.

### EN-12 — Admin log viewer: CLI / in-app admin page / separate admin subapp over `public.logs` — `OPEN (owner-requested 2026-07-14)`
- **Report (owner):** view logs via a CLI interface (not the normal UI) — in admin mode click a link to open a CLI-type, menu-driven (or agent-assisted) interface to examine logs; alternatively a web page in admin mode, or a separate admin subapp accessible via the admin login. "The logs are in the database so it should be possible."
- **Feasibility (thought through):** a browser/PWA **cannot spawn the OS terminal** (sandbox) — so "click a link → opens macOS Terminal" isn't directly possible from the web app. Realistic forms, best-first:
  - **(a) In-app admin log viewer (web, admin-mode)** — a page/subapp querying `public.logs` with filters (level, category, `correlation_id`/`session_id`/`request_id`/`trace_id`, time range, user), full-text search on message/details, auto-refresh/tail, and **drill-down by `correlation_id`** (pivot to every event in one request). No extra infra; ships on-device. Most feasible.
  - **(b) Repo CLI (`scripts/logs-cli.mjs`)** — a Node CLI (menu-driven or query-args) that connects to the DB and prints/queries logs, for use in a real terminal by an admin or an agent. Matches the "CLI in a terminal" intent; pair with an **agent skill** for natural-language log triage ("show me errors for correlation X").
  - **(c) Separate admin subapp** — a distinct admin route/build gated by admin login hosting (a)/(b).
- **Recommendation:** ship (a) first (in-app admin viewer), add (b) the repo CLI + optional agent skill for terminal/AI-assisted analysis; document the CLI command in place of the (infeasible) "open Terminal from a link." 
- **Access/RLS:** `public.logs` must be **admin-readable** (verify/add an RLS policy) and the viewer must use an admin-authenticated path; the CLI uses a service-role/DB path (same DB-access design point as **SW-4** agent-ticket skill). Ties the observability contract (correlation IDs are the pivot key).
- **Owner:** Agent E (in-app viewer) + tooling (CLI/skill) + ops (RLS). **Status:** OPEN.

## Security backlog (owner-requested 2026-07-14)

### SEC-1 — Rate limiting on edge functions (DoS / abuse protection) — `OPEN (HIGH — security, owner-requested 2026-07-14)`
- **Report (owner):** "there should also be some kind of rate limiting config as well to avoid DoS attacks."
- **Why this is distinct from the voice limit:** the daily voice limit (TB-8/EN-11) is a **fair-use quota** enforced **client-side** (`useTutorSession`) against `profiles.voice_usage_today` — it is NOT abuse protection (a malicious client bypasses client checks and can hammer the edge functions directly). Need **server-side rate limiting** on the edge functions (`gemini` tts/chat/translate/generate-lesson, `log-sink`, `delete-account`) to cap request rate per IP and per authenticated user.
- **Current state:** `log-sink` already has a **per-IP throttle** (pattern to reuse); the `gemini` edge fn enforces the daily voice count server-side but has no general rate limit. No centralized limiter.
- **Direction:** a shared `_shared/rateLimit.ts` (token-bucket / fixed-window) keyed by IP + user_id, with per-action configurable limits sourced from `global_settings` (so limits are tunable without redeploy); return `429` + a structured `RATE_LIMITED` error via the canonical `errorResponse`/observability path (correlation IDs). Consider Supabase/Postgres-backed counters or an edge KV. Config lives in `global_settings` (e.g. `rate_limit_*`). Ties the observability contract (structured errors) + AUTH-1 (bot/human verification reduces abuse surface).
- **Owner:** Agent E / ops (security). Before any wide/public beta. Test: e2e/integration that N rapid calls get `429` with the structured code. **Status:** OPEN (HIGH — security).

### EN-16 — Vocabulary review: settable content scope + word count (fixes "few words / looks general") — `APPROVED (owner 2026-07-15) — IN PROGRESS (Lane B)`
- **Report (owner):** vocab practice "seems limited to a very few words" and the grade buttons "don't do what they're supposed to." Root cause (diagnosed): the deck is scoped to ONE situation when entered from the situation browser (`VocabularyView.tsx`), so it shows only that situation's authored `vocabulary`; the screen doesn't say what scope it is, so it "looks general." (Grade-button perception is separate — see note.) Words are AUTHORED content: `situation.vocabulary` (`content/schema.ts:106`), stored in `public.situations.payload` JSONB (packs, migration 00006), delivered by `contentRepository`.
- **Approved design (owner, via decision 2026-07-15):**
  - The vocab review screen **shows its scope** (which lesson/situation, or all) — not general-looking. (Read-only label already built as the foundation.)
  - Scope is **settable in that same screen** via a selector: **This lesson / This track / All lessons**, each showing a **word count** (so a small deck is a visible choice, never a surprise).
  - **Session scales to the chosen scope** (remove the fixed 20/session cap for scope-driven sessions — play all due+new in scope). Header shows `<scope> · N words · M ready now` so pool-size vs session-size is explicit.
  - "This track" = the user's active goal-track (`user_track_selection`); if none picked, that option is empty/disabled and falls back to All.
- **Grade-button note (separate, tracked):** SM-2 math (`srs.ts:90`) is correct + persisted, but for new/early cards the interval is fixed (rep1→1d, rep2→6d) regardless of Hard/Good/Easy, and only "Again" has in-session effect — so the buttons feel interchangeable. Options (future EN): show the scheduled interval per grade ("+1d / +6d / +2wk") and/or Anki-style learning steps + Easy bonus. Owner to decide separately.
- **Owner:** Lane B (build). **Status:** SUPERSEDED by EN-18 (2026-07-17). The `This lesson / This track / All lessons` selector was replaced by EN-18's theme/category **focus picker** (hub) + situation-scoped entry (lesson); `buildSessionCards` scope-scaling is retained (still unit-covered in `buildSessionCards.test.ts`). The EN-16 e2e (`57`) was rewritten to assert the focus picker.

### EN-18 — Vocabulary practice → objective reinforcement quiz (comprehension + production, progress-aware, thematic) — `ON DEVELOP + E2E GREEN (2026-07-17) — all WP1–WP7, full gate incl. e2e run; only manual mic verify owed`
- **Owner critique:** self-graded flashcards make the learner "judge and jury"; the app has no objective right/wrong signal (no input field, no listening mode). It should observe performance and reinforce: wrong → returns sooner, right → later.
- **Approved design (owner 2026-07-15), full spec:** `docs/EN-18-VOCAB-REINFORCEMENT-QUIZ-REQUIREMENTS.md`. Loop: source from themes the learner has WORKED ON (started + played some) → PT word + play → **type the meaning** (comprehension, checked by EN-10 `vocabSearch`) → **say it** (production, pt-PT speech recog) → SUCCESS/PARTIAL/FAILURE (no-mic = typed PASS/FAIL, no partial) → SM-2 schedules. Self-grade buttons replaced by objective grading. Focus is **thematic** (situations grouped by category `daily/social/travel/work`), never lesson numbers; the pool grows with progress.
- **Reuse:** TTS (pt-PT), EN-10 vocabSearch (typed check), first-win pt-PT speech recognition (spoken check), SM-2 (`srs.ts`/`useDueItems`), `user_situation_progress`+`mastery_items` (sourcing), situations/categories (themes).
- **★ OWNER-APPROVED TO BUILD + BUILD STARTED (2026-07-17):** owner said "let's implement the typed-answer + the spec and logic." Decisions: (1) build **all-in-one** — typed comprehension AND spoken production together (not phased); (2) word pool = **started situations** (any `user_situation_progress` row `in_progress`/`completed`), no new tracking write. Executable plan: `~/.claude/plans/velvet-napping-pike.md` (7 work packages, `/plan`-approved).
  - **DONE + pushed (commit `8ef0305`, origin/develop):** **WP2** `src/features/practice/vocabulary/comprehension.ts` — accent/case/typo-tolerant typed-meaning grader (reuses EN-10 `normalizeForSearch`+`boundedLevenshtein`, accepts translation alternates). **WP4** `src/features/practice/vocabulary/scoring.ts` — objective SUCCESS/PARTIAL/FAILURE → per-dimension SM-2 grades (comprehension→`retrieve`, production→`say`; no-mic → comprehension-only). 14 unit tests, tsc clean.
  - **DONE on `feat/en18` (2026-07-17):** **WP1** `sourcing.ts` — progress-aware pool (`buildVocabPool`: started situations minus 0-word stress tests, grouped by category; `loadStartedSituationIds`), 15 tests. **WP3** `production.ts` — pt-PT mic grader (`matchesSpokenWord` fuzzy + `checkProduction` → PASS/FAIL/SKIPPED), 12 tests; `config.vocabulary` added. **WP5** `useVocabularySession.ts` + `VocabularyView.tsx` — quiz state machine (prompt→reveal→listening→feedback), typed meaning + say-it + objective feedback + return-timing; **situation entry scopes to that lesson, hub entry uses the progress-aware pool + theme focus picker (default "all started"), signed-out falls back to the full pool** so the mode never dies. **WP6** rewrote e2e `user/15` for the typed-answer flow + `dedupeByWord` unit. **WP7** reconciled the whole e2e suite (30/40/44/57) + `control-inventory.json` to the new UI. Commits `66b5978`, `e85163f`, `521c9fb`, `372f5d6`, `9027fd0`. No DB migration.
  - **Gate (2026-07-17, run against dev Supabase):** tsc clean · **383 unit tests pass** (merged tree) · prod build OK · coverage checker OK (202 controls). **E2E RUN — full chromium regression 127/127 green.** EN-18-affected specs all pass: `07` mode-routing, `15` quiz flow, `30` offline mastery queue (retrieve grade 4), `44` play-word, `57` focus picker. Fixed 3 test-only bugs found by the run: type the first translation alternate (`15`/`30`; `Boa noite`→`Good evening/night` was failing comprehension + re-enqueuing forever); `07` asserts the scope line not a due/new counter (fresh user → empty state); and `createThrowawayUserContext` origin now honours `BASE_URL` (was hardcoded `:4173`, broke `09-account-deletion` on a non-default port — not an EN-18 issue). **OWED before release:** **manual mic verify** on staging (Step 2 spoken production is unit-mocked only — hard to e2e).
  - **Build-time resolutions to note:** (a) `no-speech`/`timeout` on the mic step lets the learner retry then **Skip speaking → not graded (productionPass=null)** rather than the spec's "then FAIL" — a silent/hardware miss shouldn't reset the `say` track; a genuine wrong word (mismatch) → **Move on → FAIL**. (b) A prod-safe test-only affordance exposes the expected meaning as `data-answer` on the input **only** when `localStorage['fm:e2e']==='1'` (never set in the shipped app), so the typed flow is deterministic.
- **TB-19 (flip-button workaround): MOOT** — the flip card is gone; the objective quiz has no flip. Retired in WP6/WP7.
- **Owner:** Agent E (`feat/en18` → on `develop`). **Status:** ON DEVELOP, full gate incl. e2e green. Remaining before prod: manual mic verify on staging (operator-gated, staging-first). EN-16 scope selector folded into WP7's theme focus picker (spec `57` rewritten).

### EN-13 — Tutor: tap / select-to-translate for Portuguese text — `NEEDS REQUIREMENTS (then owner approval before any coding)`
- **Report (owner 2026-07-14):** tutor replies are sometimes mixed PT/English (fine) but sometimes **all Portuguese with no translation**. Want to translate by **tapping/clicking a word or section, or highlighting a selection** in the tutor window.
- **Existing building block:** the `TranslatableText` primitive (tap-to-reveal translation, commit `1d16e6f`) already exists — the tutor window likely renders raw markdown without it. Direction: route tutor message text through a translate-on-tap mechanism (word/section) + support selection-to-translate, using the existing `translate` edge action. → Needs a written spec (granularity: word vs sentence vs highlighted selection; inline-reveal vs popover; caching; markdown handling) + owner approval before coding.
- **Owner:** Agent E. **Status:** NEEDS REQUIREMENTS.

### EN-14 — Lesson-start background prefetch of all lesson audio (in order, non-blocking, survives leaving) — `NEEDS REQUIREMENTS (then owner approval before any coding)`
- **Report (owner 2026-07-14):** on starting a lesson, if the audio clips aren't cached yet, download the **whole lesson's audio in the background, in lesson order, non-blocking** (user keeps interacting with the UI/lesson), and the download should **complete even if the user leaves the lesson**.
- **Ties:** EN-7 (modular/resilient chunked downloads — HIGH), EN-8 (server-hosted audio → what gets fetched), TB-9 (persistence). Direction: on lesson open, enqueue the lesson's clips (ordered) into a background prefetch queue that is non-blocking, resumable, and **persists past navigation away** from the lesson; respects the cache LRU + persistence availability (TB-9); coordinates with EN-8's server cache (fetch static file, else generate). → Needs a written spec (queue lifecycle + ordering + concurrency; "complete even if user leaves" semantics re: service worker / in-memory queue; cancellation; interaction with EN-7/EN-8) + owner approval before coding.
- **Owner:** Agent E. **Status:** NEEDS REQUIREMENTS.

## Auth & security backlog (owner-requested 2026-07-14)

### AUTH-1 — Stronger sign-in: MFA + magic link + human (bot) verification — `OPEN (backlog)`
- **Report (owner):** want MFA, magic-link sign-in, and a way to verify a human (not a machine) is signing up/in. Owner has **mail services on the Verpex server** (where the site is hosted) that "just need to get set up." Backlog for now; assemble suggestions.
- **Current state (verified in code, `src/features/auth/AuthScreen.tsx`):**
  - Password sign-in (`signInWithPassword`), signup with email-confirmation (`signUp`, `emailRedirectTo`), and a `skipVerification` fast-path. ✓
  - **Magic link ALREADY IMPLEMENTED** (`handleMagicLink` → `signInWithOtp`) and password-reset email-OTP (`resetPasswordForEmail` → `verifyOtp type:'recovery'`). ✓ — so magic link is not net-new; it is almost certainly **blocked on email delivery**: Supabase's built-in SMTP is heavily rate-limited (~a few/hour) and not production-grade, so links/OTPs are unreliable or land in spam.
  - **No MFA** (no `auth.mfa.enroll`). **No CAPTCHA / bot gate.** — both net-new.
- **Prerequisite (do first): custom SMTP = the Verpex mail server.** Configure it in Supabase → Auth → SMTP. Unblocks magic link, email OTP, signup confirmation, password reset at real volume. **Deliverability is the actual work:** set SPF + DKIM + DMARC for the sending domain or Gmail/Outlook will spam-folder or reject. Use a dedicated `no-reply@…` mailbox. This is a config/ops task (Supabase dashboard + Verpex DNS), not app code.
- **Suggestions (layered, defense-in-depth; all Supabase-native):**
  1. **Human verification → Cloudflare Turnstile** (free, privacy-friendly, often invisible) or hCaptcha — **both natively supported by Supabase Auth**. Enable in Auth settings + add the client widget on signup/sign-in/magic-link; Supabase verifies the token server-side. Recommend **Turnstile**. Lowest-friction bot gate; strongest single win against automated signup abuse. Email double-opt-in (already present) is a weak secondary filter.
  2. **Magic link / email OTP** — already coded; just needs SMTP (step above). Reduces password reuse / credential-stuffing. **PWA caveat:** a magic link opens in the default browser and may not return to the *installed* PWA/iOS Capacitor context — needs redirect/deep-link handling and testing on installed app + iOS (universal links). Prefer **6-digit email OTP** over click-link for installed apps to sidestep the deep-link problem.
  3. **MFA → TOTP (authenticator app)** via `auth.mfa.enroll({ factorType: 'totp' })` + challenge/verify. **Free, strongest, no SMS cost, no SMTP dependency.** Offer optional enrollment in Settings; generate recovery codes; define an admin reset path. (SMS/phone MFA needs a paid provider — Twilio/MessageBird — skip for now.) Email-OTP step-up is a lighter alternative for sensitive actions once SMTP is live.
- **Sequencing:** (0) Verpex SMTP + DNS auth records → (1) Turnstile bot gate → (2) magic-link/email-OTP usable → (3) optional TOTP MFA. Each independently shippable.
- **Cross-refs / considerations:** CAPTCHA vendor + email processor are new sub-processors → update GDPR/privacy disclosures (ties EN-4 legal/`ALL-COMP-1`). Turnstile is more privacy-friendly than hCaptcha for EU. Per §3 methodology each stage ships with tests (e2e: bot-gate present on auth; magic-link/OTP happy path against a test inbox; MFA enroll+challenge).
- **Owner:** Agent E (`feat/*`) + ops (SMTP/DNS). Priority: backlog. **Status:** OPEN.

### EN-15 — Content access: honor `unlimited`/`admin` bypass so an admin can grant a user ALL training levels — `BUILT (on develop, commit 10accaf, 2026-07-15) — bypass + admin control + live RLS repair`
- **Report (owner, 2026-07-15):** "if I want a user to have access to all app training levels, how do I do that as an admin? Is it possible or missing?"
- **Findings (code-read 2026-07-15):** content access gates purely on `profiles.unlocked_level`, advanced **+1 per shared access-key entry** (`useLessons.ts:203 handleUnlockLevel`, key in `global_settings.level_unlock_key`; new profiles start at 1). `subscription_tier` (`'free'|'premium'|'unlimited'`, `types.ts:62`) exists and bypasses ONLY the **voice limit** (`useTutorSession.ts:249`), NOT content. **No admin UI grants access.** RLS DOES already let an admin UPDATE any profile row (`00001_initial_schema.sql:121`), so today's only grant path is a manual `UPDATE profiles SET unlocked_level = 8 WHERE email=…` (8 = top of the `getLevelName` 1–8 scale). Two gaps: (1) no admin grant/set-level UI; (2) `subscription_tier` is half-built (defined + voice-only).
- **★ OWNER DECISION — Option B:** make content gating honor `role==='admin' || subscription_tier==='unlimited'` as a full-access bypass (mirroring the voice-limit rule). Then "grant all levels" = set the user's `subscription_tier='unlimited'` (admins get it automatically). `unlocked_level` stays the paywall progression for free/premium. Requirements: `docs/EN-15-CONTENT-ACCESS-BYPASS-REQUIREMENTS.md`.
- **Scope (per the requirements doc):** (1) new `src/lib/access.ts` (`hasFullContentAccess` + `effectiveUnlockedLevel`) + route content-gate reads (HomeView/useLessons/LearningView) through it + hide the unlock CTA for full-access; (2) **BUNDLED admin control (owner decided 2026-07-15):** an `AdminView` panel to look up a user by email and set their `subscription_tier` (→ `unlimited`) and/or `unlocked_level` — RLS already permits admin profile writes (no service-role), with audit logging + confirm. No schema change. Do NOT touch `useTutorSession.ts` (Lane B EN-8) — the voice-check unification is a deferred follow-up.
- **Coordination:** HomeView/useLessons/LearningView + new access lib are conflict-free vs Lane B. Interacts with TB-1 (both touch HomeView — TB-1 moves the label to proficiency, EN-15 the gate) — sequence together. Immediate grant available now via the DB `UPDATE` above.
- **★ BUILT (Lane A, owner go 2026-07-15, commit `10accaf`):** `src/lib/access.ts` (`hasFullContentAccess` = admin||unlimited; `canAccessLevel` bypass short-circuit, no MAX_LEVEL constant) + 12 unit tests. HomeView hides the access-key unlock CTA+modal for full-access users (progress pointers untouched). Thin admin **User Access** panel (`useUserAccess.ts` + `UserAccessPanel.tsx` + AdminView tab): look up a user by email → set `subscription_tier`/`unlocked_level` via RLS-authorized admin write, correlation-ID audit logging + confirm + toast. e2e `tests/e2e/admin/09-admin-user-access-grant.spec.ts`. Gate: unit 12/12, full suite 216/216, eslint/tsc clean, e2e passes.
- **⚠ LIVE RLS REPAIR (owner-approved 2026-07-15) — migration `00013_profiles_admin_select_update.sql`, APPLIED LIVE + independently verified:** the live `profiles` SELECT/UPDATE policies had NO admin bypass (only `auth.uid()=id`) — real drift from what `00001` documented — so the admin control silently affected 0 rows. Migration recreates both policies as `auth.uid()=id OR public.is_admin()` (same helper/pattern as the live DELETE policy + 00009). Renumbered 00012→**00013** to avoid EN-8's reserved 00012. **See SEC-2 — this widens profiles access (admins can now read/write any profile), relevant to SEC-2's profiles-isolation audit.**
- **Deferred:** the finding audit showed `useLessons`/`LearningView` `unlocked_level` reads are progress pointers, not access gates (only the Home unlock flow is a gate) — so they were not changed. R5 voice-limit unification (`useTutorSession.ts`) still deferred (Lane B EN-8 file). TB-1 also touches HomeView (label→proficiency) — sequence together.
- **Owner:** Agent E (`feat/*`). **Status:** BUILT on develop; owner to verify on the next staging cut. Immediate grant also available via the admin panel or a DB `UPDATE`.

### EN-17 — User manual + in-app chat "help mode" are stale and have NO single source of truth — `(b) DONE (content refreshed on develop) · (a) QUEUED — DESIGN DISCUSSION required before coding`
- **⚠ RENUMBERED EN-16 → EN-17 (2026-07-15):** collided with Lane B's EN-16 (vocabulary review, approved + in progress). The commits labeled "EN-16b" (`a6a3d68`, `7189611`) and the in-file "see EN-16" cross-refs (`UserManualModal.tsx` header, `_shared/gemini.ts` help prompt) refer to THIS item (EN-17) — minor internal-ref cleanup deferred; not user-visible.
- **Report (owner, 2026-07-15):** are the manual and what the chat can tell the user about the app updated for all the new functions/changes?
- **Findings (code-read 2026-07-15) — NO on both, and they're disconnected:**
  - **User manual** (`src/features/settings/UserManualModal.tsx`, 97 lines, last touched an early build checkpoint) is a philosophy/overview doc (Learning Philosophy / Daily Ritual / AI Practice / Voice Limits). Does NOT cover: the 4 learning paths + goal-track chooser (TB-11/11b), sidebar sign-out (EN-9), bidirectional/fuzzy vocab lookup (EN-10), the situation simulator (TB-12), granular offline downloads (EN-7), tutor read-aloud opt-in (TB-5), admin User Access (EN-15).
  - **Chat help mode EXISTS** (`getSystemInstruction(tutor, isHelpMode, learner)` → "FalaMadeira App Guide", `supabase/functions/_shared/gemini.ts:90`) but is a **hardcoded, static 4-line app-structure list** that is stale AND now factually wrong ("Users can unlock months 1-3" — the scale is 1–8 and EN-15 made access tier-based). Knows nothing of paths/chooser/vocab/simulator/offline/sign-out location/read-aloud.
  - **STRUCTURAL:** the manual (React JSX) and the chat help (edge-fn string literal) are TWO separate hand-maintained sources — no single source of truth, so updating one never updates the other, and neither is tied to feature changes. This is why new functions silently miss both.
- **Recommended fix:** one canonical "app features / how-to" content source that BOTH the manual UI renders AND is injected into the chat help-mode system prompt; wire into the `/update-docs` doc-sync discipline + a CI drift check. Interim: refresh both surfaces + fix the "months 1-3" error.
- **Process gap:** "test every change + update docs" has been updating trackers/requirements docs but NOT the user-facing manual or chat help — extend the discipline to include them on any user-facing feature change.
- **✅ (b) INTERIM CONTENT REFRESH — DONE (Lane A, owner go 2026-07-15, commit `a6a3d68`):** both surfaces refreshed to current features. Manual (`UserManualModal.tsx`) gained sections for learning paths + goal chooser, practice modes incl. the Situation Simulator, vocab lookup, offline downloads, navigation (sidebar sign-out), and in-app help; corrected AI-practice (read-aloud opt-in) + voice-limit copy. Chat help prompt (`_shared/gemini.ts` isHelpMode) rewritten to current tabs/features + access/limits, fixing the "unlock months 1-3" error. 0 NUL, lint/tsc clean. **DEPLOY NOTE:** the manual is client-side (ships next web deploy); the help-prompt change is in the gemini EDGE fn and reaches users only on the next gemini deploy — NOT deployed by Lane A; coordinate with Lane B (active on the gemini fn for EN-8/TB-13). Pre-existing minor bug noted: the manual's older sections render literal `**` (markdown not parsed) — new copy avoids it; full fix deferred.
- **⏸ (a) SINGLE-SOURCE CONTENT SYNC — WANTED (owner confirmed 2026-07-15); design NOT yet defined:** one canonical "app features / how-to" *content* source that BOTH the manual UI renders AND the chat help prompt is built from, so the two never drift again + a doc-sync/CI drift guard. **Scope = CONTENT only** (descriptions of what features do). Do NOT start coding — design still open. Open questions: format/location (structured TS in `src/content` vs MD vs shared module); how the edge help prompt ingests it without bloating the system prompt or a per-request fetch; the CI drift check (fits the existing `check-*-drift.mjs` family); it touches `_shared/gemini.ts` (Lane B EN-8/TB-13 edge file) → coordinate; and it should be provider-neutral, not coupled to `gemini.ts` (see EN-19). **Shares a substrate** (a per-feature "capability" record) with EN-18 guidance — design the data model once, but the two remain separate features (EN-17a = describe; EN-18 = navigate/surface).
- **✅ (a) BUILT (Lane A, owner go 2026-07-16, merged to develop `f06cfd3`):** the single App Capability Registry (`src/content/appCapabilities.ts`, 15 capabilities) now drives BOTH the manual (`UserManualModal` renders it; literal-`**` bug fixed) AND the chat help prompt (`_shared/gemini.ts` consumes `appHelp.generated.ts` from `scripts/gen-app-help.mjs`). Drift guard `scripts/check-help-drift.mjs` in preflight. Built autonomously via `plans/plan-2026-07-15-en17-help-registry.yaml` (isolated worktree; 270 unit tests + e2e green; full gate clean on the merge). **Chat half reaches users only after the next gemini edge deploy** (in the staging cut below). Design: `docs/EN-17-EN-18-HELP-GUIDANCE-DESIGN.md`.
- **✅ STAGED in `2026.07.16.1` (2026-07-16):** cut `develop→main` (Option 2, Lane A took the release lane, Lane B stood down), full preflight green, deployed to staging `testfalamadeira.searchingfool.com` (HTTP 200), gemini edge fn deployed (chat-help artifact live on the shared backend — carried Lane B's committed TB-13 too), `main` pushed. Awaiting owner staging-verify (manual + help "Take me there" + hints) then `npm run deploy:approve` to promote to prod.
- **Owner:** Agent E (`feat/*` + content). **Status:** (a)+(b) STAGED (`2026.07.16.1`); owner to verify on staging + approve to prod. See also EN-18, EN-19.

### EN-18 — In-app guidance: take-me-there navigation (reactive) + contextual surfacing (proactive) — `STAGED (Lane A, 2026.07.16.1, 2026-07-16) — awaiting owner staging-verify`
- **Intent (owner):** where possible, the app should help the user *do* the thing rather than just describe it. Two facets of one capability, tracked together (split later if the work diverges):
  - **Reactive ("take me there"):** when a user ASKS (help chat, or a manual entry), the answer navigates them to the exact control (deep-link) instead of reciting a path. Generalizes the TB-11b mechanism (Home → goal chooser via `focusGoalChooser` scroll+highlight) into a reusable "focus/goto <control>" the help layer can trigger.
  - **Proactive (contextual surfacing):** the app surfaces the right control at the right moment WITHOUT being asked — empty states, first-use hints, the Home recommended-next-step (already exists), TB-11b's auto deep-link. Difference from reactive = the *trigger* (app-initiated vs user-asked); same underlying "where is this control" substrate.
- **Shared substrate with EN-17(a):** a per-feature record { area, title, description, **navigation target** (route + control/`data-testid`, e.g. `goal-track-chooser`/`path-switcher`) }. EN-17a uses the description; EN-18 uses the navigation target. Design the record once; keep the features separate.
- **Principle (owner-aligned):** guide-and-offer / one-tap "take me there" — the user stays in control. NOT silent automation.
- **Explicitly OUT OF SCOPE:** the chat *performing* actions for the user ("do it for me" / tool-use) — that was proposed by the assistant and is **not intended** (owner 2026-07-15). If ever revisited it needs its own guardrail design.
- **✅ BUILT (Lane A, 2026-07-16, develop `f06cfd3`):** `src/lib/focusControl.ts` (generalized focus/scroll/highlight), `src/features/guidance/{navigateToCapability,matchCapabilities,ContextualHint}` — reactive "Take me there" chips in the tutor/help modal (`TutorPracticeModal`) + proactive contextual hints (e.g. Practice hub → roadmap), all driven by the registry's `target` (data-testids added to App/Sidebar/LearningView/PracticeHubView). e2e covers navigate + hint flows. Chat-acts (#4) remains out of scope.
- **Owner:** Agent E (`feat/*`). **Status:** STAGED (`2026.07.16.1`); owner to verify "Take me there" + hints on staging.

### EN-19 — LLM/text is Gemini-only (not provider-abstracted like TTS) — `OPEN (backlog — separate architectural gap, owner-flagged 2026-07-15)`
- **Observation (owner):** TTS is provider-agnostic (routes via `_shared/tts/router.ts`, honors per-user `tts_provider` + BYO key), but the **LLM/text side is hardcoded to Gemini** — chat/tutor, help mode, `translate`, `scenario-generator`, `error-analyst` all go through `_shared/gemini.ts` (a direct Gemini REST wrapper) inside the `gemini/` edge fn. No LLM-provider router, no per-user LLM provider.
- **Why it matters:** provider choice/BYO/resilience for text (parity with TTS); and any future action/assistant tooling should be built provider-neutral rather than wired into `gemini.ts`. Also informs EN-17(a): keep the help content out of `gemini.ts`.
- **Scope (not yet designed):** an LLM router mirroring the TTS one (chain + per-user preference + BYO), with the edge fn renamed/kept as a thin entry. Larger change; edge-function territory (coordinate with Lane B). Backlog until prioritized.
- **Owner:** Agent E / backend. **Status:** OPEN (backlog); separate from the EN-17/EN-18 help theme.

### EN-20 — Persistent, discoverable Help/assistant entry in the navigation — `BUILT (Lane A, develop 28f7c83, 2026-07-16) — desktop sidebar; mobile parity via EN-21`
- **Report (owner):** "how do I open the chat? it should really be directly accessible from the side navigation all the time."
- **Findings (code-read 2026-07-16):** the app HAS a chat tab, but two discoverability problems: (1) the tutor chat nav item is labeled **"Tutor"** (`NAV_ITEMS`, `App.tsx:72`, MessageCircle) — always in the sidebar + bottom nav, but doesn't read as "chat"; (2) the **"App Guide" help chat** (isHelpMode) is **buried** — it's only a *toggle inside a tutor practice session* (`TutorPracticeModal`, reached via `startAIPractice`), with NO direct/always-available entry. So the help assistant the owner wants is effectively hidden.
- **Proposed scope (needs approval before coding):** (a) a persistent, clearly-labeled **Help** affordance always available (sidebar entry and/or header button) that opens the chat directly in **help mode**; (b) optionally clarify the "Tutor" tab (label/subtitle) so it reads as chat, and expose help mode from `TutorChatView` itself. Extends EN-18 (in-app guidance/discoverability); reuses the existing isHelpMode chat + the EN-17 registry-backed help content. Small client change; no edge/deploy beyond what EN-17 already shipped.
- **✅ BUILT (Lane A, 2026-07-16, develop `28f7c83`):** `openHelp()` in `useTutorSession` (reuses `START_PRACTICE` + help greeting + the existing help-mode modal & EN-18 chips — no lesson, no in-modal toggle) + a persistent **Help** sidebar entry mirroring the Admin nav-action pattern (`Sidebar.helpItem`/`onOpenHelp`, `App.tsx` `HELP_NAV_ITEM`). e2e `user/58`; tsc/eslint clean, 270 unit pass. Reuse-first (no duplicate chat UI). Not yet in a release cut.
- **Deferred to EN-21:** mobile parity (the sidebar is desktop-only, matching the Admin precedent) — the EN-21 intent opener surfaces help everywhere; and the "route to help mode" intent lives in EN-21.
- **Owner:** Agent E (`feat/*`). **Status:** BUILT on develop; folds into the next staging cut. EN-21 design next.

### EN-21 — Chat entry: intent picker + mode routing (fix default translation-drop + help-not-interpreted) — `OPEN (owner-requested 2026-07-16; investigated)`
- **Report (owner):** opening the Tutor "no matter what I do goes directly into a Portuguese↔English translated session"; the take-me-there chips only work "after clicking the top question button"; "it is not at all interpreting anything I say as a question about the manual." Wants a "what do you want to do?" opener with options that route to the right workflow.
- **Root causes (thorough trace 2026-07-16):**
  1. **Default = translation drill by accident.** Free chat uses the base tutor system prompt which contains a "FORMATTING & TRANSLATION RULE" (`_shared/gemini.ts:132-150`); with NO lesson context and NO intent, every message gets the Português/Pronunciation/English block. Not a chosen mode — an emergent default. (Persisted transcripts also skip the welcome CTAs.)
  2. **Help mode unreachable + no intent detection.** `isHelpMode` is only togglable via the orange HelpCircle button INSIDE the AI-Practice modal (`TutorPracticeModal.tsx:103`, reached only via `startAIPractice`); the Tutor-tab free chat is hardwired `isHelpMode=false` with ZERO intent detection (`useTutorSession.ts:414` sends every message to the same tutor prompt). So manual/how-to questions are never interpreted as help.
  3. **The "top question button" = the orange help button.** Chips render only when help mode ON + answer matches a capability (`TutorPracticeModal.tsx:172`, `matchCapabilities`) — hence "click it first."
  4. **5 hidden modes, fragmented entry:** free chat, lesson-anchored practice, help/App-Guide, translation-by-default, situation simulator (Practice-tab only). No way to declare intent.
- **Proposed design (needs approval before coding):** an **intent opener** on chat open (and a "new chat" affordance) — "What would you like to do?" → routes: **Ask about the app** (`isHelpMode=true` + EN-17 registry answers + EN-18 chips) · **Have a conversation** (conversation mode, level-aware, NOT the translation block) · **Translate a word/phrase** (explicit translate mode / EN-10 vocab lookup for single words) · **Practice today's lesson** (existing lesson practice) · **Role-play a situation** (Situation Simulator). Each choice sets the correct session mode/prompt so input is interpreted per intent. Fixes all three reported issues; subsumes EN-20's "route to help" and reuses EN-10/EN-17/EN-18.
- **Design DRAFTED (Lane A, 2026-07-16):** `docs/EN-21-CHAT-INTENT-ROUTER-DESIGN.md` — intent registry + a prompt-mode split (`getSystemInstruction(mode)`: help / conversation / translate / lesson) that moves the translation-formatting rule OUT of the default so "conversation" stops being a translation drill (P1) and help is a first-class routed mode (P2). Reuse-first: routes to EN-20 `openHelp`, `startAIPractice`, the simulator, EN-10 vocab, EN-17 help content, EN-18 chips; shared prompt blocks (PT-PT, correction) reused across modes. Staged 1 (split+plumbing) → 2 (picker UI + mobile help parity) → 3 (translate mode). Edge split coordinates with Lane B.
- **Owner:** Agent E (`feat/*`) + edge (`_shared/gemini.ts` — coordinate with Lane B; EN-19 provider-neutral). **Status:** design DRAFTED → awaiting owner approval + the 4 open decisions in the doc before coding.

### EN-22 — AudioButton feedback is negligible: no visible loading, and NO "playing" state (app-wide) (reporter: owner, 2026-07-16) — `OPEN (root cause found)`
- **Report (owner):** audio buttons barely show when audio is loading or active; thought EN-1 improved this but the feedback is negligible — applies to ALL audio buttons, not just chat.
- **Root cause (code-read 2026-07-16, `src/components/AudioButton.tsx`):** the button only has a `busy` flag that lasts until `onPlay()` resolves, and per its own contract `onPlay` resolves when playback **STARTS**, not ends (`geminiService.playSpeech` resolves on start). So: (1) there is **NO "playing" state** — the button flips spinner→idle the instant audio begins and shows nothing for the actual playback; (2) the **loading** cue is just an icon swap (`Loader2` spin ↔ `Volume2`) + faint `opacity-70`, and for cached audio it barely flashes. EN-1 only delivered a brief *loading* hint; *playing* + strong visuals were never built.
- **Fix (design, app-wide via the shared component):** make `AudioButton` a **3-state** control — idle / loading / **playing** — with visually distinct states (spinner while loading; an animated *playing* indicator — pulsing / equalizer / stop-icon + coloured ring/bg — until playback **ends**; idle otherwise). Requires a playback-**end** signal (small `geminiService.playSpeech` contract addition: `onEnd`/resolve-on-end). Consolidate the chat's existing message-level "speaking" ring (`currentlySpeakingIndex`, `useTutorSession`) into the shared button so every surface behaves identically. Reuse-first; client-only.
- **Owner:** Agent E (`feat/*`). **Status:** OPEN — EN-1 follow-up; app-wide (all audio buttons). Complements TB-14 (per-phrase play uses this same button).

### EN-23 — Admin audio-management panel: inventory + preview + quality review + enqueue-for-regeneration (reporter: owner, 2026-07-16) — `APPROVED (2026-07-17) + BUILT on feat/en23 + MIGRATION 00014 APPLIED & VERIFIED LIVE — remaining: live Playwright e2e run + merge to develop`
- **Report (owner):** "a way in admin to review the clips quality, add that to the queue … we should probably have that sooner than later." Supersedes/absorbs the earlier "what-is-where audio panel" EN-8 follow-up.
- **Owner decisions (2026-07-16):** D1 scope = **review + act** (inventory/coverage + play/preview + quality verdict + enqueue-for-regeneration; delete/replace deferred). D2 regen path = **enqueue for CLI/batch** (panel flags → existing `pregen-audio.mjs` consumes, operator-run staging-first; NO new runtime write-to-prod — respects the gated `TTS_BUFFER_WRITEBACK`/multi-env buffer-contention design). D3 quality = **manual verdict + automated signals** (byte size, content-type, duration, silent/too-small flag, tier presence).
- **MVP ("minimum to start", owner-asked 2026-07-16):** new **Audio** tab in the existing `AdminView` (role==='admin' gate, no new route) → scope selector (Level/Track/Situation, default Level 0) → per-clip coverage + automated signals list → inline preview → good/bad/re-record verdict (persisted) → enqueue bad clips for regeneration. Deferred: bulk actions, in-panel delete/replace-in-storage, waveform silence scoring, version/diff, generation-status tail (ties EN-12).
- **New stores needed (none exist today):** `public.tts_audio_review` (verdicts + signals) + `public.tts_audio_regen_queue` (CLI-consumed). Admin-only RLS (`public.is_admin()`, EN-15 pattern); new migration via `node apply-migrations.js` — coordinate numbering with DB/EN-8 agent. (JSON-artifact MVP is the fallback if owner prefers zero schema churn — open item.)
- **Reuse:** `AdminView.tsx`/`App.tsx` admin gate; `audioCache.buildKey`+`synthesizeCached` tiers; `linesForSituation()` (already enumerates dialogue+phrases+vocab+**roleplay NPC nodes** — COORD-1 gap handled) + `contentRepository.listSituations`; `audit-audio.mjs`/`pregen-audio.mjs` (extend with `--from-queue`); `logger`/`userMessage` + `public.logs`.
- **Dependencies:** **BLOCKED-BY EN-8 Phase 2** (server-tier config + hosted files + the two CLI scripts live on `feat/en8-server-hosted-audio`, not develop). Ties EN-12 (admin log viewer). DB migration coordination.
- **Open items — RESOLVED (owner 2026-07-17):** (1) **Postgres tables** (not JSON); (2) **Level 0** default scope confirmed; (3) **good/bad/re-record + notes** (reason taxonomy deferred); (4) MVP = §4 loop **+ automated silence/loudness scoring pulled forward** from §8. (5) sequencing: build the **EN-8-independent slice now on develop**, server-tier + `pregen --from-queue` stubbed behind the EN-8 config seam. See doc §10.
- **✅ BUILT (claude-en23, 2026-07-17, branch `feat/en23` off develop; commits 005c3e3 / 8a10f53 / 659f774 / 6db2677 / 49743e2):** Audio tab in `AdminView` (scope selector, coverage+tier+signals list, inline preview, verdict+notes, enqueue); Web-Audio silence/loudness scoring (`audioSignals.ts`, RMS/peak/silent-ratio/dead-air + raw-PCM fallback); EN-8 server tier stubbed behind `config.audio.serverBase` (honest "pending EN-8", no hardcoded fallback); reuses exported `linesForSituation()` + `buildKey`; all repo errors via `logger`+`userMessage`.
- **✅ MIGRATION 00014 APPLIED & VERIFIED LIVE (2026-07-17, owner-approved):** applied via the `aws-1-eu-west-2` Supavisor session pooler (direct IPv6 host unreachable — no IPv6 route on this machine; see APPLIED.md). Verified live: both tables `rowsecurity=t`, 3 admin policies each, silence-scoring cols + partial-unique live-index present. **Live DB round-trip verified** (psql): valid verdict insert OK, invalid verdict rejected by CHECK, enqueue OK, duplicate-pending enqueue rejected by the partial-unique index (23505 — the idempotency `enqueueRegen` relies on), select-back + cleanup OK.
- **✅ TESTS (properly exercise EN-23 functions):** tsc clean, eslint clean, **vitest 408/408** — **34 EN-23 unit tests**: `audioSignals` (silence/loudness math, pcm decode, suspicious rollup), `audio-download.enumeration` (exported `linesForSituation`), `ttsAudioReviewRepo` (happy + no-client + query-error typed surfaces + 23505 idempotent), `useAudioReview` (load→enumerate→merge; verdict & enqueue optimistic + rollback-on-error; non-admin no-op), `audioServerTier` (null base / present / missing / throw). control-inventory drift exit 0.
- **REMAINING:** (a) live **Playwright e2e** run of `tests/e2e/admin/13-admin-audio-panel.spec.ts` (DB round-trip test no longer self-skips now 00014 is applied) — needs operator-provisioned `.env.local`+admin creds in the worktree (cp permission-gated) OR merge-to-develop + run in the main worktree; (b) **merge `feat/en23`→develop**. EN-8 wire-up (server-tier presence + `pregen --from-queue` consumer) lands when EN-8 reaches develop.
- **Findings:** `src/lib/audio-download.ts` carries a real `\x00` byte (offset 6545, deliberate dedup delimiter — trips the queue binary-file guard; future cleanup candidate); project tsconfig is **non-strict** (no `strictNullChecks`) so `if (!r.ok)` doesn't narrow discriminated unions — handled via an explicit `isRepoError` guard.
- **Owner:** Agent E (`feat/*`) + DB agent (migration apply) + ops (CLI queue consumer). **Status:** APPROVED + BUILT (EN-8-independent slice); migration-apply / live-e2e / merge operator-gated.

### TB-15 — Tutor-tab free chat can't send a message (silent no-op) (reporter: owner, 2026-07-16) — `FIXED (on develop; regression e2e proven to bite pre-fix; awaiting release cut)`
- **Report (owner):** opening the Tutor from the nav shows the "Olá! I'm João… What's on your mind?" greeting, but typing + send does nothing.
- **Root cause (code-read 2026-07-16):** `handleSendMessage` (`useTutorSession.ts:414`) begins `if (!chatSession) return;` — it bails BEFORE adding the message or clearing the input, so a null session = every send is a silent no-op (message never appears, input never clears). The Tutor-tab free chat **shares one `session`** with the AI-Practice/Help modal, and the reducer **nulls `session` on close** (`CLOSE`/`RESET_FOR_LOGOUT`), while `initChat` (`:129`) only re-runs on `[user, profile.id, profile.selected_tutor_id]` — never on modal close. So after any practice/Help session is opened+closed (or if `initChat` failed), the free-chat session is gone and can't be recreated → sends do nothing.
- **Fix (design):** give the Tutor-tab free chat a robust session lifecycle independent of the modal — options: re-init the session on send when null; don't null the shared session on modal close (or keep a separate free-chat session); and make `handleSendMessage` surface an error/toast instead of a silent return when it can't send. Belongs with the EN-21 chat rework (per-mode session lifecycle) but is a **live functional break** — candidate for a quick standalone fix first. Add a regression e2e (open Tutor → send → message appears).
- **FIX (2026-07-16, on develop):** `handleSendMessage` (`useTutorSession.ts`) no longer bails on a null `chatSession`. It now (1) validates input FIRST, then (2) lazily (re)creates the shared session via `geminiService.startChat(tutor)` when it is null (using a local `session` binding so the same-tick send doesn't depend on the async state update), and (3) surfaces a calm error toast with a correlation `Ref` (`userMessage('CHAT_SEND_FAILED', …, event.request_id)`) if session creation fails — no more silent return. This heals the shared-session model after any Practice/Help modal open+close without changing the modal's lifecycle (deferred the fuller per-mode session split to EN-21).
- **Regression (proven):** `tests/e2e/user/59-tutor-free-chat-send-after-modal-close.spec.ts` — opens the Tutor tab, opens the Practice modal and closes it (dispatches `CLOSE_PRACTICE`, nulling the shared session — the exact TB-15 precondition), then sends a free-chat message and asserts BOTH the user message and the mocked model reply render and the input clears. Verified to **FAIL** against the pre-fix `if (!chatSession) return` and **PASS** with the fix (the test genuinely bites). Full unit suite 270/270 green; tsc clean.
- **Owner:** Agent E (`feat/*`). **Status:** FIXED on develop — awaiting the develop→main release cut to reach testers. The fuller per-mode session lifecycle stays with EN-21.

### TB-16 — Video viewing area + pre-seeded videos "appear gone" (reporter: owner, 2026-07-16) — `PARTIALLY FIXED on develop 2026-07-17 (29647cd): VideoPlayer now renders an explicit "video unavailable" state instead of a silent blank (kills the "gone" symptom) + component test. OPEN follow-ups: data-liveness audit/replace of dead seeded YouTube IDs, and legacy video_url ↔ pack media[] convergence (FE3)`
- **Report (owner):** "the video options — a place where we could view videos, plus some pre-seeded videos — are gone; investigate if that was on purpose."
- **Investigation (read-only git-history + code audit 2026-07-16):** the video feature was **NOT removed** — on purpose or incidentally. Everything is present on develop: user-facing viewing (`src/components/VideoPlayer.tsx` YouTube embed; `src/features/learning/LessonDetailModal.tsx:66-74` "Video Lesson"; `src/features/practice/SituationPicker.tsx:238-240` situation `media[type==='video']`), **25 pre-seeded videos** in `src/content/packs/seed-course.ts` (+ 3 legacy `video_url` in `src/data/lessons.ts`), and the suggest→admin-approve flow (`SuggestVideoModal.tsx` → `video_suggestions` table, migration `00003`; `AdminPanel.tsx` queue). `git log --all --diff-filter=D` on video paths = **zero deletions**; `video_suggestions` never dropped. Only relevant commit: `1f915a0` (2026-07-11) swapped two DEAD YouTube IDs for working ones (removed nothing structural).
- **Most likely cause of "gone":** data-layer, not code — dead/region-blocked YouTube URLs render blank (`VideoPlayer.tsx:16` returns `null` for unparseable URLs; dead/geo-blocked IDs show empty embeds), so video sections silently vanish. Same failure mode `1f915a0` already fixed once for d1/d15. A batch of the 25 seeded IDs may now be dead. Compounded by a known source-of-truth split (LessonDetailModal reads legacy `lesson.video_url`; the pack has its own `media[]`) — tracked as **FE1/FE2/FE3 in `docs/REQUIREMENTS-TRACKER.md`**.
- **Next action (follow-up, deferred per owner):** (1) oEmbed liveness-check all 25 `seed-course.ts` URLs + 3 legacy `video_url` links; replace dead/geo-blocked IDs; (2) make `VideoPlayer` surface an explicit "video unavailable" state instead of rendering `null` (kills the silent blank); (3) converge legacy `video_url` and pack `media[]` onto one source (FE3). Ties FE2 "recurring video audit."
- **Owner:** Agent E (`feat/*`) + content. **Status:** OPEN (queued follow-up; verify-then-fix — no removal happened, so nothing to revert).

### TB-17 — "Time spent" is incorrect (inflated + under-persisted) (reporter: owner, 2026-07-16) — `FIXED (client) on develop 2026-07-17 (c24642b): active-time gating + partial-minute flush. Server-side additive write (cross-device race) STILL HELD pending DB approval`
- **Report (owner):** time spent is not correct.
- **Root cause (VERIFIED, code-read 2026-07-16 `src/hooks/useTimeTracking.ts`):**
  - A `setInterval(…,1000)` ticks `totalTimeInSeconds`+1 every WALL-CLOCK second (mounted once, `App.tsx:171`), with **no pause on background/blur/idle** — no `visibilitychange`/`document.hidden` handling. It keeps counting while the tab is backgrounded, the phone is asleep, or the user is idle → time **inflates** far beyond real study time. (Strongest cause of "not correct" = too high.)
  - Persists `total_time_spent += 60` only when `totalTimeInSeconds % 60 === 0`; any **partial minute is discarded** at unmount/logout (`resetTimeTracking`, `App.tsx:234`) → systematic **under-persist** over short sessions.
  - Read-modify-write on `profile.total_time_spent` (client-computed base) = **last-writer-wins race** across tabs/devices/offline; `src/lib/sync-queue.ts:172-178` documents that a server-side additive path for these counters "hasn't landed."
- **Persistence:** `profiles.total_time_spent integer DEFAULT 0` (seconds), migrations `00001`/`00011`. Cumulative, server-persisted; display adds live `totalTimeInSeconds` (`SettingsView.tsx:172`).
- **✅ OWNER DECISION (2026-07-17):** measure **ACTIVE time with a 30-second idle timeout**, NOT app-open wall-clock. Active = visible tab AND interaction within the last 30s. Adopted assumptions (owner may veto): audio playback counts as activity (listening app); backgrounding (`visibilitychange` hidden) pauses immediately. Partial-minute flush on unmount/logout rides along. **Server-side additive write (cross-device race fix) is HELD pending explicit DB-migration approval** — the client active-time fix ships first with no schema change.
- **✅ CLIENT FIX SHIPPED (2026-07-17, `c24642b` on develop):** `src/hooks/useTimeTracking.ts` now accrues a second only when the tab is visible AND a qualifying interaction (`pointerdown`/`keydown`/`scroll`/`touchstart`, plus `visibilitychange→visible`) happened within the last 30s — a backgrounded/idle tab no longer inflates the total. On logout `resetTimeTracking` flushes the unsynced sub-minute tail additively before reset (under-persist fix). Gating + flush math extracted to pure helpers `src/lib/timeTracking.ts` (`shouldCountSecond`, `unsyncedSeconds`) with **10 unit tests** (`src/lib/__tests__/timeTracking.test.ts`); full suite 431 green, tsc/eslint/build clean. No schema change. **NB (owner assumption pending veto):** "audio playback counts as activity" is NOT yet wired — the hook has no audio signal and wiring it would touch the audio layer (other agents' territory). Passive listening with no interaction pauses after 30s. Tracked as sub-item below.
- **STILL OPEN — server-side additive write (cross-device last-writer-wins race):** HELD pending explicit DB-migration approval. Route the increment through a server-side additive RPC/trigger; add an additive-write test. Do NOT build without owner DB approval.
- **STILL OPEN — audio-as-activity:** if owner confirms passive audio playback should keep time counting, expose an activity signal from the audio/playback layer into `useTimeTracking` (or a shared activity store) and treat active playback as a `markActive` source. Coordinate with the audio owners.
- **Next action:** owner to (a) confirm/veto the audio-as-activity assumption, (b) approve the DB migration for the server-additive write. Both are follow-ons; the client inflation/under-persist fix is live.
- **Owner:** Agent E (`feat/*`) + DB. **Status:** FIXED (client) — server-additive + audio-as-activity remain OPEN, owner-gated.

### TB-18 — Streak is incorrect (write-side is UNIMPLEMENTED — permanently 0) (reporter: owner, 2026-07-16) — `OPEN (root cause found — DECISIVE; NEEDS requirements + approval before build)`
- **Report (owner):** streak is not correct.
- **Root cause (VERIFIED, exhaustive grep + code-read 2026-07-16):** `profiles.streak` and `profiles.last_active` are **NEVER written after profile creation.** The ONLY writes are the initial-row defaults (`useAuth.ts:69,73`; `useSettings.ts:409,413` — both profile *creation*). There is **no increment-on-activity anywhere** — no client updater, no edge function, no DB trigger (existing triggers are only `on_auth_user_created` + `updated_at`). `last_active` isn't even bumped on login. So `profile.streak` stays at its creation default (**0**) forever, and the elaborate `useHome` freeze machinery (`useHome.ts:169-214`) reconciles against a `last_active` that never advances. **This isn't "incorrect" — the streak write-side does not exist.**
- **Secondary (VERIFIED):** the streak-FREEZE balance lives in `platform.storage` per device (`home:streak-freeze:${userId}`, `config.ts:294-304`) — **not server-persisted**, so it diverges across devices/reinstall. Timezone split latent: `useHome` diffs day boundaries in LOCAL time while `last_active` is a UTC `timestamptz` (`00001`) → off-by-one near midnight once a writer exists.
- **Persistence:** `profiles.streak integer DEFAULT 0`, `profiles.last_active timestamptz` (`00001:10,14`). Freeze balance: client-only (no DB column — documented seam).
- **Product decisions needed before build:** (1) what defines a "day" — UTC or user-local? (2) what counts as qualifying daily activity? (3) should the freeze balance be server-persisted?
- **Next action:** implement the missing write path — on a qualifying daily activity bump `last_active` + increment/reset `streak` (server-side RPC/trigger safest, given the same additive-counter race as TB-17); persist freeze server-side; pin the timezone. Add coverage: day-boundary increment/reset, freeze reconcile (AGENTS §3).
- **Owner:** Agent E (`feat/*`) + DB. **Status:** OPEN — NEEDS requirements + owner approval before coding. Pairs with TB-17 (both are the "computed/displayed but write-side broken-or-missing" pattern + the shared additive-counter race).

### EN-24 — Test-coverage enforcement is mechanical + the edge-function tree needs a test harness (owner directive, 2026-07-16) — `IN PROGRESS (coverage-gate BUILT + verified; edge-test debts open)`
- **Directive (owner):** "tests must always be updated/enhanced … this is not optional." Elevates AGENTS §3 from doctrine to a mechanically-enforced gate. Triggered by a 2026-07-16 coverage audit.
- **Audit result:** client/UI coverage discipline is strong (EN-20, EN-17a/18, EN-15, SEC-2, EN-10, EN-16, EN-8 client tiers all ship paired unit+e2e). Real gaps: (1) **`supabase/functions/**` has ZERO tests** — the entire edge tree is uncovered, incl. EN-8 `_shared/audioStore.ts` (data-loss-adjacent write/copy-confirm/delete); (2) **TB-13** European-PT TTS steering (`_shared/tts/router.ts`) shipped to prod with no paired test; (3) EN-8 server-tier e2e + audit round-trip absent. Plus a **drift**: TB-15's fix+spec were local-only — RESOLVED 2026-07-16 (pushed to origin/develop).
- **✅ BUILT + verified (2026-07-16):** mechanical **coverage-diff gate** — `scripts/check-test-coverage-diff.mjs` fails any range that adds/modifies `src/**` or `supabase/functions/**` code without a paired test in the same range (escape hatch: `[skip-coverage: <reason>]` in a commit message). Wired into `.githooks/pre-push` (local) + `.github/workflows/coverage-gate.yml` (CI — the repo's FIRST workflow, so it holds even under `--no-verify`). Verified against real ranges: PASSes TB-15 (code+spec), FAILs TB-13 (edge code, no test — proves it would have blocked TB-13), PASSes docs-only.
- **Update 2026-07-17 (EN-27 established a no-deno path + owner decision on the glue):** edge **decision logic** is now testable WITHOUT deno via the **pure-core extraction pattern** (`docs/TEST_MANUAL.md §8`; reference cores: `deleteUserData`, `tts/routeCore`, `log-sink/rows`, `ai-gateway/voiceLimit`) — several debts below can adopt this instead of waiting on the harness (e.g. TB-13 router order is now assertable against `routeCore`'s exported `DEFAULT_CHAIN`). The thin `Deno.serve` **HTTP glue** is, by owner decision, covered by the **mandatory agentic edge-glue review** (`AGENTS §4`), NOT the deno harness. So the deno harness is now **optional** (would only automate the glue); it is no longer blocking.
- **Open debts (logic → pure-core+vitest per EN-27; glue → agentic review; deno harness optional):**
  - EN-8 `audioStore` edge test → **folded into `plan-2026-07-16-en8-completion.yaml` step `test-edge-audiostore`** (Phase 3, gates the gemini prod deploy).
  - EN-8 server-tier e2e (`user/60`) + audit/pregen round-trip unit → **folded into the same plan, Phase 2a** (`test-server-tier-e2e`, `test-audit-roundtrip-unit`, gate the develop merge).
  - **TB-13 router steering unit/Deno test** — still owed (stand up the harness; assert DEFAULT_CHAIN locale-pinned order + buildChain precedence). Owner: Agent E.
  - Add a **`deno test supabase/functions`** CI job alongside the coverage-gate so edge coverage is enforced, not just diff-presence.
- **Owner:** Agent E (harness + edge tests) + platform (the coverage-gate + edge CI job may be harvested into the aidevops standard — see [[project-release-standardization-not-oneoff]]). **Status:** IN PROGRESS.

### EN-25 — Consolidate ALL admin functions under ONE nav admin entry point (reporter: owner, 2026-07-16) — `SHIPPED to PRODUCTION in 2026.07.17.1 (2026-07-17)`
- **✅ SHIPPED (2026-07-17):** single Admin nav → Review Queues / Content Studio / User Access / **Config**; global voice limit moved into Config; **per-user `voice_limit`** added to User Access (= EN-11); legacy admin-mode panel + toggle deleted (video moderation retained in Review Queues). Full gate green (tsc/eslint/276 unit/build/observability/cors/coverage-gate) + admin **e2e 4/4** (`tests/e2e/admin/12-admin-single-entry` + rerouted 01/02). develop→main release cut → auto-bump **2026.07.17.1** + CHANGELOG → staging→approve→**production** (asset-hash verified live on falamadeira.searchingfool.com). Spec: `docs/EN-25-ADMIN-CONSOLIDATION-REQUIREMENTS.md`.
- **Report (owner):** there are **two** confusing admin links — (1) the sidebar nav "Admin" (lower-right), (2) the "admin mode" toggle — "and neither does what I need." Some admin views have **no navigation to them at all**. Owner wants **a single admin link — the one in the navigation sidebar — that reaches ALL admin functions.**
- **Current state (verified 2026-07-16):**
  - **Sidebar "Admin"** → `AdminView` (`src/features/admin/AdminView.tsx`), 3 tabs: Review Queues / Content Studio / User Access (`role==='admin'` gate, `App.tsx:315/527`).
  - **"Admin mode" toggle** → legacy floating `AdminPanel` (`src/features/settings/AdminPanel.tsx`), **video-suggestion moderation only** — **DEPRECATED/pending removal** and **duplicates** the Review Queues tab (which already moderates `video_suggestions`).
  - **Global voice-limit control** lives inline in `SettingsView` gated by the admin-mode toggle — **not under either admin link, no nav to it** (the "screen I should see is not visible" symptom).
  - **Per-user voice-limit**: no UI at all (EN-11).
- **Target (owner-specified direction):** ONE entry point = the sidebar nav Admin link → a single admin surface exposing **every** admin function:
  1. keep Review Queues / Content Studio / User Access;
  2. **fold the global voice-limit config** (from SettingsView admin-mode) into an admin **Settings/Config** tab;
  3. **add per-user `voice_limit`** to User Access (delivers **EN-11**);
  4. **retire** the legacy `AdminPanel` + the `isAdminMode` toggle (its only unique function, video moderation, already lives in Review Queues — verify, then remove);
  5. ensure NO admin view is reachable only by a hidden toggle — everything hangs off the one nav link.
- **Coordination:** ⚠ an `ai-gateway` edge rename is in progress (another agent) — unrelated to this UI work but both may touch admin/settings-adjacent code; sequence. Touches `AdminView.tsx`, `SettingsView.tsx`, `UserAccessPanel.tsx`/`useUserAccess.ts`, `App.tsx` (remove admin-mode wiring), delete `AdminPanel.tsx`. Reserve shared files.
- **Coverage:** per AGENTS §3 + the new coverage-gate — e2e that the single nav Admin link reaches each function (incl. voice-limit set, per-user + global) and that the legacy toggle is gone; unit for the per-user `voice_limit` write.
- **Owner:** Agent E (`feat/*`). **Status:** NEEDS REQUIREMENTS confirmation (owner gave the direction; a short spec + owner approval before coding, per the requirements gate). Absorbs EN-11.

### EN-26 — Admin User Access: partial-email search + a browsable list of all users (reporter: owner, headed test 2026-07-16) — `OPEN (root cause found; small)`
- **Report (owner):** in Admin → User Access, you must remember the user's **entire** email to find them — search should match **partial** emails, not only complete ones. Also a **dropdown/list of all users** would help see who's in there.
- **Root cause (code-read 2026-07-16):** `useUserAccess.ts:88–90` does `.from('profiles').select('id,email,…').ilike('email', trimmed)` — `ilike` is case-insensitive but there are **no `%` wildcards**, so it's an EXACT (case-insensitive) match → partial emails miss. There is also **no user-list query** at all; the panel is a single exact-lookup box.
- **Fix direction (small, client + one query):** (1) partial search — `.ilike('email', '%' + trimmed + '%')` (and optionally match display name) returning **multiple** matches → render a picklist instead of a single target; guard against overly-broad/empty queries. (2) A browsable **all-users list/dropdown** — `select('id,email,subscription_tier,role').order('email').limit(N)` with a search filter (admin SELECT on any profile is already granted by RLS 00001:119). Consider pagination if the user count grows.
- **Relationship:** enhances the **EN-15** User Access panel and fits inside **EN-25** (consolidate admin functions — User Access tab). Could be built with EN-25 or standalone (it's independent + small).
- **Coverage:** e2e — partial email returns matches + picklist select sets the target; unit for the query builder. Per AGENTS §3 + coverage-gate.
- **Owner:** Agent E (`feat/*`). **Status:** OPEN — small admin-UX fix; no product decision needed (can fold into EN-25).

### TB-19 — Vocab flashcard: nested audio button inside the flip button — mid-card tap on short words plays audio instead of flipping (reporter: e2e run 23 / coverage audit, 2026-07-16) — `OPEN (root cause proven in-trace; fix is app-side restructure + regression test)`
- **Report:** found by the 2026-07-16 full regression (E2E-LIVE-RUN-TRACKER run 23): `user/15-practice-vocabulary-session` failed deterministically at deck card 21 — the word **"Ali"**. Trace-proven: the card never flipped because the click landed on the nested speaker button.
- **Root cause (code-read + trace):** the flashcard is a `role="button"` div (`onClick={flip}`, `VocabularyView.tsx` ~:269–283) whose front face centers `word + 44px SpeakerButton` (`stopPropagation()`, `:54–66`). **Interactive control nested inside a button role — ARIA/WCAG nested-interactive antipattern.** For SHORT words the geometric center of the card IS the speaker button, so a mid-card tap plays audio instead of flipping; screen readers announce a button inside a button (broken semantics regardless of word length).
- **User impact:** real users tapping the middle of a short-word card get audio instead of the flip they were promised ("tap to flip"); SR users get no coherent flip affordance at all.
- **Fix (design, app-side):** the audio control must not be a DOM descendant of the flip button — either (a) keep the card as the flip button and render the speaker as an absolutely-positioned SIBLING overlay inside a wrapper div, or (b) make the card a plain div with two explicit sibling buttons (flip surface + speaker). Keep ≥44px targets and the existing `aria-label` flip/back semantics. Same pattern exists on the back face (`:130`) — fix both.
- **Test note:** e2e worked around it with a corner click (`user/15`, commit `1e95802`) so the suite is green — **the workaround masks this bug in the suite**. The app fix MUST restore a plain center `flashcard.click()` in `user/15` (that click failing IS the regression guard) and should add a component test asserting no interactive descendant inside the flip button.
- **Owner:** Agent E (`feat/*`/`fix/*`). **Status:** OPEN — PF-11 a11y family; straightforward bug fix (no product decision needed).

### TB-20 — Tutor read-aloud: voice ignores the selected tutor AND alternates female/male within one message (reporter: owner, headed test 2026-07-16) — `OPEN (root cause found; ties EF-37/TB-13)`
- **Report (owner):** in "Start Today's Lesson" chat, playback "does not honor the choice made for the speaker, and sometimes it reads as a female and sometimes male in the same sentence/message."
- **Root cause (code-read 2026-07-16):**
  1. **Per-sentence burst of separate TTS calls.** `playMessageInChunks` (`src/features/tutor/useTutorSession.ts`) splits the reply by sentence and fires a **separate** `geminiService.playSpeech` → edge `tts` call for each chunk, ~600ms apart.
  2. **Gemini TTS is the only keyed prod provider (no Azure/locale-pinned key — TB-13/EF-37) and fails rate-correlated:** the provider comment (`_shared/tts/gemini.ts`) documents rapid bursts (~1s) returning empty audio / 503 **75–100%** of the time.
  3. **On failure, `playSpeech` falls back to DEVICE speech** (`platform.audio.speak`, browser default pt-PT) — which is **tutor-agnostic** and a single fixed browser voice (often female). So within one message, some chunks use the tutor's Gemini voice (e.g. male João = `Charon`/`Fenrir`) and others use the browser default → **alternating gender + ignores the selected speaker.**
  4. **Compounding (even on the server path):** `voiceForTutor` maps to only **4 voices by gender+age** (female>40 Zephyr / ≤40 Kore / male>40 Charon / ≤40 Fenrir) — distinct personas can share a voice, so the "selected speaker" isn't distinctly honored.
- **Fix direction:** (a) OPS: add a locale-pinned TTS key (`AZURE_SPEECH_KEY`) so server TTS stops 503-ing (EF-37/TB-13); (b) don't burst-fire per-sentence calls (batch or serialize with backoff); (c) if any fallback occurs, keep ONE consistent voice for the whole message (don't mix server+device mid-message); (d) consider a richer per-persona voice map. Ties **TB-13** (accent), **EF-37** (503 window), **TB-5/TB-14** (read-aloud model).
- **Owner:** Agent E (client playback) + ops (TTS key). **Status:** OPEN — root cause found; the device-fallback-mid-message mix is the primary "female/male in same message" cause.

### TB-21 — "Time spent" display: never scales past minutes + ambiguous (session vs total) label (reporter: owner, headed test 2026-07-16) — `FIXED on develop 2026-07-17 (5796f9d): scaling formatDuration (min→h→d) + explicit "Total time" label + unit test. NB: DISTINCT from TB-17 (computation/idle-pause) which stays OPEN pending your decision`
- **Report (owner):** time spent "is in minutes and continues in minutes even if it is days/weeks/months"; and it's "not clear if time spent is this session or total — it should be clear."
- **Root cause (direction):** `profiles.total_time_spent` / `totalTimeInSeconds` are stored in **seconds** (`src/types.ts:59`, `src/hooks/useTimeTracking.ts` / `App.tsx:171,413`); the display formats to **minutes with no roll-up** to hours/days, and the label doesn't distinguish per-session vs cumulative total. (Pinpoint the exact render/format site at fix time.)
- **Fix direction:** a duration formatter that scales (min → h → d, e.g. "3h 12m", "2d 4h"), and an explicit label ("Total time" vs "This session"). Small, client-only, deterministic.
- **Relationship:** DISTINCT from **TB-17** (which is the *computation/persistence* bug — the counter inflates because it ticks every wall-clock second with no idle/background pause). TB-21 is purely display/formatting/labelling. Fix can ship independently.
- **Owner:** Agent E (`feat/*`). **Status:** OPEN — small display fix; no product decision needed.

### TB-22 — Tutor free chat: no way back to the tutor start page once a conversation begins + dead header button (reporter: owner, headed test 2026-07-16) — `FIXED on develop 2026-07-17 (d35973c): dead Settings button replaced with a working "New chat" control (shown once a conversation exists) that clears chatMessages → welcome state, no reload + component test`
- **Report (owner):** after "Just Want to Chat" and sending, there is **no way to get back to the starting tutor page** (the welcome state with "Start Today's Lesson" / "Just Want to Chat") **without reloading the entire page.** "Poor UX."
- **Root cause (code-read 2026-07-16, `src/features/tutor/TutorChatView.tsx`):** the welcome/empty state is gated on `chatMessages.length === 0` (`:84`). Once any message exists it's hidden, and there is **no reset/back/"New chat" control** to clear the conversation — so the only way back to the start is a full page reload. `setChatMessages` is already a prop (`:20/:39`), so a control calling `setChatMessages([])` would restore the start page trivially.
- **Secondary (same component):** the header **Settings icon button (`:78–80`) has NO `onClick`** — it renders but does nothing (a dead control; poor UX + a latent bug). Either wire it (open Settings / tutor picker) or remove it.
- **Fix direction (small, client-only):** add a "New chat"/back affordance in the tutor header (or an empty-conversation shortcut) that calls `setChatMessages([])` (and optionally resets the shared session) to return to the welcome state; wire or remove the dead Settings button. Ties **EN-21** (chat rework — build the header/controls once) and **TB-15** (shared session lifecycle).
- **Coverage (per AGENTS §3 + the "every action control → outcome" principle):** e2e — from a live conversation, the back/New-chat control returns to the welcome state (Start Lesson / Just Want to Chat visible again) without a reload; assert the header button does something (or is absent).
- **↩ CORRECTION (owner, 2026-07-16):** the practice modal's **close X is fine** (acceptable) — the earlier "no way back from the practice lesson" was actually the confusion caused by the **adjacent audio/mute button also showing an X**. That is a distinct icon defect → split to **TB-23**. TB-22 is scoped to the free-chat no-back + dead header button only.
- **Owner:** Agent E (`feat/*`). **Status:** OPEN — small tutor-chat UX fix; no product decision needed (can fold into EN-21).

### TB-23 — Practice modal: mute/audio toggle uses a bare "X" icon when muted — confusing next to the close X (reporter: owner, headed test 2026-07-16) — `FIXED on develop 2026-07-17 (4b6ef9b): muted state uses lucide VolumeX, not a bare X + component test asserting the icon is volume-x, not the close x`
- **Report (owner):** in the "Start Today's Lesson" chat, the close X is fine, but the **audio button next to it also shows an X** (when muted), which is very confusing — it reads like a second close/dismiss.
- **Root cause (code-read 2026-07-16):** `TutorPracticeModal.tsx:124` renders `isSoundEnabled ? <Volume2/> : <X/>` — the **muted state uses a bare `X`**, placed immediately next to the close X (`:131`). Two Xs side by side.
- **Fix (trivial, client-only):** use a proper muted-speaker icon for the off state (e.g. lucide `VolumeX`), never a bare `X`. Keep the `aria-label`/`aria-pressed` mute semantics. One-icon swap.
- **Owner:** Agent E (`feat/*`). **Status:** OPEN — trivial icon swap; no product decision needed.

### EN-27 — Error-handling hardening sweep: eliminate quiet failures (owner directive 2026-07-16: "make sure it is not quiet — it logs meaningfully") — `DONE (merged to develop 2026-07-17; all error-handling LOGIC outcome-tested via vitest cores; thin Deno.serve HTTP glue covered by the mandatory agentic edge-glue review — AGENTS §4 + TEST_MANUAL §8)`
- **MERGED + tested on develop (2026-07-17):** merged; vitest **370 green** (grew as this + other agents' work landed); full live e2e on develop **127 passed / 4 failed → all 4 resolved**: user/42 (quiz content) + user/55 (evidence-client auth-session) passed on fresh re-run (flaky/environmental); user/47 @clean + user/56 help were **harness-stale** and fixed (consoleGuard 503-exempt didn't follow the gemini→ai-gateway rename; help chip needed `.first()` for AI-repeat nondeterminism) — both re-run green. See E2E-LIVE-RUN-TRACKER.
- **Test-coverage state — FULL (owner directive "full coverage, code + tests aligned"):** every EN-27 fix now has a real OUTCOME test. The 4 edge functions had no unit tests initially (no `deno`; they import `https://esm.sh/...` + Deno globals, so vitest can't load the handlers) — resolved via **Option-1 pure-core extraction** (2026-07-17): the error-handling DECISION logic was lifted into Deno/esm-free modules the handlers call, and unit-tested in vitest.
  - ✅ client/platform (outcome-tested): web audio, web storage, native storage, native speech, speech-fallback, audio-download, logger, useLessonModals, repository.
  - ✅ edge cores (outcome-tested, EN-27 Option-1): `deleteUserData` (delete-account partial-failure/privacy), `tts/routeCore` (EF-37 provider-failure persist + chain fallback + BYO-ref + all-fail), `log-sink/rows` (batch caps + row mapping + spoofed-user_id→NULL + details clamp), `ai-gateway/voiceLimit` (TB-8/EN-11 precedence + daily reset + allow/deny). vitest 270→370.
  - ✅ residual (thin `Deno.serve` HTTP glue) — **covered by a MANDATORY agentic review activity, not an automated test (owner decision 2026-07-17):** the deno harness for the glue is declined; instead any `supabase/functions/**` diff gets a `/code-review` (`/security-review` for auth/secrets) applying the **edge-glue checklist** (`docs/TEST_MANUAL.md §8`) — routed in `AGENTS.md §4`. This closes EN-27's coverage: decision logic = vitest cores; HTTP glue = agentic git/code review. The `--strict` gate still backs it (no bare-console).
- **REUSABLE PATTERN (documented in `docs/TEST_MANUAL.md §8`):** edge error-handling is covered WITHOUT deno by putting the decision logic in a pure sibling module (no `https://` / no `Deno.*`), calling it from the thin `Deno.serve` handler, and unit-testing the module in vitest (`vitest.config.ts` scans `supabase/functions/**`); the handler glue is covered by the agentic edge-glue review. Every extracted core file carries a header explaining this.
- **STATUS: DONE** — all error-handling logic outcome-tested; HTTP-glue coverage delegated to the agentic review activity. No open EN-27 work.
- **Delivered (branch `fix/en27-error-hardening`, 4 commits `a535f8d`/`83c6b7d`/`11c3b69`/`c0a90f1`; plan `plans/plan-2026-07-17-en27-error-hardening.yaml`, state `.plan-state-en27.yaml`):** all P0 + P1 + the actionable P2/P3.
  - **P0:** delete-account partial-failure now fails loud (extracted pure `_shared/deleteUserData.ts` + per-step persistLog; **unit-tested** — the privacy bug can't regress); tts router console-only → persistLog at both sites (EF-37 storm would now leave queryable rows); web-audio device-speech onerror logs; native storage/speech read/probe/transcript-fetch failures log before null/no-match (distinguishing routine not-found from real read errors).
  - **P1:** web IndexedDB degrade logs; speech-fallback errors log before the optional errorCb; ai-gateway profile/global_settings/usage query errors checked; audio-download splits deterministic (4xx/edge-code) from transient before retry (**unit-tested**); Suggest-Video silent guard now logs + Ref-toasts (**unit-tested**).
  - **P2:** repository degraded-cache background-refresh swallow logged; log-sink no longer leaks raw dbMessage; logger `droppedCount` + `getDiagnostics()` so "Send Logs" never silently omits (**unit-tested**).
  - **P3 (enforcement — the keystone):** `check-observability.mjs --strict` is now a **HARD preflight stage** (any new console-only error path / hardcoded fallback fails the build; toast-no-log stays advisory); `check-standards.sh` adds a WARN scan for session/client bare-return guards (the TB-15 silent-no-op class) — 19 candidates surfaced for future case-by-case triage.
  - **Tests:** +18 (vitest 270→288): deleteUserData partial-failure, web-audio onerror, web-storage IndexedDB-degrade, audio-download deterministic/transient, logger droppedCount, useLessonModals guard-branch.
- **Scoped deviations (documented):** (a) useHome streak-freeze persist stays **log-only** — recoverable low-impact grace loss; a load-time toast would over-notify (doctrine §5 severity=channel). (b) Suggest-Video guard delivered as a **handler unit test** not e2e — the branch is unreachable via live UI when signed in. (c) event_type UPPER_SNAKE casing check **dropped** — a scan showed lowercase snake_case is the dominant client convention (82 sites); no norm to enforce, not a quiet-failure concern.
- **Owner action before merge:** run the full live e2e (`npm run test:e2e` with `.env.local` present — it could not run in the isolated worktree because copying the secrets file was correctly blocked); then merge `fix/en27-error-hardening` → develop. Edge changes (delete-account/router/ai-gateway) reach users only on the next operator edge deploy.
- **Owner:** any agent / operator (merge + edge deploy operator-gated). **Status:** FIXED-PENDING-MERGE.

<details><summary>Original findings (verified 2026-07-16) — retained for reference</summary>
- **Source:** whole-repo `/error-review` sweep 2026-07-16 (all of `src/**` + `supabase/functions/**`; findings verified against code, not just reported). Overall: the doctrine is well-followed — logger/userMessage discipline is strong in features, sync-queue, geminiService; TB-15's fixed send path is now the exemplar (lazy re-init + logger.error + Ref toast). The gaps cluster in the **platform adapter layer**, the **edge TTS router**, and **delete-account**.
- **P0 — CRITICAL (fix first):**
  1. `supabase/functions/delete-account/index.ts:38-44` — the 7 table deletes are un-destructured `await`s; supabase-js returns PostgREST failures in `{error}` (does NOT throw), so **a failed table delete is invisible, never reaches the catch, and the function still returns `{deleted: true}`** — user asked for deletion, rows silently persist (privacy/GDPR class). Fix: check `error` per delete, persistLog the failed table + steps completed, fail the request.
  2. `supabase/functions/_shared/tts/router.ts:160,237` — provider failures + BYO-key-ref misses log via `console.*` only, never `persistLog` → **the EF-37 503 storm left ZERO queryable rows in public.logs**; ops cannot ask "which providers failed for whom in the last hour". Fix: persistLog (WARN/ERROR) with provider, status, attempt; keep the console line.
  3. `src/platform/web/audio.web.ts:176` — `utterance.onerror = () => options?.onEnded?.()` — device-speech (the TTS fallback of last resort) failing = **silence reported as success**, unlogged. Fix: logger.error with the utterance error before onEnded.
  4. `src/platform/native/storage.native.ts:199-213 (+160)` — blob/pref read errors return `null` identical to "absent"; corrupt offline audio/user state looks like "nothing saved" (**this is the TB-9 symptom shape**). Fix: log read/parse failures with key before returning null.
  5. `src/platform/native/speech.native.ts:87,116,268` — availability-probe failure, final-transcript fetch failure (user's speech WAS captured, then lost → surfaces as "no match"), and stop() failure are unlogged or callback-only (**TB-6/TB-10 symptom shapes**). Fix: logger call before each callback/fallback.
- **P1 — HIGH:**
  6. `src/platform/web/storage.web.ts:72-80` — IndexedDB open failure silently degrades to localStorage/memory (state lost on reload), unlogged. 7. `src/platform/speech-fallback.ts:107,137` — recording/transcription failures route to an OPTIONAL errorCb only. 8. `supabase/functions/ai-gateway/index.ts:150,187` — profiles/global_settings query + usage-update errors un-destructured → silent free-tier/limit-5 fallback on DB failure. 9. `src/lib/audio-download.ts:236-256` — retry loop doesn't split deterministic (4xx) from transient; inspect EdgeFunctionError.code before retrying. 10. `src/features/learning/useLessonModals.ts:87` — `if (!selectedLesson || !supabase || !user) return;` silent no-op on Suggest-Video submit (TB-15 class).
- **P2 — MEDIUM:** `src/content/repository.ts:409` `.catch(() => undefined)` on degraded-cache background refresh; `src/features/home/useHome.ts:295,303` streak-freeze persist/load failures logged but user-invisible (freezes silently evaporate); logger self-observability (`logger.ts:114-165` — persist-queue requeue truncation + ring-buffer overflow drop events uncounted → add droppedCount to diagnostics); `log-sink/index.ts:161` leaks raw `dbMessage` to the client envelope (sanitize; ops keeps it via console).
- **P3 — enforcement so the class can't recur:** (a) promote `check-observability.mjs` BARE-CONSOLE (edge error paths) from WARN to HARD once router.ts is fixed; (b) add a WARN-mode standards check for silent no-op guards in user-action handlers (`if (...) return;` with no logger/toast in `handle*` functions); (c) event_type casing lint (one `sync_queue_flush_failed` vs UPPER_SNAKE elsewhere).
- **Per AGENTS §3:** each fix ships with a test that fails pre-fix where feasible (delete-account partial-failure is unit-testable against a mocked admin client; platform adapters have __tests__ dirs; router persistLog is assertable in the edge harness — ties EN-24's edge-test debt).
- **Owner:** any agent (P0 items are small, surgical); edge deploys operator-gated as usual. **Status:** READY — this enforces the already-approved observability doctrine (no new product behavior); P0.1 (delete-account) should land before the next release cut.

</details>

### EN-28 — New-release notification: tell open/installed users a new version is available/installed → link to release notes (reporter: owner, 2026-07-17) — `NEEDS REQUIREMENTS (then owner approval before coding — AGENTS §3)`
- **Report (owner 2026-07-17):** "when an update is deployed, any user who has the app open or installed — beta testers or normal users — should get a notification of some kind that they'll see, that a new release has been installed or can be installed, taking them to the release notes."
- **Why it fits the stack:** the app is a **PWA** (`vite-plugin-pwa` + Workbox; `dist/sw.js`, 60-entry precache). The service-worker update lifecycle is the natural, standard mechanism — no push infrastructure needed for the "app is open" case. Two distinct situations to design for:
  1. **App open (foreground) when a new build deploys** → the new SW is downloaded and enters `waiting`. `vite-plugin-pwa`'s `registerSW({ onNeedRefresh })` fires → show a non-intrusive "New version available — Reload" prompt; on accept, `updateSW()` activates the waiting SW and reloads. This covers both beta (staging) and normal (prod) since both are PWAs.
  2. **Installed PWA reopened after an update already activated** → detect that the running version changed since last seen (compare a build/version stamp, e.g. the root `VERSION` / a `__BUILD_ID__` injected at build) and show a "You're now on vX — see what's new" toast/banner.
- **Take them to the release notes:** needs a **release-notes source + surface** (open question): a `CHANGELOG.md`-derived page, an in-app "What's new" modal, or a hosted release-notes URL. The notification's action opens it, anchored to the new version. Ties the release-engineering CHANGELOG discipline (global standard) — the same version bump that cuts a release should produce the notes the notification links to.
- **Open questions (for the requirements doc):** (a) notification UX — toast vs banner vs modal; dismiss/snooze; force vs optional reload; (b) beta-vs-normal differentiation (do beta testers get a louder/earlier prompt?); (c) release-notes format + where it lives + how it's versioned/anchored; (d) how the running build knows its version + the "last seen" comparison for the reopened-installed case; (e) whether to also cover true background push (out of scope for v1 — the open/installed cases need no push service); (f) staging vs prod behavior (staging updates far more often — avoid nagging beta testers every deploy).
- **Reuse / relationships:** `vite-plugin-pwa` update lifecycle (already in the build); the version-bump/CHANGELOG release-engineering standard; EN-17 App Capability Registry / manual (a "What's new" surface could live near the user manual); the toast system (`useToast`) + EN-18 take-me-there pattern for the "see what's new" action.
- **Owner:** Agent E (`feat/*`) + release engineering. **Status:** NEEDS REQUIREMENTS — captured from owner feedback 2026-07-17; no coding until a written spec is drafted AND owner-approved (AGENTS §3 requirements gate).
