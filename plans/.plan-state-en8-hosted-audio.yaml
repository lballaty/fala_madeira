# File: plans/.plan-state-en8-hosted-audio.yaml
# Description: Resumable execution state for plan-2026-07-15-en8-hosted-audio.yaml
#   (/execute-plan-autonomously). NB: the generic .plan-state.yaml is occupied by the completed
#   plan-2026-07-09-full-product.yaml — do not overwrite it; this EN-8 run uses its own state file.
#   Work happens in the feat worktree fala_madeira-feat on branch feat/en8-server-hosted-audio.
#   OPERATOR-GATED steps (Verpex spike, shared-prod migration apply, edge/verpex deploys, pregen
#   run, flag flip, verify-win) CANNOT run autonomously → marked `deferred-operator` and surfaced,
#   not force-failed; the walk executes code steps and skips server/infra steps it cannot perform.
# Author: Claude (EN-8 owner)
# Created: 2026-07-15
# Last Updated: 2026-07-15

plan: plans/plan-2026-07-15-en8-hosted-audio.yaml
worktree: /Users/liborballaty/LocalProjects/GitHubProjectsDocuments/fala_madeira-feat
branch: feat/en8-server-hosted-audio
mode: autonomous

steps:
  setup-branch:               { status: succeeded, note: "feat/en8 off origin/develop 7dd8af3" }
  verpex-feasibility-spike:   { status: in-progress, note: "READ-ONLY recon done 2026-07-15 (owner granted Verpex access, review-then-plan-then-write). Staging = /home/gomadeir/testfalamadeira.searchingfool.com (cPanel/CloudLinux, Frankfurt). GREEN indicators: PHP 8.2.31 CLI, allow_url_fopen=1, curl+openssl yes (outbound HTTPS feasible); rsync + crontab present; crontab empty; NO /audio dir yet (clean). CRITICAL FINDING: root .htaccess SPA fallback rewrites ANY non-existent path -> index.html (200), so a Verpex MISS returns the HTML shell with 200, NOT 404 -> client tryFetchPcm would accept HTML as PCM. FIX (2 parts): (a) client guard — treat text/html content-type as a miss (client-only, add now); (b) .htaccess — exclude /audio/ from the SPA fallback so misses 404 (Verpex write, needs approval). Remaining spike = WRITE test (mkdir /audio + serve _spike.bin + _probe.php outbound + 1-min cron) — pending owner approval." }
  extract-audio-key:          { status: succeeded, commit: fba5d7f }
  test-audio-key:             { status: succeeded, commit: fba5d7f }
  extract-lines:              { status: succeeded, commit: e21b311, note: "moved to content/lines.ts; validated via audio-download.test 8/8; incidentally removed a pre-existing stray NUL byte from the file" }
  test-lines-moveonly:        { status: succeeded, commit: 83e6361, note: "lines.test.ts locks full enumeration + dedup" }
  enumerate-roleplay-options: { status: succeeded, commit: 83e6361, note: "option.text enumerated in default voice; 13/13 tests" }
  commit-phase1:              { status: succeeded, note: "satisfied by incremental commits fba5d7f/e21b311/83e6361" }
  client-voicetype-mirror:    { status: succeeded, commit: 0cba5b9 }
  test-voicetype-parity:      { status: succeeded, commit: 0cba5b9, note: "3/3; mirrors router.ts mapping table" }
  normalize-keys-and-hostable:{ status: succeeded, note: "commit deferred to commit-phase2 (branch incoherent until Listening shares the key space). geminiService keys by resolveVoice(voiceType||voiceTypeForTutor(tutor)); shared resolveVoice added to lib/voiceType.ts; hostable threaded via PlaySpeechOptions. DEVIATION: useSpeechPlayback left NOT-hostable (it is shared with TutorChatView free-chat + user vocab lookups — plan's curated assumption was wrong; code wins). hostable:true on useDrillAudio/useEntryAudio/audio-download; hostable:false explicit on useTutorSession free-chat. tsc 0 + 242 tests green." }
  route-listening-shared:     { status: succeeded, commit: bce9e45, note: "listeningAudio now keys via audioCache.buildKey('default',voiceType,text) (shared space; drops listening_* scheme) + hostable:true; non-default-rate uncached guard preserved. DEFERRED to Phase 4: route listening cache-MISS through the shared server-tier fetch (needs rate-aware fetch) so hosted dialogue serves from Verpex." }
  test-key-and-scope:         { status: succeeded, commit: bce9e45, note: "geminiService.test: keys by resolved archetype (not tutor id) + hostable true/false/undefined forwarding; audio-download.test: pre-check key uses resolved 'teacher' (EN-7 fix) + hostable:true. 248 tests total." }
  commit-phase2:              { status: succeeded, commit: bce9e45, note: "FOLLOW-UPS: (1) opt-in hostable:true for MissionsView/referenceAudio/useVocabularySession/scripted-simulator NPC (curated, out of plan's named 4); (2) per-consumer hostable for useSpeechPlayback curated plays; (3) Phase-4 listening server-tier read." }
  pinned-store-adapter:       { status: succeeded, commit: 4421425, note: "StorageAdapter gains getPinnedBlob/setPinnedBlob/pinnedUsage/clearPinned; web IDB DB_VERSION 2->3 (additive create-if-missing of 'audio_pinned'); native fm-pinned dir." }
  pinned-store-migration-test:{ status: succeeded, commit: 4421425, note: "ADDED fake-indexeddb devDep. storage.web.test.ts: real v2->v3 upgrade keeps 'audio' blobs + adds 'audio_pinned'; clearBlobs never touches pinned; pinnedUsage accounting." }
  wire-pinned-cache:          { status: succeeded, commit: 4421425, note: "audioCache.getPinned/setPinned/pinnedUsage; synthesizeCached reads cache->pinned->[server P4]->provider + writes pinned when opts.pinned; downloadForOffline pinned:true + bounds run by pinnedUsage()." }
  logout-clears-lru-cache:    { status: succeeded, commit: 4421425, note: "audioCache.clear()=LRU-only wired into App.tsx onLogoutCleanup (after SEC-2 clearDeviceUserState); pinned preserved. audioCache.test locks delegation." }
  commit-phase3:              { status: succeeded, commit: 4421425, note: "13 files, 252 tests green, pushed. Phase 3 DONE. Next: Phase 4 client-server-tier (config-server-audio first)." }
  config-server-audio:        { status: succeeded, commit: adcf18c, note: "config.audio.verpexBase (VITE_AUDIO_VERPEX_BASE, default /audio) + supabaseAudioBucket ('tts-audio'); pure publicObjectUrl(bucket,path) helper in lib/supabase.ts (no vite-env.d.ts change needed — vite/client index sig)." }
  client-server-tier:         { status: succeeded, commit: adcf18c, note: "fetchServerTier(key): GET verpex /audio/<keyToServerPath> then Supabase public URL; best-effort (404/CORS/timeout = miss, never throws). Hit warms LRU + skips provider. tts_source(cache|pinned|verpex|supabase|provider) emitted at debug. INERT until operator deploys server side." }
  test-tier-order:            { status: succeeded, commit: adcf18c, note: "5 cases: cache=0 fetch/edge; pinned=0; verpex200=1 fetch no provider; supabase200=2 fetch no provider; all-miss=provider. 257 tests total." }
  commit-phase4:              { status: succeeded, commit: adcf18c, note: "pushed. Phase 4 DONE. NEXT = Phase 5 (SERVER: edge write-back + Supabase buffer). storage-bucket-migration + deploy-edge are deferred-operator (owner: no Verpex/server changes without approval, staging-first). edge module code (edge-audiostore-module, edge-tts-writeback, edge-audio-sync-endpoint) can be WRITTEN now but NOT deployed." }
  storage-bucket-migration:   { status: deferred-operator, note: "applies to SHARED prod DB" }
  edge-audiostore-module:     { status: pending }
  edge-tts-writeback:         { status: pending }
  edge-audio-sync-endpoint:   { status: pending }
  deploy-edge:                { status: deferred-operator }
  commit-phase5:              { status: pending }
  verpex-pull-cron:           { status: pending }
  deploy-rsync-exclude:       { status: pending }
  deploy-verpex-crons-both:   { status: deferred-operator }
  commit-phase6:              { status: pending }
  pregen-script:              { status: pending }
  run-pregen-level0:          { status: deferred-operator }
  enable-writeback-flag:      { status: deferred-operator }
  coverage-report-script:     { status: pending }
  commit-phase7:              { status: pending }
  full-gate:                  { status: pending }
  update-docs:                { status: pending }
  verify-win:                 { status: deferred-operator, note: "gating proof; operator confirms on staging" }
