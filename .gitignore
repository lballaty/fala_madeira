node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Deploy credentials: .env.deploy is secret (git-ignored by .env* above); its template is committed.
!.env.deploy.example
# Local staging/approval state for the staged deploy (deploy-verpex.sh) — never commit.
.deploy-state.json

.admin-temp-credentials.txt

# Generated knowledge graph (graphify)
graphify-out/

# One-off DB inspection scratch scripts (safe to delete)
inspect-fk-columns.js
check2.mjs

# Supabase DB backup snapshots — contain auth.users PII (emails + bcrypt hashes) and role
# password hashes; never commit to git history. The runbook (README.md) and helper
# (backup-restore.sh) at the supabase/backups/ root ARE tracked; only the timestamped
# snapshot dirs are ignored. Force-add a snapshot only if you accept the secret exposure.
supabase/backups/*/

# Diagnostic probe scratch (session 2026-07-11; safe to delete)
_probe-vocab-hang.mjs
_probe-inserts.mjs

# Claude Code local settings + agent-local state (DF8)
.claude/settings.local.json
.claude/version-history.json
.claude/worktrees/

# Playwright e2e artifacts (generated; never commit the live session state under .auth/)
tests/e2e/.auth/
test-results/
playwright-report/
blob-report/
.playwright/
.probe-tmp.mjs

# E2E run snapshots + coverage evidence (generated by the runner; large, never commit)
artifacts/
